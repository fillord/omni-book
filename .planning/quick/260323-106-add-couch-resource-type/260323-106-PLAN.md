---
phase: quick
plan: 260323-106
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/validations/resource.ts
  - lib/resources/types.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Zod validation accepts 'couch' as a valid resource type"
    - "RESOURCE_TYPE_LABELS includes 'couch' with Russian label"
    - "Build passes with no type errors"
  artifacts:
    - path: "lib/validations/resource.ts"
      provides: "couch in RESOURCE_TYPES array and RESOURCE_TYPE_LABELS"
      contains: "couch"
  key_links: []
---

<objective>
Add "couch" to the legacy RESOURCE_TYPES list in lib/validations/resource.ts so the Zod validation schema accepts it as a valid resource type.

Purpose: Currently the Zod validation enum only allows staff|room|court|table|other. The niche config (beauty) already defines couch as a resource type, and i18n translations for resource_type_couch already exist in all 3 languages (RU/KZ/EN). But the Zod schema would reject "couch" during form submission. This plan fixes that gap.

Output: Updated validation file, passing build.

NOTE: lib/resources/types.ts is dead code (zero imports) -- update it for consistency but it has no runtime impact. Prisma has NO ResourceType enum (types stored as plain strings), so no migration needed.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/validations/resource.ts
@lib/resources/types.ts
@lib/niche/config.ts (already has couch in beauty niche, line 79)
@lib/i18n/translations.ts (already has resource_type_couch in all 3 languages)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add couch to RESOURCE_TYPES and RESOURCE_TYPE_LABELS</name>
  <files>lib/validations/resource.ts, lib/resources/types.ts</files>
  <action>
1. In lib/validations/resource.ts:
   - Add 'couch' to the RESOURCE_TYPES array: `['staff', 'room', 'court', 'table', 'other', 'couch'] as const`
   - Add couch entry to RESOURCE_TYPE_LABELS: `couch: 'Кушетка',`

2. In lib/resources/types.ts (dead code, zero imports, update for consistency):
   - Add `| 'couch'` to the ResourceType union type, with comment `// Beauty`

3. Run `npm run build` to verify no type errors and successful build.

NOTE: Do NOT touch prisma/schema.prisma -- there is no ResourceType enum in Prisma, types are stored as plain strings. Do NOT touch i18n translations -- resource_type_couch already exists in all 3 languages. Do NOT run prisma migrate -- not needed.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - RESOURCE_TYPES array includes 'couch'
    - RESOURCE_TYPE_LABELS has couch: 'Кушетка'
    - lib/resources/types.ts ResourceType union includes 'couch'
    - npm run build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Restart application</name>
  <files></files>
  <action>
Run `pm2 restart omni-book` to apply the changes to the running application.
  </action>
  <verify>
    <automated>pm2 status omni-book | grep -E "online|status"</automated>
  </verify>
  <done>pm2 shows omni-book status as "online"</done>
</task>

</tasks>

<verification>
- npm run build passes
- pm2 shows omni-book online
- grep confirms 'couch' present in lib/validations/resource.ts
</verification>

<success_criteria>
"couch" is accepted as a valid resource type throughout the validation layer. Application is rebuilt and restarted.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-106-add-couch-resource-type/260323-106-SUMMARY.md`
</output>
