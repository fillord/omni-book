---
quick_task: 260323-106
subsystem: resource-validation
tags: [resource-types, validation, beauty-niche]
key-files:
  modified:
    - lib/validations/resource.ts
    - lib/resources/types.ts
decisions:
  - No Prisma migration needed — resource types stored as plain strings
  - lib/resources/types.ts updated for consistency only (zero imports / dead code)
metrics:
  duration: "< 5 minutes"
  completed: "2026-03-23"
  tasks: 2
  files: 2
---

# Quick Task 260323-106: Add couch resource type — Summary

**One-liner:** Added 'couch' to Zod RESOURCE_TYPES enum and RESOURCE_TYPE_LABELS so beauty niche couch resources pass form validation.

## What Was Done

Added `'couch'` as a valid resource type to close the gap between the beauty niche config (which already defined `couch`) and the Zod validation schema (which only accepted staff | room | court | table | other).

i18n translations for `resource_type_couch` already existed in all three languages (RU/KZ/EN). No migration was needed — Prisma stores resource types as plain strings.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add couch to RESOURCE_TYPES and RESOURCE_TYPE_LABELS | a0801f6 | lib/validations/resource.ts, lib/resources/types.ts |
| 2 | Restart application | (no commit — pm2 operation) | — |

## Changes Made

**lib/validations/resource.ts**
- `RESOURCE_TYPES` array: added `'couch'`
- `RESOURCE_TYPE_LABELS`: added `couch: 'Кушетка'`

**lib/resources/types.ts** (dead code — zero imports)
- `ResourceType` union: added `| 'couch'  // Beauty`

## Verification

- `npm run build` passed with no type errors
- `pm2 status` shows both omni-book instances (id 12, 13) online

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- lib/validations/resource.ts contains 'couch' — confirmed by edit
- lib/resources/types.ts contains 'couch' — confirmed by edit
- Commit a0801f6 exists — confirmed by git output
- Build passed — confirmed by npm run build output
- pm2 omni-book online — confirmed by pm2 status output
