---
phase: quick-260323-1el
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/validations/resource.ts
autonomous: true
requirements: [QUICK-260323-1el]

must_haves:
  truths:
    - "All niche config resource type values are valid Zod enum members"
    - "RESOURCE_TYPE_LABELS has a label for every value in RESOURCE_TYPES"
    - "npm run build completes without type errors"
  artifacts:
    - path: "lib/validations/resource.ts"
      provides: "RESOURCE_TYPES tuple and RESOURCE_TYPE_LABELS mapping with all niche-required types"
  key_links:
    - from: "lib/validations/resource.ts RESOURCE_TYPES"
      to: "lib/niche/config.ts resourceTypes[].value"
      via: "z.enum(RESOURCE_TYPES) validates submitted type field"
      pattern: "z\\.enum\\(RESOURCE_TYPES\\)"
---

<objective>
Add all niche-specific resource type keys to RESOURCE_TYPES and RESOURCE_TYPE_LABELS in lib/validations/resource.ts.

Purpose: Niche configs (horeca, sports, medicine, beauty) reference resource type values that are not present in the Zod enum. Creating or editing a resource with these types currently fails validation.
Output: Updated lib/validations/resource.ts with 15 additional entries; build passes; service restarted.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@lib/validations/resource.ts
@lib/niche/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add niche resource types to RESOURCE_TYPES and RESOURCE_TYPE_LABELS</name>
  <files>lib/validations/resource.ts</files>
  <action>
    Replace the RESOURCE_TYPES tuple and RESOURCE_TYPE_LABELS in lib/validations/resource.ts.

    IMPORTANT: Use the keys that the niche configs in lib/niche/config.ts actually reference —
    NOT the keys listed in the task description where they differ. Specifically:
    - medicine uses `office` and `treatment_room` (NOT `office_scan` / `procedure_room`)
    - beauty uses `sink` (NOT `washing_station`)

    New RESOURCE_TYPES tuple (extend the existing array, keep original entries first):
    ```
    export const RESOURCE_TYPES = [
      'staff', 'room', 'court', 'table', 'other', 'couch',
      // Рестораны
      'vip_lounge', 'terrace', 'bar_counter', 'karaoke_room',
      // Спорт
      'trainer', 'field', 'pool', 'sauna', 'equipment',
      // Медицина
      'doctor', 'nurse', 'operating_room', 'laboratory', 'office', 'treatment_room',
      // Красота
      'master', 'manicure_table', 'sink',
    ] as const
    ```

    Add corresponding entries to RESOURCE_TYPE_LABELS (translations already exist in lib/i18n/translations.ts — use the same Russian labels):
    ```
    vip_lounge: 'VIP-зал',
    terrace: 'Терраса',
    bar_counter: 'Барная стойка',
    karaoke_room: 'Зал с karaoke',
    trainer: 'Тренер',
    field: 'Поле',
    pool: 'Бассейн',
    sauna: 'Сауна',
    equipment: 'Снаряжение',
    doctor: 'Врач',
    nurse: 'Медсестра',
    operating_room: 'Операционная',
    laboratory: 'Лаборатория',
    office: 'Кабинет (УЗИ/МРТ)',
    treatment_room: 'Процедурный кабинет',
    master: 'Мастер',
    manicure_table: 'Маникюрный стол',
    sink: 'Мойка',
    ```

    lib/resources/types.ts has a separate ResourceType union that is NOT used for Zod validation
    (it has its own older keys like `staff_member`). Do NOT modify that file — it is unused in
    the validation path and would require a larger migration. Only lib/validations/resource.ts
    needs updating.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - RESOURCE_TYPES contains all 24 values (6 original + 18 new)
    - RESOURCE_TYPE_LABELS has an entry for every value in RESOURCE_TYPES (TypeScript enforces this via Record&lt;ResourceType, string&gt;)
    - npm run build exits 0 with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Restart service</name>
  <files></files>
  <action>
    Restart the production service to pick up the rebuilt code:
    pm2 restart omni-book
  </action>
  <verify>
    <automated>pm2 show omni-book | grep -E "status|restarts"</automated>
  </verify>
  <done>pm2 shows omni-book status as "online"</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run: node -e "const {RESOURCE_TYPES, RESOURCE_TYPE_LABELS} = require('./lib/validations/resource.ts'); console.log(RESOURCE_TYPES.length)" — expect 24
2. All niche config resource type values (doctor, nurse, operating_room, laboratory, office, treatment_room, vip_lounge, terrace, bar_counter, karaoke_room, trainer, field, pool, sauna, equipment, master, manicure_table, sink) are present in RESOURCE_TYPES
3. Build passes clean
</verification>

<success_criteria>
- lib/validations/resource.ts has 24 entries in RESOURCE_TYPES
- RESOURCE_TYPE_LABELS maps all 24 types to Russian labels
- npm run build exits 0
- pm2 shows omni-book online
</success_criteria>

<output>
After completion, create `.planning/quick/260323-1el-bulk-resource-types-update/260323-1el-SUMMARY.md`
</output>
