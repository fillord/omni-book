---
phase: quick-260404-m66
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/booking-form.tsx
autonomous: true
requirements: [neumorphic-booking-steps, neumorphic-success-page]

must_haves:
  truths:
    - "Step indicator circles use neu-raised for inactive/done and neu-inset for active step (not flat border-2)"
    - "Service and resource selection cards use neu-raised when unselected and neu-inset when selected (not border-2)"
    - "Success screen checkmark circle uses neu-raised (not bg-green-100)"
    - "Success screen info card uses neu-raised bg-[var(--neu-bg)] (not border border-border bg-card)"
    - "Confirm step summary card uses neu-raised bg-[var(--neu-bg)] (not border border-border bg-card)"
    - "Time slot buttons use neu-raised when unselected and neu-inset when selected (not border-2)"
    - "Back buttons use neu-raised instead of border-2 border-border"
  artifacts:
    - path: "components/booking-form.tsx"
      provides: "Neumorphic booking form UI"
  key_links:
    - from: "components/booking-form.tsx"
      to: "app/globals.css"
      via: "neu-raised, neu-inset utility classes"
      pattern: "neu-raised|neu-inset"
---

<objective>
Apply neumorphic styling to the booking form component (steps, selection cards, success screen).

Purpose: The public booking page container and section tabs already use neumorphic styling, but the booking form internals (step indicator, service/resource cards, time slots, success screen) still use flat border-based styling. This creates a visual inconsistency.

Output: Fully neumorphic booking form matching the rest of the public page.

Note: The timezone bug (task 3) was already fixed in quick task 260404-g9o. The legal pages (task 4) already have the Navbar component with correct neumorphic styling. No work needed for either.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@components/booking-form.tsx
@app/globals.css (neu-raised, neu-inset, neu-bg class definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neumorphic step indicator and selection cards</name>
  <files>components/booking-form.tsx</files>
  <action>
Update the StepIndicator component (lines 136-179):
- Replace the flat `border-2` circle styling with neumorphic classes.
- Default (future) step: `w-8 h-8 rounded-full flex items-center justify-center text-sm font-semibold neu-raised bg-[var(--neu-bg)] text-muted-foreground`
- Active step: `w-8 h-8 rounded-full flex items-center justify-center text-sm font-semibold neu-inset bg-[var(--neu-bg)]` plus the existing `colors.stepActive` text color (strip the border- class from stepActive, keep only the text- class).
- Done step: Keep the existing `colors.stepDone` bg+text fill (these look good as solid filled circles).
- The connecting line between steps: keep as-is (done uses `colors.stepLine`, undone uses `bg-border`).

Update NicheColorClasses type and all four color configs:
- Change `stepActive` from `'border-blue-600 text-blue-600'` to just `'text-blue-600'` (and same for pink/orange/green). The border is no longer needed since we use neu-inset.
- Change `serviceSelected` from `'border-blue-600 bg-card dark:bg-blue-950/40'` to `'neu-inset bg-[var(--neu-bg)] text-blue-600'` (and same for pink/orange/green -- use respective color for text).
- Change `resourceSelected` from `'border-blue-600 bg-card dark:bg-blue-950/40'` to `'neu-inset bg-[var(--neu-bg)] text-blue-600'` (and same pattern for all colors).

Update service selection cards (around line 495):
- Change unselected from `'border-border hover:border-border/80'` to `'neu-raised bg-[var(--neu-bg)]'`.
- Remove the outer `border-2` from the base classes. Use `rounded-xl` (keep) + `cursor-pointer transition-all` (keep) + `p-4` (keep).
- Selected state gets the updated `colors.serviceSelected` which now includes `neu-inset`.

Update resource selection cards (around line 552):
- Same pattern as service cards above.

Update time slot buttons (around line 674-689):
- Remove `border-2` from base classes.
- Unavailable: `rounded-xl bg-muted/50 text-muted-foreground/40 cursor-not-allowed neu-inset`
- Selected: keep `colors.slotSelected` as-is (solid fill bg looks good for selected time).
- Unselected available: `neu-raised bg-[var(--neu-bg)] text-foreground` + keep `colors.slotHover`.

Update back buttons (lines 601, 699):
- Change from `border-2 border-border ... hover:bg-muted` to `neu-raised bg-[var(--neu-bg)] ... hover:text-neu-accent`.

Update submit/next buttons (lines 524, 607, 705):
- Keep the solid colored `colors.submitBtn` styling -- these should stand out with color fill.
- Add `neu-raised` class for depth when not disabled.

Update confirm step summary card (line 718):
- Change `rounded-xl border border-border bg-card` to `rounded-xl neu-raised bg-[var(--neu-bg)]`.

Update date input (line 641):
- Change `border-2 border-input ... bg-background` to `neu-inset bg-[var(--neu-bg)]`. Keep focus ring: `focus:outline-none focus:ring-2 focus:ring-ring`.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All booking form elements (steps, cards, slots, buttons, summary) use neumorphic classes. No flat border-2 patterns remain in the booking flow UI elements.</done>
</task>

<task type="auto">
  <name>Task 2: Neumorphic success screen</name>
  <files>components/booking-form.tsx</files>
  <action>
Update the SuccessScreen component (starts at line 184):

1. Checkmark circle (line 231-234):
   - Change `bg-green-100 dark:bg-green-950/40` to `neu-raised bg-[var(--neu-bg)]`.
   - Keep the green SVG checkmark icon color (`text-green-600 dark:text-green-400`).
   - Keep the scale/opacity animation transition.

2. Info summary card (line 267):
   - Change `border border-border bg-card` to `neu-raised bg-[var(--neu-bg)]`.
   - Keep `rounded-xl p-5 text-sm space-y-3 text-left max-w-sm`.

The Telegram button (line 289) and Book Again / Add to Calendar buttons (lines 301, 307) already use `neu-raised bg-[var(--neu-bg)]` -- no changes needed there.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Success screen checkmark and info card use neumorphic container styling consistent with the rest of the booking page.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- No `border-border bg-card` patterns remain in booking-form.tsx UI elements (summary cards)
- No `border-2 border-border` patterns remain on interactive elements (service/resource cards, time slots, back buttons)
- Step indicator uses neu-raised/neu-inset instead of border-2 circles
- grep for remaining flat patterns: `grep -n 'border-border.*bg-card\|border-2 border-border\|border-2 border-input' components/booking-form.tsx` should return 0 lines
</verification>

<success_criteria>
- All booking form interactive elements (step circles, service cards, resource cards, time slots, nav buttons) use neumorphic depth classes
- Success screen checkmark and summary card use neu-raised instead of flat borders
- Confirm step summary card uses neu-raised instead of flat border
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260404-m66-restore-neumorphic-ui-on-public-booking-/260404-m66-SUMMARY.md`
</output>
