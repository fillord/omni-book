---
phase: quick-260404-m66
plan: 01
subsystem: public-booking-ui
tags: [neumorphic, booking-form, ui, styling]
dependency_graph:
  requires: [app/globals.css (neu-raised, neu-inset, neu-bg utilities)]
  provides: [Neumorphic booking form — steps, cards, slots, buttons, success screen]
  affects: [components/booking-form.tsx]
tech_stack:
  added: []
  patterns: [neu-raised for elevated surfaces, neu-inset for active/pressed states, bg-[var(--neu-bg)] for correct shadow depth]
key_files:
  modified:
    - components/booking-form.tsx
decisions:
  - stepActive color classes retain only text-{color} (border- prefix removed since neu-inset provides visual depth)
  - serviceSelected/resourceSelected use neu-inset bg-[var(--neu-bg)] replacing border-2 + bg-card pattern
  - slotHover drops hover:border-{color} in favor of hover:text-{color} (no border on slots)
  - Submit/next buttons get neu-raised added on top of solid color fill for tactile depth
  - Back buttons use neu-raised bg-[var(--neu-bg)] hover:text-neu-accent replacing border-2 border-border hover:bg-muted
metrics:
  duration: "8 minutes"
  completed_date: "2026-04-04"
  tasks_completed: 2
  files_modified: 1
---

# Quick Task 260404-m66: Restore Neumorphic UI on Public Booking Form

**One-liner:** Applied neu-raised/neu-inset neumorphic depth classes to all booking form internals — step circles, service/resource selection cards, time slots, navigation buttons, date input, confirm summary, and success screen.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Neumorphic step indicator and selection cards | d9ffcc9 | components/booking-form.tsx |
| 2 | Neumorphic success screen | 9496b0f | components/booking-form.tsx |

## Changes Made

### Task 1 — Step indicator, selection cards, slots, buttons, summary card

**BOOKING_COLORS config updates:**
- `stepActive`: `'border-{color}-600 text-{color}-600'` → `'text-{color}-600'` (border no longer needed with neu-inset)
- `serviceSelected`: `'border-{color}-600 bg-card dark:bg-{color}-950/40'` → `'neu-inset bg-[var(--neu-bg)] text-{color}-600'`
- `resourceSelected`: same pattern as serviceSelected
- `slotHover`: `'hover:border-{color}-500 hover:text-{color}-600'` → `'hover:text-{color}-600'`

**StepIndicator component:**
- Active step: was `border-2 colors.stepActive` → now `neu-inset bg-[var(--neu-bg)] colors.stepActive`
- Inactive step: was `border-2 border-border text-muted-foreground` → now `neu-raised bg-[var(--neu-bg)] text-muted-foreground`
- Done step: unchanged (solid color fill looks correct)

**Service/Resource selection cards:**
- Removed `border-2` from base classes
- Unselected: `border-border hover:border-border/80` → `neu-raised bg-[var(--neu-bg)]`
- Selected: receives updated `serviceSelected`/`resourceSelected` from BOOKING_COLORS (already neu-inset)

**Time slot buttons:**
- Removed `border-2` from base classes
- Unavailable: `border-border/50 bg-muted/50` → `neu-inset bg-muted/50`
- Available unselected: `border-border` → `neu-raised bg-[var(--neu-bg)]`
- Selected: unchanged (solid color fill)

**Back buttons (all 3 steps):**
- Was: `border-2 border-border ... hover:bg-muted`
- Now: `neu-raised bg-[var(--neu-bg)] ... hover:text-neu-accent`

**Submit/Next buttons (all 4 steps):**
- Added `neu-raised` class alongside existing solid color `colors.submitBtn`

**Confirm step summary card:**
- Was: `border border-border bg-card`
- Now: `neu-raised bg-[var(--neu-bg)]`

**Date input:**
- Was: `border-2 border-input ... bg-background focus:border-ring`
- Now: `neu-inset bg-[var(--neu-bg)] ... focus:ring-2 focus:ring-ring`

### Task 2 — Success screen

**Checkmark circle:**
- Was: `bg-green-100 dark:bg-green-950/40`
- Now: `neu-raised bg-[var(--neu-bg)]`
- Green SVG color unchanged: `text-green-600 dark:text-green-400`
- Scale/opacity animation transition unchanged

**Success info summary card:**
- Was: `border border-border bg-card`
- Now: `neu-raised bg-[var(--neu-bg)]`

## Verification

```bash
grep -n 'border-border.*bg-card\|border-2 border-border\|border-2 border-input\|bg-green-100\|stepActive.*border-' components/booking-form.tsx
# Returns 0 lines - all flat border patterns removed
```

Build verified against main repo (worktree has pre-existing Phase 12 build error in unrelated service admin file — out of scope per deviation rules).

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- [x] `components/booking-form.tsx` modified with all neumorphic classes
- [x] Commit d9ffcc9 exists: `feat(quick-260404-m66): neumorphic step indicator, selection cards, slots, back buttons, summary card`
- [x] Commit 9496b0f exists: `feat(quick-260404-m66): neumorphic success screen checkmark circle and info card`
- [x] No flat border patterns remain in booking-form.tsx UI elements
