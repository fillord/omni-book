---
phase: 00-infrastructure-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/infrastructure-validation.test.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "globals.css body rule inside @layer base applies bg-background text-foreground"
    - "@theme inline block bridges --color-background to var(--background) (and all other tokens)"
    - "Both AdminThemeProvider and BookingThemeProvider pass attribute='class' to ThemeProvider"
  artifacts:
    - path: "__tests__/infrastructure-validation.test.ts"
      provides: "Automated verification of all three foundation requirements"
      contains: "describe('Infrastructure Validation'"
  key_links:
    - from: "__tests__/infrastructure-validation.test.ts"
      to: "app/globals.css"
      via: "fs.readFileSync content assertion"
      pattern: "readFileSync.*globals\\.css"
    - from: "__tests__/infrastructure-validation.test.ts"
      to: "components/theme-providers.tsx"
      via: "fs.readFileSync content assertion"
      pattern: "readFileSync.*theme-providers"
---

<objective>
Validate the CSS token foundation by creating automated tests that assert the three infrastructure requirements (FOUND-01, FOUND-02, FOUND-03) are satisfied in the current codebase.

Purpose: Certify that the three-layer token chain (CSS custom properties -> @theme inline bridge -> Tailwind utilities) and the next-themes class injection are correctly configured before any component remediation work begins in Phases 1-4.

Output: A single test file `__tests__/infrastructure-validation.test.ts` that passes, proving the foundation is sound.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-infrastructure-validation/00-RESEARCH.md
@.planning/phases/00-infrastructure-validation/00-VALIDATION.md

Source files under inspection (read-only — no modifications):
@app/globals.css
@components/theme-providers.tsx
@jest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create infrastructure validation test file</name>
  <files>__tests__/infrastructure-validation.test.ts</files>
  <read_first>
    - app/globals.css (the file being asserted against for FOUND-01 and FOUND-02)
    - components/theme-providers.tsx (the file being asserted against for FOUND-03)
    - jest.config.ts (confirm testMatch pattern includes __tests__/**/*.test.ts)
    - __tests__/lib/tenant/resolve.test.ts (reference for project test conventions: describe/it structure, import style)
  </read_first>
  <behavior>
    - Test: FOUND-01 — globals.css contains `@layer base` block with `body` rule that includes `bg-background` and `text-foreground`
      - Must match inside `@layer base { ... body { @apply bg-background text-foreground; } ... }` — NOT the raw `body { font-family: ... }` block at line 11-13
    - Test: FOUND-02 — globals.css contains `@theme inline` block with `--color-background: var(--background)` entry
      - Must confirm the block is `@theme inline {` (with the `inline` keyword), not just `@theme {`
      - Must confirm `--color-background: var(--background)` exists within that block
    - Test: FOUND-03 — theme-providers.tsx exports both AdminThemeProvider and BookingThemeProvider, each using attribute='class'
      - Must confirm `attribute: 'class'` exists in commonProps or equivalent
      - Must confirm both `BookingThemeProvider` and `AdminThemeProvider` functions spread commonProps or directly pass `attribute="class"` to ThemeProvider
      - Must confirm both providers pass `{...commonProps}` to ThemeProvider
  </behavior>
  <action>
Create `__tests__/infrastructure-validation.test.ts` with the following structure:

```typescript
import fs from "fs";
import path from "path";

describe("Infrastructure Validation", () => {
  // ---- FOUND-01 ----
  describe("FOUND-01: body applies bg-background text-foreground", () => {
    // Read app/globals.css via fs.readFileSync
    // Extract the @layer base block using regex: /@layer base\s*\{[\s\S]*?\n\}/
    // Assert the @layer base block contains "body" with "bg-background" and "text-foreground"
    // Use regex: /body\s*\{[^}]*bg-background[^}]*text-foreground/  (or reversed order)
    // Also test: the raw body rule at lines 11-13 does NOT contain bg-background (proves we're checking the right block)
  });

  // ---- FOUND-02 ----
  describe("FOUND-02: @theme inline bridges CSS variables to Tailwind", () => {
    // Read app/globals.css
    // Assert file contains "@theme inline {" (with inline keyword)
    // Extract the @theme inline block
    // Assert it contains "--color-background: var(--background)"
    // Also assert "--color-foreground: var(--foreground)" as a sanity check
  });

  // ---- FOUND-03 ----
  describe("FOUND-03: theme providers use attribute='class'", () => {
    // Read components/theme-providers.tsx
    // Assert file contains "attribute: 'class'"
    // Assert file contains "BookingThemeProvider"
    // Assert file contains "AdminThemeProvider"
    // Assert both providers spread commonProps: look for "{...commonProps}" appearing twice
    // (or alternatively, count occurrences of {...commonProps} and assert >= 2)
  });
});
```

Concrete values for regex patterns:
- FOUND-01 @layer base extraction: `/@layer base\s*\{[\s\S]*?\n\}/` to capture the entire block
- FOUND-01 body rule assertion: the extracted block must match `/body\s*\{[^}]*bg-background/` AND `/body\s*\{[^}]*text-foreground/`
- FOUND-02 block detection: `/\@theme inline\s*\{/` must match (NOT `/\@theme\s*\{/` without inline)
- FOUND-02 bridge assertion: `--color-background: var(--background)` as a literal substring
- FOUND-03 attribute assertion: `attribute: 'class'` as a literal substring
- FOUND-03 provider spread: count occurrences of `{...commonProps}` — must be >= 2

File path resolution: use `path.resolve(__dirname, "..", "app", "globals.css")` and `path.resolve(__dirname, "..", "components", "theme-providers.tsx")` to keep tests portable.

Use `fs.readFileSync(filePath, "utf-8")` — no async needed for static file reads.

Follow project conventions:
- Top-level describe: `"Infrastructure Validation"`
- Nested describes per requirement ID: `"FOUND-01: ..."`, `"FOUND-02: ..."`, `"FOUND-03: ..."`
- Individual `it()` cases for each assertion
- No mocks needed — these are pure file content assertions
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npm test -- --testPathPattern="infrastructure-validation"</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/infrastructure-validation.test.ts exists
    - File contains `describe("Infrastructure Validation"`
    - File contains `FOUND-01` in a describe block name
    - File contains `FOUND-02` in a describe block name
    - File contains `FOUND-03` in a describe block name
    - File contains `fs.readFileSync` (reads source files for assertion)
    - File contains `globals.css` in a path resolution
    - File contains `theme-providers.tsx` in a path resolution
    - File contains `@theme inline` in a test assertion or regex
    - File contains `bg-background` in a test assertion or regex
    - File contains `text-foreground` in a test assertion or regex
    - File contains `attribute: 'class'` or `attribute.*class` in a test assertion
    - File contains `commonProps` in a test assertion
    - File contains `BookingThemeProvider` in a test assertion
    - File contains `AdminThemeProvider` in a test assertion
    - `npm test -- --testPathPattern="infrastructure-validation"` exits with code 0
    - Test output shows 0 failures
  </acceptance_criteria>
  <done>All three FOUND-* requirements have passing automated tests. The test file reads the actual source files and asserts the exact patterns that prove the token foundation is correct.</done>
</task>

</tasks>

<verification>
1. Run `npm test -- --testPathPattern="infrastructure-validation"` — all tests pass (exit code 0)
2. Run `npm test` — full suite passes (no regressions introduced)
3. Confirm test file covers all three requirement IDs: FOUND-01, FOUND-02, FOUND-03
</verification>

<success_criteria>
- __tests__/infrastructure-validation.test.ts exists with tests for FOUND-01, FOUND-02, FOUND-03
- `npm test -- --testPathPattern="infrastructure-validation"` passes with 0 failures
- `npm test` full suite passes with 0 failures
- No production source files were modified (globals.css, theme-providers.tsx untouched)
</success_criteria>

<output>
After completion, create `.planning/phases/00-infrastructure-validation/00-01-SUMMARY.md`
</output>
