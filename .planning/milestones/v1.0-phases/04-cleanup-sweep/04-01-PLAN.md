---
phase: 04-cleanup-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/cleanup-surface.test.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "Tests for CLEAN-01 fail (RED) because banned-actions.tsx still contains bg-zinc-* classes"
    - "Tests for CLEAN-02 fail (RED) because booking-status-badge.tsx CANCELLED entry still uses bg-zinc-100"
    - "Tests for CLEAN-03 fail (RED) because dashboard/page.tsx lacks intentional comment near bg-white/10"
    - "Regression tests fail (RED) because booking-form.tsx serviceSelected lacks dark: override"
    - "Regression tests fail (RED) because tenant-public-page.tsx badge entries lack dark: override"
  artifacts:
    - path: "__tests__/cleanup-surface.test.ts"
      provides: "RED-state test scaffold for all Phase 4 requirements"
      min_lines: 80
  key_links:
    - from: "__tests__/cleanup-surface.test.ts"
      to: "components/banned-actions.tsx"
      via: "fs.readFileSync static scan"
      pattern: "readComponent.*banned-actions"
    - from: "__tests__/cleanup-surface.test.ts"
      to: "components/booking-status-badge.tsx"
      via: "fs.readFileSync static scan"
      pattern: "readComponent.*booking-status-badge"
    - from: "__tests__/cleanup-surface.test.ts"
      to: "app/dashboard/page.tsx"
      via: "fs.readFileSync static scan"
      pattern: "readApp.*dashboard/page"
    - from: "__tests__/cleanup-surface.test.ts"
      to: "components/booking-form.tsx"
      via: "fs.readFileSync static scan"
      pattern: "readComponent.*booking-form"
    - from: "__tests__/cleanup-surface.test.ts"
      to: "components/tenant-public-page.tsx"
      via: "fs.readFileSync static scan"
      pattern: "readComponent.*tenant-public-page"
---

<objective>
Create the RED-state test scaffold for all Phase 4 requirements (CLEAN-01, CLEAN-02, CLEAN-03) and the critical dark mode regression in booking-form.tsx and tenant-public-page.tsx.

Purpose: All subsequent remediation plans (04-02 and 04-03) need tests to run RED before fixes and GREEN after. This follows the established Phase 0/1/2/3 pattern.
Output: `__tests__/cleanup-surface.test.ts` with failing tests for every Phase 4 requirement.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-sweep/04-RESEARCH.md
@.planning/phases/04-cleanup-sweep/04-VALIDATION.md

@__tests__/booking-surface.test.ts
@__tests__/dashboard-auth-surface.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cleanup-surface.test.ts with RED-state assertions</name>
  <files>__tests__/cleanup-surface.test.ts</files>
  <read_first>
    __tests__/booking-surface.test.ts
    __tests__/dashboard-auth-surface.test.ts
    components/banned-actions.tsx
    components/booking-status-badge.tsx
    app/dashboard/page.tsx
    components/booking-form.tsx
    components/tenant-public-page.tsx
  </read_first>
  <action>
Create `__tests__/cleanup-surface.test.ts` following the established test pattern from `booking-surface.test.ts` and `dashboard-auth-surface.test.ts`.

Use two helpers:
```typescript
const readComponent = (name: string) =>
  fs.readFileSync(path.resolve(__dirname, '..', 'components', name), 'utf-8');
const readApp = (relPath: string) =>
  fs.readFileSync(path.resolve(__dirname, '..', relPath), 'utf-8');
```

Test groups:

**CLEAN-01 group (`banned-actions.tsx`):**
1. `not.toMatch(/\bbg-zinc-\d+\b/)` — no bg-zinc classes
2. `not.toMatch(/\bborder-zinc-\d+\b/)` — no border-zinc classes
3. `not.toMatch(/\btext-zinc-\d+\b/)` — no text-zinc classes
4. `not.toMatch(/\bhover:bg-zinc-\d+\b/)` — no hover:bg-zinc classes
5. `toContain('bg-foreground')` — contact support button uses semantic bg
6. `toContain('text-muted-foreground')` — return home link uses semantic text

**CLEAN-02 group (`booking-status-badge.tsx`):**
1. CANCELLED entry contains `bg-muted text-muted-foreground border-border` (single semantic string, no zinc)
2. CANCELLED entry does NOT contain `bg-zinc` — `not.toMatch(/CANCELLED[\s\S]*?bg-zinc/)`
3. Preservation assertions: source still contains `bg-amber-100` (PENDING preserved)
4. Preservation assertions: source still contains `bg-blue-100` (CONFIRMED preserved)
5. Preservation assertions: source still contains `bg-green-100` (COMPLETED preserved)
6. Preservation assertions: source still contains `bg-red-100` (NO_SHOW preserved)

**CLEAN-03 group (`app/dashboard/page.tsx`):**
1. File still contains `bg-white/10` (not removed)
2. File contains the word `intentional` within proximity of `bg-white/10` — scan lines: find a line with `bg-white/10`, check that within 5 lines above OR on the same line there is a comment containing `intentional`

**Regression: booking-form.tsx selection states:**
1. For each of 4 niches (blue, pink, orange, green): source contains `dark:bg-{color}-950/40` — `toContain("dark:bg-blue-950/40")` etc.
2. `serviceSelected` lines contain a `dark:` class — extract lines containing `serviceSelected:` and assert each `.toMatch(/dark:/)`
3. `resourceSelected` lines contain a `dark:` class — same pattern
4. Error block: source contains `dark:bg-red-950/40` and `dark:text-red-300`
5. Success screen: source contains `dark:bg-green-950/40` near the success icon `bg-green-100`

**Regression: tenant-public-page.tsx badge/avatarBg:**
1. For each of 4 niches (blue, pink, orange, green): the `badge:` entry line contains `dark:bg-{color}-950/40` and `dark:text-{color}-300`
2. For each of 4 niches: the `avatarBg:` entry line contains `dark:bg-{color}-950/40` and `dark:text-{color}-300`
3. Line 613 emoji container: the line with `w-12 h-12 rounded-xl` contains a `dark:` class — `toMatch(/w-12 h-12.*dark:/)`

All tests should FAIL (RED) against current source. This is expected — the remediation hasn't happened yet.
  </action>
  <verify>
    <automated>npx jest __tests__/cleanup-surface.test.ts --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/cleanup-surface.test.ts exists and contains `describe("CLEAN-01`
    - __tests__/cleanup-surface.test.ts contains `describe("CLEAN-02`
    - __tests__/cleanup-surface.test.ts contains `describe("CLEAN-03`
    - __tests__/cleanup-surface.test.ts contains `describe("regression` (case-insensitive)
    - jest exits with failures (RED state) — at least CLEAN-01 zinc assertions fail because banned-actions.tsx still has bg-zinc-900
    - jest exits with failures (RED state) — regression assertions fail because booking-form.tsx serviceSelected lacks dark:bg-blue-950/40
    - Existing booking-surface.test.ts still passes: `npx jest __tests__/booking-surface.test.ts --no-coverage` exits 0
  </acceptance_criteria>
  <done>Test file exists with all CLEAN-01/02/03 and regression groups. Tests run RED. Existing Phase 2 tests unaffected.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/cleanup-surface.test.ts --no-coverage 2>&1 | tail -30` — shows test failures (RED state expected)
- `npx jest __tests__/booking-surface.test.ts --no-coverage 2>&1 | tail -10` — still passes (guard tests green)
- `wc -l __tests__/cleanup-surface.test.ts` — at least 80 lines
</verification>

<success_criteria>
- cleanup-surface.test.ts exists with describe blocks for CLEAN-01, CLEAN-02, CLEAN-03, and regression
- Tests run and FAIL (RED state) — confirming they correctly detect the current violations
- Phase 2 guard tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-sweep/04-01-SUMMARY.md`
</output>
