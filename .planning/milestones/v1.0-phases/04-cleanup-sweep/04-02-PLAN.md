---
phase: 04-cleanup-sweep
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/banned-actions.tsx
  - components/booking-status-badge.tsx
  - components/booking-form.tsx
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "banned-actions.tsx renders correctly in dark mode with no hardcoded zinc classes"
    - "booking-status-badge.tsx CANCELLED status uses semantic tokens, all other status colors preserved"
    - "booking-form.tsx selected service/resource items show a readable dark-tinted background in dark mode"
    - "booking-form.tsx error block is readable in dark mode"
    - "booking-form.tsx success screen icon circle is readable in dark mode"
  artifacts:
    - path: "components/banned-actions.tsx"
      provides: "Semantically-themed banned user actions component"
      contains: "bg-foreground text-background"
    - path: "components/booking-status-badge.tsx"
      provides: "Status badge with semantic CANCELLED entry"
      contains: "bg-muted text-muted-foreground border-border"
    - path: "components/booking-form.tsx"
      provides: "Booking form with dark mode overrides on selection states and error/success blocks"
      contains: "dark:bg-blue-950/40"
  key_links:
    - from: "components/banned-actions.tsx"
      to: "semantic tokens"
      via: "class replacement"
      pattern: "bg-foreground|text-background|border-border|text-muted-foreground|hover:bg-muted"
    - from: "components/booking-form.tsx"
      to: "BOOKING_COLORS map"
      via: "dark: override suffix additions"
      pattern: "dark:bg-(blue|pink|orange|green)-950/40"
---

<objective>
Remediate banned-actions.tsx (CLEAN-01), booking-status-badge.tsx (CLEAN-02), and booking-form.tsx (dark mode regression on selection states, error block, success screen).

Purpose: Fix the critical dark mode visual regression in the booking flow and clean up the last two edge-case components with hardcoded neutrals.
Output: Three files with all semantic token replacements and dark: overrides applied. CLEAN-01 and CLEAN-02 tests go GREEN. Regression tests for booking-form.tsx go GREEN.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cleanup-sweep/04-RESEARCH.md
@.planning/phases/04-cleanup-sweep/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate banned-actions.tsx and booking-status-badge.tsx (CLEAN-01, CLEAN-02)</name>
  <files>components/banned-actions.tsx, components/booking-status-badge.tsx</files>
  <read_first>
    components/banned-actions.tsx
    components/booking-status-badge.tsx
    __tests__/cleanup-surface.test.ts
  </read_first>
  <action>
**banned-actions.tsx (CLEAN-01) — 4 lines, 7 class replacements:**

Line 20 (contact support `<a>`):
- BEFORE: `bg-zinc-900 text-white text-sm font-medium hover:bg-zinc-700`
- AFTER:  `bg-foreground text-background text-sm font-medium hover:bg-foreground/90`

Line 28 (sign-out `<button>`):
- BEFORE: `border border-zinc-200 text-zinc-700 text-sm font-medium hover:bg-zinc-50`
- AFTER:  `border border-border text-foreground text-sm font-medium hover:bg-muted`

Line 32 (divider `<div>`):
- BEFORE: `border-t border-zinc-100`
- AFTER:  `border-t border-border`

Line 33 (return home `<Link>`):
- BEFORE: `text-zinc-400 hover:text-zinc-600`
- AFTER:  `text-muted-foreground hover:text-foreground`

Do NOT change any other classes (px-4, py-2.5, rounded-lg, w-full, transition-colors, etc.). Only swap the color classes listed above.

**booking-status-badge.tsx (CLEAN-02) — 1 entry replacement:**

CANCELLED entry (line 32):
- BEFORE: `className: 'bg-zinc-100 text-zinc-600 border-zinc-300 dark:bg-zinc-800 dark:text-zinc-400'`
- AFTER:  `className: 'bg-muted text-muted-foreground border-border'`

Do NOT touch PENDING, CONFIRMED, COMPLETED, or NO_SHOW entries. Do NOT touch `dotColor: 'bg-amber-500'`. These are functional status colors and must be preserved exactly as-is.
  </action>
  <verify>
    <automated>npx jest __tests__/cleanup-surface.test.ts --no-coverage -t "CLEAN-01|CLEAN-02" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - components/banned-actions.tsx contains `bg-foreground text-background`
    - components/banned-actions.tsx contains `border border-border text-foreground`
    - components/banned-actions.tsx contains `border-t border-border`
    - components/banned-actions.tsx contains `text-muted-foreground hover:text-foreground`
    - components/banned-actions.tsx does NOT match `/\bbg-zinc-\d+\b/`
    - components/banned-actions.tsx does NOT match `/\btext-zinc-\d+\b/`
    - components/banned-actions.tsx does NOT match `/\bborder-zinc-\d+\b/`
    - components/booking-status-badge.tsx CANCELLED className is `'bg-muted text-muted-foreground border-border'`
    - components/booking-status-badge.tsx still contains `bg-amber-100` (PENDING preserved)
    - components/booking-status-badge.tsx still contains `bg-red-100` (NO_SHOW preserved)
    - CLEAN-01 and CLEAN-02 tests pass (GREEN)
  </acceptance_criteria>
  <done>banned-actions.tsx has zero zinc/slate classes. booking-status-badge.tsx CANCELLED uses semantic tokens, all other status colors preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add dark: overrides to booking-form.tsx BOOKING_COLORS, error block, and success screen</name>
  <files>components/booking-form.tsx</files>
  <read_first>
    components/booking-form.tsx
    __tests__/cleanup-surface.test.ts
  </read_first>
  <action>
**BOOKING_COLORS map — add `dark:bg-{color}-950/40` to every serviceSelected and resourceSelected entry:**

Line 68: `serviceSelected: 'border-blue-600 bg-blue-50'`
  AFTER: `serviceSelected: 'border-blue-600 bg-blue-50 dark:bg-blue-950/40'`

Line 69: `resourceSelected: 'border-blue-600 bg-blue-50'`
  AFTER: `resourceSelected: 'border-blue-600 bg-blue-50 dark:bg-blue-950/40'`

Line 78: `serviceSelected: 'border-pink-600 bg-pink-50'`
  AFTER: `serviceSelected: 'border-pink-600 bg-pink-50 dark:bg-pink-950/40'`

Line 79: `resourceSelected: 'border-pink-600 bg-pink-50'`
  AFTER: `resourceSelected: 'border-pink-600 bg-pink-50 dark:bg-pink-950/40'`

Line 88: `serviceSelected: 'border-orange-600 bg-orange-50'`
  AFTER: `serviceSelected: 'border-orange-600 bg-orange-50 dark:bg-orange-950/40'`

Line 89: `resourceSelected: 'border-orange-600 bg-orange-50'`
  AFTER: `resourceSelected: 'border-orange-600 bg-orange-50 dark:bg-orange-950/40'`

Line 98: `serviceSelected: 'border-green-600 bg-green-50'`
  AFTER: `serviceSelected: 'border-green-600 bg-green-50 dark:bg-green-950/40'`

Line 99: `resourceSelected: 'border-green-600 bg-green-50'`
  AFTER: `resourceSelected: 'border-green-600 bg-green-50 dark:bg-green-950/40'`

**Error block (line 741):**

BEFORE: `className="rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700"`
AFTER:  `className="rounded-xl border border-red-200 bg-red-50 dark:border-red-800 dark:bg-red-950/40 dark:text-red-300 px-4 py-3 text-sm text-red-700"`

**Success screen icon circle (line 232):**

BEFORE: `'w-20 h-20 rounded-full bg-green-100 flex items-center justify-center transition-all duration-500'`
AFTER:  `'w-20 h-20 rounded-full bg-green-100 dark:bg-green-950/40 flex items-center justify-center transition-all duration-500'`

Also add `dark:text-green-400` to the checkmark icon on the next line if it uses `text-green-600` (check for `text-green-600` on the Check/CheckCircle icon element and add `dark:text-green-400`).

Do NOT change any `bg-{color}-600`, `text-white`, `hover:bg-{color}-700` values in the map — those are the strong accent colors and must be preserved.
  </action>
  <verify>
    <automated>npx jest __tests__/cleanup-surface.test.ts --no-coverage -t "regression.*booking-form|booking-form" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - components/booking-form.tsx contains `dark:bg-blue-950/40` (at least 2 occurrences: serviceSelected + resourceSelected)
    - components/booking-form.tsx contains `dark:bg-pink-950/40` (at least 2 occurrences)
    - components/booking-form.tsx contains `dark:bg-orange-950/40` (at least 2 occurrences)
    - components/booking-form.tsx contains `dark:bg-green-950/40` (at least 3 occurrences: serviceSelected + resourceSelected + success screen)
    - components/booking-form.tsx contains `dark:bg-red-950/40` (error block)
    - components/booking-form.tsx contains `dark:text-red-300` (error block)
    - components/booking-form.tsx still contains `bg-blue-600` (accent preserved)
    - components/booking-form.tsx still contains `bg-blue-50` (light mode preserved)
    - Regression tests for booking-form.tsx pass (GREEN)
    - Phase 2 guard tests still pass: `npx jest __tests__/booking-surface.test.ts --no-coverage` exits 0
  </acceptance_criteria>
  <done>All 8 BOOKING_COLORS selection entries have dark: overrides. Error block and success screen have dark: overrides. Phase 2 guard tests remain green.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/cleanup-surface.test.ts --no-coverage -t "CLEAN-01|CLEAN-02" 2>&1 | tail -10` — GREEN
- `npx jest __tests__/cleanup-surface.test.ts --no-coverage -t "regression.*booking-form" 2>&1 | tail -10` — GREEN
- `npx jest __tests__/booking-surface.test.ts --no-coverage 2>&1 | tail -10` — GREEN (guard tests)
</verification>

<success_criteria>
- banned-actions.tsx has zero zinc/slate/gray hardcoded neutral classes
- booking-status-badge.tsx CANCELLED entry uses bg-muted text-muted-foreground border-border
- booking-form.tsx all 8 serviceSelected/resourceSelected entries have dark:bg-{color}-950/40
- booking-form.tsx error block has dark: overrides
- booking-form.tsx success screen has dark: overrides
- All CLEAN-01, CLEAN-02, and booking-form regression tests GREEN
- Phase 2 guard tests remain GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/04-cleanup-sweep/04-02-SUMMARY.md`
</output>
