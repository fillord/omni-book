---
phase: 03-dashboard-auth-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/dashboard-auth-surface.test.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Running `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage` exits 0 (all assertions red or skipped — test scaffold exists)"
    - "Every DASH-01 through AUTH-03 requirement has at least one test assertion in the file"
    - "Test file follows the fs.readFileSync + regex pattern established in booking-surface.test.ts and landing-surface.test.ts"
  artifacts:
    - path: "__tests__/dashboard-auth-surface.test.ts"
      provides: "Static assertion coverage for all 8 Phase 3 requirements"
      exports: []
  key_links:
    - from: "__tests__/dashboard-auth-surface.test.ts"
      to: "components/dashboard-sidebar.tsx"
      via: "fs.readFileSync relative path"
      pattern: "readFile.*dashboard-sidebar"
    - from: "__tests__/dashboard-auth-surface.test.ts"
      to: "app/dashboard/settings/billing/billing-content.tsx"
      via: "fs.readFileSync relative path"
      pattern: "readFile.*billing-content"
---

<objective>
Create the test scaffold for Phase 3: all 15 static assertions covering DASH-01 through AUTH-03 in a single test file that matches the established fs.readFileSync + regex pattern from prior phases.

Purpose: The Nyquist rule requires automated verify commands for every subsequent plan task. This plan creates the file first so Wave 2 plans can reference it.
Output: `__tests__/dashboard-auth-surface.test.ts` with 15 describe/it blocks — all start RED (file content violations not yet fixed) and turn GREEN as Plans 02-04 complete their remediations.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-auth-surface/03-RESEARCH.md
</context>

<interfaces>
<!-- Existing test pattern — match exactly -->
<!-- From __tests__/booking-surface.test.ts and __tests__/landing-surface.test.ts -->

```typescript
import fs from "fs";
import path from "path";

const COMPONENTS_DIR = path.resolve(__dirname, "..", "components");
const readComponent = (name: string) =>
  fs.readFileSync(path.join(COMPONENTS_DIR, name), "utf-8");
```

<!-- For app/ paths, use a separate helper: -->
```typescript
const readApp = (relPath: string) =>
  fs.readFileSync(path.resolve(__dirname, "..", relPath), "utf-8");
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create dashboard-auth-surface test scaffold</name>
  <files>__tests__/dashboard-auth-surface.test.ts</files>
  <read_first>
    - __tests__/booking-surface.test.ts (pattern reference — must match file/describe/it structure exactly)
    - __tests__/landing-surface.test.ts (pattern reference — readComponent helper style)
    - .planning/phases/03-dashboard-auth-surface/03-RESEARCH.md (violation inventory for exact assertion strings)
  </read_first>
  <action>
Create `__tests__/dashboard-auth-surface.test.ts` with the following structure and assertions.

Use two file-reader helpers:
```typescript
import fs from "fs";
import path from "path";

const readComponent = (name: string) =>
  fs.readFileSync(path.resolve(__dirname, "..", "components", name), "utf-8");

const readApp = (relPath: string) =>
  fs.readFileSync(path.resolve(__dirname, "..", relPath), "utf-8");
```

Write the following describe blocks in order:

**DASH-01 block** (sidebar token family):
```typescript
describe("DASH-01: dashboard-sidebar.tsx uses sidebar token family, not bg-background", () => {
  it("root div uses bg-sidebar not bg-background", () => {
    const source = readComponent("dashboard-sidebar.tsx");
    expect(source).not.toMatch(/flex flex-col h-full bg-background/);
    expect(source).toContain("bg-sidebar");
  });
  it("footer section uses bg-sidebar not bg-background", () => {
    const source = readComponent("dashboard-sidebar.tsx");
    const lines = source.split("\n");
    const footerLines = lines.filter((l) => l.includes("mt-auto"));
    footerLines.forEach((l) => expect(l).not.toMatch(/\bbg-background\b/));
  });
  it("sidebar uses border-sidebar-border not border-border on root and footer", () => {
    const source = readComponent("dashboard-sidebar.tsx");
    expect(source).toContain("border-sidebar-border");
    expect(source).not.toMatch(/flex flex-col h-full.*border-border/);
  });
});
```

**DASH-02 block** (no dark:! in billing):
```typescript
describe("DASH-02: billing-content.tsx has no dark:! force-override classes", () => {
  it("billing-content.tsx contains no dark:! classes", () => {
    const source = readApp("app/dashboard/settings/billing/billing-content.tsx");
    expect(source).not.toMatch(/dark:![a-z]/);
  });
  it("billing-content.tsx contains no bare text-zinc-* neutral text classes", () => {
    const source = readApp("app/dashboard/settings/billing/billing-content.tsx");
    expect(source).not.toMatch(/\btext-zinc-\d+\b/);
  });
  it("billing-content.tsx contains no bare bg-zinc-* neutral background classes", () => {
    const source = readApp("app/dashboard/settings/billing/billing-content.tsx");
    expect(source).not.toMatch(/\bbg-zinc-\d+\b/);
  });
  it("billing-content.tsx contains no bare border-zinc-* neutral border classes", () => {
    const source = readApp("app/dashboard/settings/billing/billing-content.tsx");
    expect(source).not.toMatch(/\bborder-zinc-\d+\b/);
  });
});
```

**DASH-03 block** (analytics chart token fixes):
```typescript
describe("DASH-03: analytics-dashboard.tsx Recharts props use CSS variable references", () => {
  it("analytics-dashboard.tsx EmptyState does not use text-zinc-400", () => {
    const source = readComponent("analytics-dashboard.tsx");
    expect(source).not.toMatch(/\btext-zinc-400\b/);
  });
  it("CartesianGrid stroke does not use hardcoded hex #f4f4f5", () => {
    const source = readComponent("analytics-dashboard.tsx");
    expect(source).not.toMatch(/stroke="#f4f4f5"/);
  });
  it("Tooltip cursor fill does not use hardcoded hex #f4f4f5", () => {
    const source = readComponent("analytics-dashboard.tsx");
    expect(source).not.toMatch(/fill: '#f4f4f5'/);
  });
  it("axis tick fill does not use hardcoded hex #a1a1aa or #52525b", () => {
    const source = readComponent("analytics-dashboard.tsx");
    expect(source).not.toMatch(/fill: '#a1a1aa'/);
    expect(source).not.toMatch(/fill: '#52525b'/);
  });
  it("contentStyle border does not use hardcoded hex #e4e4e7", () => {
    const source = readComponent("analytics-dashboard.tsx");
    expect(source).not.toMatch(/#e4e4e7/);
  });
});
```

**DASH-04 block** (manager files clean):
```typescript
describe("DASH-04: manager components have no hardcoded neutral color classes", () => {
  const MANAGERS = ["staff-manager.tsx", "services-manager.tsx", "resources-manager.tsx"];
  const NEUTRAL_PATTERN = /\b(bg-white|bg-zinc-\d+|bg-slate-\d+|text-zinc-\d+|text-slate-\d+)\b/;

  MANAGERS.forEach((file) => {
    it(`${file} has no hardcoded neutral background or text classes`, () => {
      const source = readComponent(file);
      expect(source).not.toMatch(NEUTRAL_PATTERN);
    });
  });
});
```

**DASH-05 block** (no text-white bg-zinc-900 in sidebar):
```typescript
describe("DASH-05: dashboard-sidebar.tsx admin link uses sidebar-accent tokens", () => {
  it("admin link does not use bg-zinc-900", () => {
    const source = readComponent("dashboard-sidebar.tsx");
    expect(source).not.toMatch(/\bbg-zinc-900\b/);
  });
  it("admin link does not use text-white on hardcoded dark background", () => {
    const source = readComponent("dashboard-sidebar.tsx");
    // text-white is only allowed if NOT paired with bg-zinc-900
    expect(source).not.toMatch(/text-white\s+bg-zinc-900|bg-zinc-900\s+.*text-white/);
  });
});
```

**AUTH-01, AUTH-02, AUTH-03 blocks** (auth pages clean):
```typescript
describe("AUTH-01: login/page.tsx has no neutral zinc/slate/gray background violations", () => {
  it("login/page.tsx has no bare bg-zinc-* background classes", () => {
    const source = readApp("app/(auth)/login/page.tsx");
    expect(source).not.toMatch(/\bbg-zinc-\d+\b/);
  });
  it("login/page.tsx has no bare bg-slate-* background classes", () => {
    const source = readApp("app/(auth)/login/page.tsx");
    expect(source).not.toMatch(/\bbg-slate-\d+\b/);
  });
});

describe("AUTH-02: register/page.tsx has no neutral zinc/slate/gray background violations", () => {
  it("register/page.tsx has no bare bg-zinc-* background classes", () => {
    const source = readApp("app/(auth)/register/page.tsx");
    expect(source).not.toMatch(/\bbg-zinc-\d+\b/);
  });
  it("register/page.tsx has no bare bg-slate-* background classes", () => {
    const source = readApp("app/(auth)/register/page.tsx");
    expect(source).not.toMatch(/\bbg-slate-\d+\b/);
  });
});

describe("AUTH-03: verify-otp/page.tsx has no neutral zinc/slate/gray background violations", () => {
  it("verify-otp/page.tsx has no bare bg-zinc-* background classes", () => {
    const source = readApp("app/(auth)/verify-otp/page.tsx");
    expect(source).not.toMatch(/\bbg-zinc-\d+\b/);
  });
  it("verify-otp/page.tsx has no bare bg-slate-* background classes", () => {
    const source = readApp("app/(auth)/verify-otp/page.tsx");
    expect(source).not.toMatch(/\bbg-slate-\d+\b/);
  });
});
```

Important preservation rules (do NOT add negative tests for these):
- `ACTIVE_LINK` and `BADGE_CLS` maps in dashboard-sidebar.tsx use intentional niche accent colors (bg-blue-50, dark:bg-blue-950/40, etc.) — these are preserved exactly like BOOK-04 niche accents
- `PIE_COLORS` array and bar/fill hex values in analytics-dashboard.tsx — out of scope per REQUIREMENTS.md
- `bg-orange-600 hover:bg-orange-700` on login force-button — intentional warning color, not a neutral violation
- `border-green-500/40 bg-green-500/5 text-green-600` in verify-otp — intentional success status color
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/dashboard-auth-surface.test.ts` exists
    - File contains `describe("DASH-01` string
    - File contains `describe("DASH-02` string
    - File contains `describe("DASH-03` string
    - File contains `describe("DASH-04` string
    - File contains `describe("DASH-05` string
    - File contains `describe("AUTH-01` string
    - File contains `describe("AUTH-02` string
    - File contains `describe("AUTH-03` string
    - File contains `readComponent` and `readApp` helpers
    - `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage` runs (may fail with assertion errors — that is expected at this stage; the test runner itself must not crash with syntax errors)
  </acceptance_criteria>
  <done>Test file exists with all 8 describe blocks. Running jest produces test output (not a compile/parse error). DASH-01 through DASH-05 assertions fail RED (violations not yet fixed). AUTH-01/02/03 assertions pass GREEN (auth pages are already clean).</done>
</task>

</tasks>

<verification>
`npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage` runs without TypeScript/syntax errors.
AUTH-01, AUTH-02, AUTH-03 assertions pass (those files are already clean).
DASH-01 through DASH-05 assertions fail (expected — violations exist, Plans 02-03 fix them).
Full suite: `npx jest --no-coverage` still passes all prior-phase tests.
</verification>

<success_criteria>
Test scaffold exists and is syntactically valid. 6 auth assertions are GREEN. DASH assertions are RED, documenting the violations that Plans 02 and 03 will fix.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-auth-surface/03-01-SUMMARY.md`
</output>
