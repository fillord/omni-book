---
phase: 03-dashboard-auth-surface
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "login/page.tsx is confirmed clean — no neutral zinc/slate/gray background violations"
    - "register/page.tsx is confirmed clean — no neutral violations"
    - "verify-otp/page.tsx is confirmed clean — no neutral violations"
    - "AUTH-01 through AUTH-03 tests pass GREEN (no source changes needed)"
  artifacts:
    - path: "app/(auth)/login/page.tsx"
      provides: "Auth login page (audit confirmed clean)"
    - path: "app/(auth)/register/page.tsx"
      provides: "Auth register page (audit confirmed clean)"
    - path: "app/(auth)/verify-otp/page.tsx"
      provides: "Auth OTP page (audit confirmed clean)"
  key_links:
    - from: "__tests__/dashboard-auth-surface.test.ts"
      to: "app/(auth)/login/page.tsx"
      via: "fs.readFileSync via readApp helper"
      pattern: "readApp.*auth.*login"
---

<objective>
Audit and confirm that all three auth pages are already semantically correct. Research confirmed they use semantic tokens at their root (`bg-muted/30`, `bg-card`, `text-foreground`, `text-muted-foreground`). Run tests to produce documented proof of compliance for AUTH-01, AUTH-02, and AUTH-03. Final checkpoint verifies the complete Phase 3 visual result.

Purpose: These requirements are audit-and-confirm tasks. The primary work is confirming the audit result, documenting intentional exceptions, and making the tests pass GREEN.
Output: AUTH-01 through AUTH-03 tests GREEN. No source changes expected unless the audit reading reveals a missed violation.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-auth-surface/03-RESEARCH.md
@.planning/phases/03-dashboard-auth-surface/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Auth page known state (from RESEARCH.md Pattern 4: Auth Pages — Already Largely Semantic) -->

<!-- login/page.tsx — intentional exceptions (DO NOT change): -->
<!--
  Line 246: border-orange-500/40 bg-orange-500/5 text-orange-600 — force-login warning, intentional status color
            bg-orange-600 hover:bg-orange-700 text-white          — force-login button, intentional warning CTA
-->

<!-- verify-otp/page.tsx — intentional exceptions (DO NOT change): -->
<!--
  Lines 106-108: border-green-500/40 bg-green-500/5 text-green-600 — success state, intentional status color
-->

<!-- register/page.tsx — all semantic, no exceptions needed -->

<!-- All three files use: -->
<!--
  bg-muted/30 or bg-muted/40 (page canvas)
  bg-background (card containers)
  bg-transparent (input backgrounds)
  text-muted-foreground, text-foreground
  border-destructive/40, bg-destructive/5, text-destructive (error states)
  bg-card (separator overlay)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit and confirm auth pages (AUTH-01, AUTH-02, AUTH-03)</name>
  <files></files>
  <read_first>
    - app/(auth)/login/page.tsx (audit read — confirm no neutral violations; note intentional orange exception)
    - app/(auth)/register/page.tsx (audit read — confirm no neutral violations)
    - app/(auth)/verify-otp/page.tsx (audit read — confirm no neutral violations; note intentional green success exception)
    - .planning/phases/03-dashboard-auth-surface/03-RESEARCH.md (Pattern 4: Auth Pages — Already Largely Semantic)
  </read_first>
  <action>
This is an audit-and-confirm task. Research confirmed all three auth pages are already semantically correct.

**Step 1 — Read each file in full.**

**Step 2 — For each file, verify the following pattern is absent:**
- No `bg-zinc-*`, `bg-slate-*`, `bg-gray-*`, `bg-white` on page/section/card backgrounds
- No `text-zinc-*`, `text-slate-*` on non-intentional text
- No `dark:!` force overrides
- No `border-zinc-*`, `border-slate-*`

**Step 3 — Document the intentional exceptions (do NOT change these):**

In `login/page.tsx`:
- `border-orange-500/40 bg-orange-500/5 text-orange-600` — force-login warning container (status color pair)
- `bg-orange-600 hover:bg-orange-700 text-white` — force-login CTA button (intentional warning action)
- Google OAuth SVG path fill attributes — brand-required per REQUIREMENTS.md Out of Scope

In `verify-otp/page.tsx`:
- `border-green-500/40 bg-green-500/5 text-green-600` — success message (intentional status color)

**Step 4 — If any neutral violation IS found (unexpected):**
Apply the standard replacement:
- `bg-white` → `bg-background` (page canvas) or `bg-card` (raised surface)
- `bg-zinc-*` → `bg-muted`
- `text-zinc-*` → `text-foreground` or `text-muted-foreground`
- `border-zinc-*` → `border-border`

**Step 5 — Run the tests to confirm GREEN.**
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="AUTH" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="AUTH"` exits 0
    - All 6 AUTH assertions (2 per page × 3 pages) pass GREEN
    - `grep -c "bg-zinc-\|bg-slate-\|bg-white" "app/(auth)/login/page.tsx"` returns 0
    - `grep -c "bg-zinc-\|bg-slate-\|bg-white" "app/(auth)/register/page.tsx"` returns 0
    - `grep -c "bg-zinc-\|bg-slate-\|bg-white" "app/(auth)/verify-otp/page.tsx"` returns 0
  </acceptance_criteria>
  <done>AUTH-01, AUTH-02, AUTH-03 confirmed clean. All auth page tests GREEN. Intentional exceptions documented (orange warning, green success status colors).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 3 remediation</name>
  <files></files>
  <action>Human visual verification of all Phase 3 surfaces in dark mode. No code changes — this is a review gate only.</action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all Phase 3 surfaces render correctly in dark mode.</done>
  <what-built>
    Complete Phase 3 remediation: sidebar tokens (DASH-01), billing force-override removal (DASH-02), analytics CSS variables (DASH-03), manager audit (DASH-04), DASH-05 admin link, and auth pages confirmation (AUTH-01/02/03). All 8 requirements addressed.
  </what-built>
  <how-to-verify>
    1. Run full test suite: `npx jest --no-coverage` — all tests must pass GREEN
    2. Toggle dark mode in the app (browser DevTools or theme toggle)
    3. Visit the dashboard (`/dashboard`):
       - Sidebar should have a perceptibly distinct background from the main content area in dark mode (darker/blue-tinted)
       - Admin panel link (if visible) should use a sidebar accent background, not hardcoded zinc-900
    4. Visit `/dashboard/settings/billing`:
       - All text and containers should be readable in dark mode with no jarring light-mode white panels
       - Plan pricing box, payment dialog should have proper dark backgrounds
    5. Visit the analytics page:
       - Chart grid lines should be visible (not invisible white on white) in dark mode
       - Axis labels should be readable (not invisible gray on dark background)
       - Tooltip cursor highlight should be visible
    6. Visit `/login`, `/register`, `/verify-otp`:
       - Pages should be readable in dark mode
       - Brand colors (orange warning on login, Google SVG) preserved
  </how-to-verify>
  <resume-signal>Type "approved" if all surfaces look correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
`npx jest --no-coverage` passes all tests.
Visual: sidebar is visually distinct surface in dark mode.
Visual: billing page has no trapped white/light panels.
Visual: analytics chart elements readable in dark mode.
Visual: auth pages readable, brand colors preserved.
</verification>

<success_criteria>
AUTH-01, AUTH-02, AUTH-03 all GREEN. Full test suite passes. Visual dark mode inspection confirms all Phase 3 surfaces render correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-auth-surface/03-04-SUMMARY.md`
</output>
