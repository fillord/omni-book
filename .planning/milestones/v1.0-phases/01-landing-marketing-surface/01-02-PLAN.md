---
phase: 01-landing-marketing-surface
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/landing/Testimonials.tsx
  - components/landing/NicheCards.tsx
  - components/landing/DemoSection.tsx
  - components/landing/StatsCounter.tsx
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04, LAND-05]

must_haves:
  truths:
    - "Testimonials card uses bg-card border-border instead of hardcoded zinc dual pairs"
    - "NicheCards heading and body text use text-foreground and text-muted-foreground"
    - "DemoSection text and arrow icon use semantic tokens"
    - "StatsCounter stat label uses text-muted-foreground"
    - "All landing surface tests pass with zero failures"
  artifacts:
    - path: "components/landing/Testimonials.tsx"
      provides: "Remediated testimonials with semantic tokens"
      contains: "bg-card"
    - path: "components/landing/NicheCards.tsx"
      provides: "Remediated niche cards with semantic tokens"
      contains: "text-foreground"
    - path: "components/landing/DemoSection.tsx"
      provides: "Remediated demo section with semantic tokens"
      contains: "text-muted-foreground"
    - path: "components/landing/StatsCounter.tsx"
      provides: "Remediated stats counter with semantic tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "__tests__/landing-surface.test.ts"
      to: "components/landing/*.tsx"
      via: "All LAND-01 through LAND-07 assertions pass green"
      pattern: "Tests:.*passed"
---

<objective>
Remediate the remaining four landing components (Testimonials, NicheCards, DemoSection, StatsCounter) by replacing hardcoded zinc/slate/gray neutral classes with semantic shadcn/ui tokens, completing the full landing surface remediation.

Purpose: Complete all LAND requirement remediations so the entire landing page renders correctly in both light and dark mode.
Output: Four fully remediated components; all landing surface tests pass green.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-landing-marketing-surface/01-RESEARCH.md
@.planning/phases/01-landing-marketing-surface/01-01-SUMMARY.md

<interfaces>
<!-- Semantic tokens available (from globals.css, confirmed in Phase 0) -->
- bg-background: page canvas
- bg-card: raised card surface
- bg-muted: subtle background
- text-foreground: primary text (headings, body)
- text-muted-foreground: secondary text (subtitles, captions)
- border-border: borders and dividers
- hover:bg-muted: hover background on transparent elements
- hover:text-foreground: hover on muted text links
- bg-muted-foreground/40: visible dot/indicator (with opacity)

<!-- Test file created in Plan 01 -->
From __tests__/landing-surface.test.ts:
- Tests read each component via fs.readFileSync
- Asserts absence of bare zinc/slate/gray classes via regex
- Footer.tsx exempted from LAND-01
- Brand accents (indigo/blue/pink/orange/green) are NOT flagged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remediate Testimonials.tsx and NicheCards.tsx</name>
  <files>components/landing/Testimonials.tsx, components/landing/NicheCards.tsx</files>
  <read_first>
    - components/landing/Testimonials.tsx (current state — find exact class locations)
    - components/landing/NicheCards.tsx (current state — find exact class locations)
    - .planning/phases/01-landing-marketing-surface/01-RESEARCH.md (Testimonials and NicheCards audit tables)
  </read_first>
  <action>
**Testimonials.tsx — 8 replacements:**

1. Find `text-zinc-900 dark:text-slate-50` on the section heading h2 -> replace with `text-foreground`
2. Find `text-zinc-500 dark:text-zinc-400` on the section subtitle p -> replace with `text-muted-foreground`
3. Find `bg-zinc-50 dark:bg-zinc-900` on the testimonial card container div -> replace with `bg-card` (remove both classes)
4. Find `border-zinc-200 dark:border-zinc-800` on the same card container -> replace with `border-border` (remove both classes)
5. Find `text-zinc-700 dark:text-zinc-200` on the quote text p -> replace with `text-foreground`
6. Find `text-zinc-900 dark:text-slate-50` on the author name p -> replace with `text-foreground`
7. Find `text-zinc-500 dark:text-zinc-400` on the author role p -> replace with `text-muted-foreground`
8. Find `bg-zinc-300 hover:bg-zinc-400` on the inactive dot button -> replace with `bg-muted-foreground/40 hover:bg-muted-foreground/60`
9. Find `bg-zinc-100 text-zinc-600` on the fallback avatar div -> replace with `bg-muted text-muted-foreground`

**DO NOT TOUCH:**
- `NICHE_COLORS` map values (blue/pink/orange/green brand accents with dark: variants) — preserve all
- Active dot styling (likely uses indigo) — preserve

**NicheCards.tsx — 4 replacements:**

1. Find `text-zinc-900 dark:text-white` on the section heading h2 -> replace with `text-foreground`
2. Find `text-zinc-500 dark:text-zinc-400` on the section subtitle p -> replace with `text-muted-foreground`
3. Find `text-zinc-900 dark:text-white` on card heading h3 -> replace with `text-foreground`
4. Find `text-zinc-600 dark:text-zinc-400` on card body p -> replace with `text-muted-foreground`

**DO NOT TOUCH:**
- `COLOR_MAP` with blue/pink/orange/green bg, icon, border values (all have dark: variants) — preserve all

For each replacement: remove BOTH the base class AND its `dark:` override. The single semantic token replaces the pair.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/landing-surface.test.ts --no-coverage -t "Testimonials|NicheCards" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Testimonials.tsx contains `bg-card` (at least 1 occurrence for card container)
    - Testimonials.tsx contains `border-border` (at least 1 occurrence for card border)
    - Testimonials.tsx contains `text-foreground` (at least 3 occurrences: heading, quote, author name)
    - Testimonials.tsx contains `text-muted-foreground` (at least 2 occurrences: subtitle, author role)
    - Testimonials.tsx contains `bg-muted-foreground/40` (dot navigation)
    - Testimonials.tsx contains `bg-muted text-muted-foreground` (fallback avatar)
    - Testimonials.tsx does NOT contain bare `bg-zinc-50` (card background replaced)
    - Testimonials.tsx does NOT contain bare `border-zinc-200` (card border replaced)
    - Testimonials.tsx does NOT contain bare `text-zinc-900` without dark: prefix
    - NicheCards.tsx contains `text-foreground` (at least 2 occurrences)
    - NicheCards.tsx contains `text-muted-foreground` (at least 2 occurrences)
    - NicheCards.tsx does NOT contain bare `text-zinc-900` without dark: prefix
    - NicheCards.tsx still contains niche brand colors (blue/pink/orange/green classes preserved)
  </acceptance_criteria>
  <done>Testimonials.tsx and NicheCards.tsx contain zero hardcoded neutral classes; all brand and niche accent colors preserved</done>
</task>

<task type="auto">
  <name>Task 2: Remediate DemoSection.tsx and StatsCounter.tsx, run full test suite</name>
  <files>components/landing/DemoSection.tsx, components/landing/StatsCounter.tsx</files>
  <read_first>
    - components/landing/DemoSection.tsx (current state — find exact class locations)
    - components/landing/StatsCounter.tsx (current state — find exact class locations)
    - .planning/phases/01-landing-marketing-surface/01-RESEARCH.md (DemoSection and StatsCounter audit tables)
  </read_first>
  <action>
**DemoSection.tsx — 6 replacements:**

1. Find `text-zinc-900 dark:text-white` on the section heading h2 -> replace with `text-foreground`
2. Find `text-zinc-500 dark:text-zinc-400` on the section subtitle p -> replace with `text-muted-foreground`
3. Find `text-zinc-900 dark:text-white` on card heading h3 -> replace with `text-foreground`
4. Find `text-zinc-600 dark:text-zinc-400` on card body p -> replace with `text-muted-foreground`
5. Find `text-zinc-500 dark:text-zinc-400` on card slug span -> replace with `text-muted-foreground`
6. Find `text-zinc-400 group-hover:text-zinc-700 dark:text-zinc-500 dark:group-hover:text-white` on the arrow icon -> replace all four classes with `text-muted-foreground group-hover:text-foreground`

**DO NOT TOUCH:**
- Niche `color` strings in the `DEMOS` array that include `dark:` variants (blue/pink/orange/green) — preserve all
- Any `bg-*` or `text-*` classes that are niche brand accents

**StatsCounter.tsx — 1 replacement:**

1. Find `text-zinc-500 dark:text-zinc-400` on the stat label p -> replace with `text-muted-foreground`

**DO NOT TOUCH:**
- `text-indigo-600` on the stat value — intentional brand accent

**After all edits:** Run the FULL landing surface test suite AND the full Jest suite to confirm all LAND tests pass and no Phase 0 regressions:
```bash
npx jest __tests__/landing-surface.test.ts --no-coverage
npx jest --no-coverage
```
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DemoSection.tsx contains `text-foreground` (at least 2 occurrences)
    - DemoSection.tsx contains `text-muted-foreground` (at least 3 occurrences)
    - DemoSection.tsx contains `group-hover:text-foreground` (arrow icon)
    - DemoSection.tsx does NOT contain bare `text-zinc-900` without dark: prefix
    - DemoSection.tsx does NOT contain `group-hover:text-zinc-700`
    - DemoSection.tsx still contains niche brand colors (dark: variants preserved in DEMOS array)
    - StatsCounter.tsx contains `text-muted-foreground` (at least 1 occurrence)
    - StatsCounter.tsx does NOT contain bare `text-zinc-500` without dark: prefix
    - StatsCounter.tsx still contains `text-indigo-600` (brand accent preserved)
    - `npx jest --no-coverage` exits 0 (all tests pass, including Phase 0 infrastructure tests)
    - `npx jest __tests__/landing-surface.test.ts --no-coverage` exits 0 (all LAND tests pass)
  </acceptance_criteria>
  <done>All landing components remediated; full test suite green; zero hardcoded neutral classes remain in components/landing/ (except intentional Footer exception); all brand accents preserved</done>
</task>

</tasks>

<verification>
After all tasks:
- `npx jest __tests__/landing-surface.test.ts --no-coverage` — ALL LAND-01 through LAND-07 assertions pass green
- `npx jest --no-coverage` — full suite green (Phase 0 infrastructure tests unaffected)
- Grep for remaining violations: `grep -rn "text-zinc-900\|bg-zinc-50\|border-zinc-200" components/landing/ --include="*.tsx"` returns zero matches (except Footer.tsx which is exempted)
</verification>

<success_criteria>
- Testimonials.tsx: card uses bg-card border-border, all text semantic, dot nav uses muted-foreground opacity
- NicheCards.tsx: all text semantic, niche COLOR_MAP brand accents preserved
- DemoSection.tsx: all text semantic, arrow icon uses semantic hover, niche DEMOS brand accents preserved
- StatsCounter.tsx: label text semantic, indigo stat value preserved
- Full Jest suite passes with 0 failures
- Zero hardcoded neutral zinc/slate/gray classes remaining in components/landing/ (Footer exempt)
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-marketing-surface/01-02-SUMMARY.md`
</output>
