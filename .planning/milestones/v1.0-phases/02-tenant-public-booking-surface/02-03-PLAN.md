---
phase: 02-tenant-public-booking-surface
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/booking-form.tsx
  - components/booking-calendar.tsx
autonomous: true
requirements:
  - BOOK-02
  - BOOK-03
  - BOOK-04

must_haves:
  truths:
    - "booking-form.tsx has zero hardcoded zinc/slate dual-class pairs"
    - "Date input uses bg-background border-input text-foreground (not bg-white border-zinc)"
    - "booking-calendar.tsx has no bg-gray-400 fallback"
    - "RESOURCE_PALETTE is preserved with intentional comment"
    - "BOOKING_COLORS niche accents (blue-600, pink-600, orange-600, green-600) are untouched"
  artifacts:
    - path: "components/booking-form.tsx"
      provides: "Fully remediated booking form"
      contains: "bg-background"
    - path: "components/booking-calendar.tsx"
      provides: "Remediated booking calendar with RESOURCE_PALETTE comment"
      contains: "intentional"
  key_links:
    - from: "components/booking-form.tsx"
      to: "globals.css token system"
      via: "Tailwind utility classes"
      pattern: "bg-background|bg-card|text-foreground|text-muted-foreground|border-input|border-border"
    - from: "components/booking-calendar.tsx"
      to: "globals.css token system"
      via: "Tailwind utility classes"
      pattern: "bg-muted-foreground"
---

<objective>
Remediate all hardcoded zinc/slate/gray color classes in `components/booking-form.tsx` and `components/booking-calendar.tsx` with semantic tokens.

Purpose: BOOK-02 (form inputs), BOOK-03 (calendar neutrals), BOOK-04 (preserve niche accents). booking-calendar.tsx needs only 1 line change + 1 comment; booking-form.tsx has 40+ replacements.

Output: Both files fully remediated. All BOOK tests pass.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-public-booking-surface/02-RESEARCH.md
@.planning/phases/02-tenant-public-booking-surface/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded zinc/slate classes in booking-form.tsx with semantic tokens</name>
  <files>components/booking-form.tsx</files>
  <read_first>
    components/booking-form.tsx
    .planning/phases/02-tenant-public-booking-surface/02-RESEARCH.md
    __tests__/booking-surface.test.ts
  </read_first>
  <action>
Open `components/booking-form.tsx` and make the following replacements. DO NOT touch any classes inside the `BOOKING_COLORS` constant object (niche brand accents). DO NOT touch `bg-green-100 text-green-600` in the SuccessScreen checkmark (functional positive feedback).

**StepIndicator sub-component:**

Inactive step circle (~line 158):
- `border-zinc-200 text-zinc-400` --> `border-border text-muted-foreground`

Active step label (~line 164):
- `text-zinc-800` (active) --> `text-foreground`
- `text-zinc-400` (inactive) --> `text-muted-foreground`

Undone step line (~line 172):
- `bg-zinc-200` --> `bg-border`

**SuccessScreen sub-component:**

Title (~line 263):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Subtitle (~line 264):
- `text-zinc-500 dark:text-zinc-400` --> `text-muted-foreground`

Summary box (~line 267):
- `border-zinc-200 dark:border-zinc-700 bg-zinc-50 dark:bg-zinc-900` --> `border-border bg-card`

Booking reference label (~line 277):
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

Booking reference value (~line 278):
- `text-zinc-600 dark:text-zinc-200` --> `text-foreground`

Back button (~line 285):
- `border-zinc-200 text-zinc-700 hover:bg-zinc-50` --> `border-border text-foreground hover:bg-muted`

Download calendar button (~line 291):
- `bg-zinc-900 text-white hover:bg-zinc-700` --> `bg-foreground text-background hover:bg-foreground/90`

**Step 1 - Service selection:**

Section title (~line 468):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Unselected service card (~line 482):
- `border-zinc-200 hover:border-zinc-300` --> `border-border hover:border-border/80`

Service name (~line 488):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Service price (~line 489):
- `text-zinc-700 dark:text-zinc-200` --> `text-foreground`

Service description (~line 494):
- `text-zinc-500 dark:text-zinc-400` --> `text-muted-foreground`

Service duration badge (~line 496):
- `bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-200` --> `bg-muted text-muted-foreground`

**Step 2 - Resource selection:**

Section title (~line 519):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Resource count badge (~line 522):
- `bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-200` --> `bg-muted text-muted-foreground`

Unselected resource card (~line 539):
- `border-zinc-200 hover:border-zinc-300` --> `border-border hover:border-border/80`

Resource name (~line 545):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Resource specialty badge (~line 549):
- `bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-200` --> `bg-muted text-muted-foreground`

Resource experience badge (~line 552):
- Same pattern --> `bg-muted text-muted-foreground`

Resource availability badge (~line 562):
- `bg-zinc-100 text-zinc-600` --> `bg-muted text-muted-foreground`

Resource rating badge (~line 566):
- `bg-zinc-100 text-zinc-600` --> `bg-muted text-muted-foreground`

Resource services badge (~line 570):
- `bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-200` --> `bg-muted text-muted-foreground`

Back button (~line 579):
- `border-zinc-200 text-zinc-700 hover:bg-zinc-50` --> `border-border text-foreground hover:bg-muted`

**Step 3 - Date/Time (BOOK-02 core):**

Section title (~line 597):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Service/resource info badges (~lines 599, 600):
- `bg-zinc-100 dark:bg-zinc-800 text-zinc-600 dark:text-zinc-200` --> `bg-muted text-muted-foreground`

Date label (~line 605):
- `text-zinc-700 dark:text-zinc-100` --> `text-foreground`

Date input (~line 619) -- BOOK-02 CORE:
- `border-zinc-200 dark:border-zinc-700` --> `border-input`
- `bg-white dark:bg-zinc-900 dark:text-slate-50` --> `bg-background text-foreground`
- `focus:border-zinc-400 dark:focus:border-zinc-500` --> `focus:border-ring`

Time slot label (~line 626):
- `text-zinc-700 dark:text-zinc-100` --> `text-foreground`

Loading text (~line 628):
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

Skeleton loader (~line 641):
- `bg-zinc-100 dark:bg-zinc-800` --> `bg-muted`

No slots text (~line 645):
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

No slots helper (~line 647):
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

Unavailable time slot (~line 660):
- `border-zinc-100 bg-zinc-50 text-zinc-300 dark:border-zinc-800 dark:bg-zinc-900 dark:text-zinc-600` --> `border-border/50 bg-muted/50 text-muted-foreground/40`

Available unselected time slot (~line 663):
- `border-zinc-200 text-zinc-700` --> `border-border text-foreground`

Time slot helper (~line 670):
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

Back button (~line 677):
- `border-zinc-200 text-zinc-700 hover:bg-zinc-50` --> `border-border text-foreground hover:bg-muted`

**Step 4 - Confirm:**

Section title (~line 694):
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

Summary box (~line 696):
- `border-zinc-200 dark:border-zinc-700 bg-zinc-50 dark:bg-zinc-900` --> `border-border bg-card`

Summary labels (~lines 714, 717, 728, 734):
- `text-zinc-700 dark:text-zinc-100` --> `text-foreground`

Back button (~line 747):
- `border-zinc-200 text-zinc-700 hover:bg-zinc-50` --> `border-border text-foreground hover:bg-muted`

**SummaryRow sub-component:**

Label (~line 779):
- `text-zinc-500 dark:text-zinc-400` --> `text-muted-foreground`

Value (~line 780):
- `text-zinc-800 dark:text-zinc-100` --> `text-foreground`

After all replacements, run the test suite to confirm BOOK-02 and BOOK-04 assertions pass.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/booking-surface.test.ts --no-coverage -t "BOOK-02|BOOK-04" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `components/booking-form.tsx` does NOT contain `bg-white` (no bare occurrences)
    - `components/booking-form.tsx` does NOT contain `bg-zinc-` (zero zinc backgrounds)
    - `components/booking-form.tsx` does NOT contain `text-zinc-` (zero zinc text)
    - `components/booking-form.tsx` does NOT contain `text-slate-` (zero slate text)
    - `components/booking-form.tsx` does NOT contain `border-zinc-` (zero zinc borders)
    - `components/booking-form.tsx` does NOT contain `dark:bg-zinc-` (zero dark zinc backgrounds)
    - `components/booking-form.tsx` does NOT contain `hover:bg-zinc-` (zero zinc hovers)
    - `components/booking-form.tsx` contains `bg-background` (date input)
    - `components/booking-form.tsx` contains `border-input` (date input border)
    - `components/booking-form.tsx` contains `text-foreground` (headings/labels)
    - `components/booking-form.tsx` contains `text-muted-foreground` (secondary text)
    - `components/booking-form.tsx` contains `bg-muted` (badges/chips)
    - `components/booking-form.tsx` contains `border-border` (card/button borders)
    - `components/booking-form.tsx` contains `bg-blue-600` (BOOKING_COLORS accent)
    - `components/booking-form.tsx` contains `bg-pink-600` (BOOKING_COLORS accent)
    - `components/booking-form.tsx` contains `bg-green-100` (SuccessScreen checkmark preserved)
    - BOOK-02, BOOK-04 tests pass
  </acceptance_criteria>
  <done>All 40+ hardcoded zinc/slate/gray dual-class pairs in booking-form.tsx replaced with semantic tokens. Date input uses bg-background border-input (BOOK-02). BOOKING_COLORS niche accents preserved (BOOK-04). BOOK-02 and BOOK-04 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix booking-calendar.tsx fallback dot and add RESOURCE_PALETTE comment</name>
  <files>components/booking-calendar.tsx</files>
  <read_first>
    components/booking-calendar.tsx
    .planning/phases/02-tenant-public-booking-surface/02-RESEARCH.md
    __tests__/booking-surface.test.ts
  </read_first>
  <action>
Open `components/booking-calendar.tsx` and make two targeted changes:

**Change 1 — RESOURCE_PALETTE intentional comment (~lines 30-37):**
Add a JS comment directly above the `const RESOURCE_PALETTE` declaration:
```typescript
// intentional: RESOURCE_PALETTE uses fixed pastel tints for resource color-coding
// (functional visual differentiation, not neutral backgrounds -- preserve in both modes)
const RESOURCE_PALETTE = [
```
DO NOT change any values inside RESOURCE_PALETTE.

**Change 2 — Fallback dot color (~line 434):**
Replace `bg-gray-400` with `bg-muted-foreground` in the fallback dot color:
```tsx
// BEFORE
className={`h-2.5 w-2.5 rounded-full ${color?.dot ?? 'bg-gray-400'}`}
// AFTER
className={`h-2.5 w-2.5 rounded-full ${color?.dot ?? 'bg-muted-foreground'}`}
```

That is the complete scope for booking-calendar.tsx. The calendar already uses semantic tokens for its structural elements (`bg-background`, `bg-muted/30`, `text-muted-foreground`, `text-primary`, `bg-primary`, `border-border`).

After changes, run the full booking-surface test suite.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/booking-surface.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `components/booking-calendar.tsx` does NOT contain `bg-gray-400`
    - `components/booking-calendar.tsx` contains `bg-muted-foreground` (replacement fallback dot)
    - `components/booking-calendar.tsx` contains `intentional` (RESOURCE_PALETTE comment)
    - `components/booking-calendar.tsx` contains `RESOURCE_PALETTE` (palette preserved)
    - `components/booking-calendar.tsx` contains `bg-blue-100` (RESOURCE_PALETTE entry preserved)
    - All BOOK-01 through BOOK-05 tests pass in full suite run
  </acceptance_criteria>
  <done>booking-calendar.tsx fallback dot uses bg-muted-foreground instead of bg-gray-400. RESOURCE_PALETTE preserved with intentional comment. BOOK-03 tests pass. Full booking-surface test suite GREEN.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/booking-surface.test.ts --no-coverage` -- all BOOK-01 through BOOK-05 tests pass
- `npx jest --no-coverage` -- full test suite passes (no regressions from Phase 0/1 tests)
- `grep -c "zinc\|slate" components/booking-form.tsx` -- count is zero (only inside BOOKING_COLORS which uses niche colors not zinc/slate)
- `grep "bg-gray-400" components/booking-calendar.tsx` -- zero matches
</verification>

<success_criteria>
- Zero hardcoded zinc/slate neutrals in booking-form.tsx (outside BOOKING_COLORS)
- Date input uses bg-background border-input text-foreground
- booking-calendar.tsx bg-gray-400 replaced with bg-muted-foreground
- RESOURCE_PALETTE preserved with intentional comment
- All BOOKING_COLORS niche accents present
- Full booking-surface test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-tenant-public-booking-surface/02-03-SUMMARY.md`
</output>
