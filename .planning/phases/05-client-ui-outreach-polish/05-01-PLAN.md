---
phase: 05-client-ui-outreach-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/client-ui-surface.test.ts
  - lib/i18n/translations.ts
  - components/dashboard-sidebar.tsx
autonomous: true
requirements: [CRM-06, CRM-12]

must_haves:
  truths:
    - "Sidebar shows a 'Clients' link that navigates to /dashboard/clients"
    - "All client UI strings exist in RU, EN, and KZ translations"
    - "Static file assertion tests exist for CRM-06 through CRM-12"
  artifacts:
    - path: "__tests__/client-ui-surface.test.ts"
      provides: "Static file assertions for CRM-06 through CRM-12"
      min_lines: 80
    - path: "lib/i18n/translations.ts"
      provides: "clients section in all three locales"
      contains: "clients:"
    - path: "components/dashboard-sidebar.tsx"
      provides: "Sidebar link to /dashboard/clients with Users icon"
      contains: "/dashboard/clients"
  key_links:
    - from: "components/dashboard-sidebar.tsx"
      to: "lib/i18n/translations.ts"
      via: "t('clients', 'title')"
      pattern: "section: 'clients'"
---

<objective>
Create the test scaffold for all Phase 5 requirements, add i18n translation strings for the clients section in all three locales, and wire the sidebar navigation link to /dashboard/clients.

Purpose: Establishes the test-first verification foundation and the shared infrastructure (translations + navigation) that Plans 02 and 03 depend on.
Output: Test file, updated translations, updated sidebar.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-client-ui-outreach-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static file assertion test scaffold for CRM-06 through CRM-12</name>
  <files>__tests__/client-ui-surface.test.ts</files>
  <read_first>
    - __tests__/client-data-surface.test.ts (safeRead pattern + test structure reference)
  </read_first>
  <action>
Create `__tests__/client-ui-surface.test.ts` using the exact `safeRead` helper pattern from `client-data-surface.test.ts`. The file must:

1. Import `fs` and `path`, define `ROOT = path.resolve(__dirname, '..')`, define `safeRead` with `fs.existsSync` check.

2. Create these describe blocks with these exact tests:

**CRM-06: Sidebar navigation to Clients page**
- `safeRead('components/dashboard-sidebar.tsx')` contains `Users` (lucide-react icon import)
- sidebar contains `/dashboard/clients` (href)
- sidebar contains `section: 'clients'` (translation namespace)
- `fs.existsSync(path.join(ROOT, 'app/dashboard/clients/page.tsx'))` is `true`

**CRM-07: Clients table component**
- `fs.existsSync(path.join(ROOT, 'components/clients-table.tsx'))` is `true`
- `safeRead('components/clients-table.tsx')` contains `Table` (UI import)
- clients-table contains `totalVisits`
- clients-table contains `totalRevenue`
- clients-table contains `hasTelegram`

**CRM-08: Search bar filtering**
- `safeRead('components/clients-table.tsx')` contains `useState`
- clients-table contains `useMemo`
- clients-table contains `searchPlaceholder` (i18n key reference)

**CRM-09: Client detail page**
- `fs.existsSync(path.join(ROOT, 'app/dashboard/clients/[clientId]/page.tsx'))` is `true`
- `safeRead('app/dashboard/clients/[clientId]/page.tsx')` contains `guestPhone` (booking query by phone, not by Client relation)
- detail page contains `notFound` (ownership check)

**CRM-10: Telegram outreach action**
- `safeRead('lib/actions/clients.ts')` contains `sendTelegramToClient`
- clients.ts contains `sendTelegramMessage` (telegram utility call)
- clients.ts contains `telegramChatId` (booking lookup for chat ID)
- `safeRead('components/client-detail.tsx')` contains `sendTelegramToClient` (UI wires to action)

**CRM-11: Neumorphism design adherence**
- `safeRead('components/clients-table.tsx')` contains `Card` (neu-raised wrapper)
- `safeRead('components/client-detail.tsx')` contains `Card` (neu-raised wrapper)

**CRM-12: i18n translations in all three locales**
- `safeRead('lib/i18n/translations.ts')` — use regex to verify `clients:` section exists within each locale block. Test that the file contains `clients:` at least 3 times (once per locale).
- translations contain `searchPlaceholder`
- translations contain `sendMessage`
- translations contain `bookingHistory`
  </action>
  <verify>
    <automated>npx vitest run __tests__/client-ui-surface.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/client-ui-surface.test.ts exists and is > 80 lines
    - File contains `safeRead` helper function
    - File contains describe blocks for CRM-06, CRM-07, CRM-08, CRM-09, CRM-10, CRM-11, CRM-12
    - File contains `dashboard-sidebar.tsx` in a safeRead call
    - File contains `clients-table.tsx` in a safeRead call
    - File contains `client-detail.tsx` in a safeRead call
    - File contains `sendTelegramToClient` string assertion
    - Tests run (most will FAIL since implementation files don't exist yet — that is expected and correct)
  </acceptance_criteria>
  <done>Test scaffold created with static file assertions covering all 7 Phase 5 requirements. Tests are expected to fail until Plans 02 and 03 implement the actual files.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n clients section + sidebar link</name>
  <files>lib/i18n/translations.ts, components/dashboard-sidebar.tsx</files>
  <read_first>
    - lib/i18n/translations.ts (full file — need to see all three locale blocks and existing section patterns)
    - components/dashboard-sidebar.tsx (full file — need to see items array and lucide-react imports)
  </read_first>
  <action>
**Part A: Add `clients` section to all three locales in `lib/i18n/translations.ts`.**

Add the following section to the `ru` locale object (after the last existing section, before the closing brace):

```
clients: {
  title:              'Клиенты',
  subtitle:           'База клиентов из истории бронирований',
  searchPlaceholder:  'Поиск по имени или телефону',
  colName:            'Имя',
  colContact:         'Контакт',
  colVisits:          'Визитов',
  colSpent:           'Потрачено',
  colLastVisit:       'Последний визит',
  colTelegram:        'Telegram',
  telegramConnected:  'Подключён',
  telegramNone:       'Нет',
  empty:              'Клиенты не найдены. Синхронизируйте базу.',
  syncButton:         'Синхронизировать',
  synced:             'Синхронизировано клиентов: {n}',
  syncError:          'Ошибка синхронизации',
  sendMessage:        'Отправить сообщение',
  messagePlaceholder: 'Текст сообщения для клиента…',
  messageSent:        'Сообщение отправлено',
  messageError:       'Ошибка отправки',
  backToList:         '← Клиенты',
  bookingHistory:     'История бронирований',
  colDate:            'Дата',
  colService:         'Услуга',
  colResource:        'Ресурс',
  colPrice:           'Сумма',
  colStatus:          'Статус',
  noBookings:         'История посещений пуста',
  noTelegram:         'Telegram не подключён',
},
```

Add the equivalent `clients` section to the `kz` locale:
```
clients: {
  title:              'Клиенттер',
  subtitle:           'Брондау тарихынан клиенттер базасы',
  searchPlaceholder:  'Аты немесе телефон бойынша іздеу',
  colName:            'Аты',
  colContact:         'Байланыс',
  colVisits:          'Келулер',
  colSpent:           'Жұмсалды',
  colLastVisit:       'Соңғы келу',
  colTelegram:        'Telegram',
  telegramConnected:  'Қосылған',
  telegramNone:       'Жоқ',
  empty:              'Клиенттер табылмады. Базаны синхрондаңыз.',
  syncButton:         'Синхрондау',
  synced:             'Синхрондалған клиенттер: {n}',
  syncError:          'Синхрондау қатесі',
  sendMessage:        'Хабарлама жіберу',
  messagePlaceholder: 'Клиентке хабарлама мәтіні…',
  messageSent:        'Хабарлама жіберілді',
  messageError:       'Жіберу қатесі',
  backToList:         '← Клиенттер',
  bookingHistory:     'Брондау тарихы',
  colDate:            'Күні',
  colService:         'Қызмет',
  colResource:        'Ресурс',
  colPrice:           'Сома',
  colStatus:          'Мәртебе',
  noBookings:         'Келу тарихы бос',
  noTelegram:         'Telegram қосылмаған',
},
```

Add the equivalent `clients` section to the `en` locale:
```
clients: {
  title:              'Clients',
  subtitle:           'Client base from booking history',
  searchPlaceholder:  'Search by name or phone',
  colName:            'Name',
  colContact:         'Contact',
  colVisits:          'Visits',
  colSpent:           'Spent',
  colLastVisit:       'Last Visit',
  colTelegram:        'Telegram',
  telegramConnected:  'Connected',
  telegramNone:       'None',
  empty:              'No clients found. Sync your database.',
  syncButton:         'Sync',
  synced:             'Synced clients: {n}',
  syncError:          'Sync error',
  sendMessage:        'Send message',
  messagePlaceholder: 'Message text for client...',
  messageSent:        'Message sent',
  messageError:       'Send error',
  backToList:         '← Clients',
  bookingHistory:     'Booking history',
  colDate:            'Date',
  colService:         'Service',
  colResource:        'Resource',
  colPrice:           'Amount',
  colStatus:          'Status',
  noBookings:         'No visit history',
  noTelegram:         'Telegram not connected',
},
```

**Part B: Add sidebar link in `components/dashboard-sidebar.tsx`.**

1. Add `Users` to the existing lucide-react import on line 8-12:
   ```
   import {
     Menu, LogOut, ExternalLink,
     LayoutDashboard, CalendarDays, Wrench, Scissors, Settings, BarChart3,
     Zap, Clock, Users
   } from 'lucide-react'
   ```

2. Add the clients entry to the `items` array (line 80-87), inserting it AFTER the bookings entry and BEFORE the analytics entry:
   ```
   { href: '/dashboard/clients',   section: 'clients',   tKey: 'title',     icon: Users,        exact: false },
   ```

   The full items array should become:
   ```
   const items = [
     { href: '/dashboard',           section: 'dashboard', tKey: 'overview',  icon: LayoutDashboard, exact: true  },
     { href: '/dashboard/resources', section: 'niche',     tKey: nicheConfig.resourceLabelPlural, icon: Wrench,       exact: false },
     { href: '/dashboard/services',  section: 'dashboard', tKey: 'services',  icon: Scissors,        exact: false },
     { href: '/dashboard/bookings',  section: 'dashboard', tKey: 'bookings',  icon: CalendarDays,    exact: false },
     { href: '/dashboard/clients',   section: 'clients',   tKey: 'title',     icon: Users,           exact: false },
     { href: '/dashboard/analytics', section: 'dashboard', tKey: 'analytics', icon: BarChart3,       exact: false },
     { href: '/dashboard/settings',  section: 'dashboard', tKey: 'settings',  icon: Settings,        exact: false },
   ]
   ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/i18n/translations.ts contains `clients:` at least 3 times (once per locale)
    - translations.ts ru locale contains `title: 'Клиенты'` inside clients section
    - translations.ts en locale contains `title: 'Clients'` inside clients section
    - translations.ts kz locale contains `title: 'Клиенттер'` inside clients section
    - translations.ts contains `searchPlaceholder`
    - translations.ts contains `sendMessage`
    - translations.ts contains `bookingHistory`
    - translations.ts contains `noTelegram`
    - components/dashboard-sidebar.tsx contains `Users` in lucide-react import
    - dashboard-sidebar.tsx contains `/dashboard/clients`
    - dashboard-sidebar.tsx contains `section: 'clients'`
    - dashboard-sidebar.tsx contains `icon: Users`
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>All 27 translation keys (3 locales x 27 keys) exist. Sidebar shows "Clients" link with Users icon pointing to /dashboard/clients.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors from sidebar or translations changes)
- `npx vitest run __tests__/client-ui-surface.test.ts` — CRM-06 sidebar tests pass, CRM-12 translation tests pass; CRM-07/08/09/10/11 tests fail (expected — implementation in Plans 02+03)
</verification>

<success_criteria>
- Test scaffold exists with static assertions for all 7 requirements (CRM-06 through CRM-12)
- Sidebar contains a "Clients" link with Users icon navigating to /dashboard/clients
- Translation strings for `clients` section exist in all three locales (ru, kz, en) with all 27 keys
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-ui-outreach-polish/05-01-SUMMARY.md`
</output>
