---
phase: 04-client-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/client-data-surface.test.ts
  - prisma/schema.prisma
autonomous: true
requirements:
  - CRM-01

must_haves:
  truths:
    - "prisma/schema.prisma contains a Client model with id, tenantId, phone, name, email, totalVisits, totalRevenue, lastVisitAt, hasTelegram fields"
    - "Client model has @@unique([tenantId, phone]) composite unique constraint"
    - "Client model has onDelete: Cascade relation to Tenant"
    - "Tenant model has clients Client[] back-relation"
    - "Test file covers CRM-01 through CRM-05 with static file assertions"
  artifacts:
    - path: "__tests__/client-data-surface.test.ts"
      provides: "Static file assertion tests for CRM-01 through CRM-05"
      min_lines: 80
    - path: "prisma/schema.prisma"
      provides: "Client model definition with composite unique and Tenant back-relation"
      contains: "model Client"
  key_links:
    - from: "__tests__/client-data-surface.test.ts"
      to: "prisma/schema.prisma"
      via: "safeRead file assertion"
      pattern: "safeRead.*prisma/schema.prisma"
    - from: "prisma/schema.prisma"
      to: "model Tenant"
      via: "clients Client[] back-relation"
      pattern: "clients\\s+Client\\[\\]"
---

<objective>
Create the test scaffold for all CRM-01 through CRM-05 requirements and add the Client Prisma model to the schema with Tenant back-relation.

Purpose: Establishes the database foundation and test-first verification so Plan 02 can implement the sync logic against a known schema and known test expectations.
Output: `__tests__/client-data-surface.test.ts` (all tests initially failing for action-related assertions), `prisma/schema.prisma` updated with Client model.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-data-foundation/04-RESEARCH.md

@prisma/schema.prisma
@__tests__/subscription-lifecycle-surface.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create client-data-surface test scaffold covering CRM-01 through CRM-05</name>
  <files>__tests__/client-data-surface.test.ts</files>
  <read_first>
    __tests__/subscription-lifecycle-surface.test.ts
    prisma/schema.prisma
  </read_first>
  <action>
Create `__tests__/client-data-surface.test.ts` following the exact pattern from `subscription-lifecycle-surface.test.ts`:

1. Import `fs` and `path`, define `ROOT` and `safeRead` helper (same as existing tests).

2. **CRM-01 describe block** ("CRM-01: Client model in schema"):
   - Read `prisma/schema.prisma` via `safeRead`
   - Extract Client model section: `schema.match(/model Client \{[\s\S]*?(?=\nmodel |\nenum )/)`
   - Test: `schema` contains `'model Client {'`
   - Test: `schema` contains `'@@unique([tenantId, phone])'`
   - Test: `schema` contains `'@@index([tenantId])'`
   - Test: Client model section contains `'onDelete: Cascade'`
   - Test: Client model section contains `'tenantId       String'` (field existence)
   - Test: Client model section contains `'phone          String'`
   - Test: Client model section matches `/totalVisits\s+Int\s+@default\(0\)/`
   - Test: Client model section matches `/totalRevenue\s+Int\s+@default\(0\)/`
   - Test: Client model section contains `'lastVisitAt    DateTime?'`
   - Test: Client model section matches `/hasTelegram\s+Boolean\s+@default\(false\)/`
   - Extract Tenant model section: `schema.match(/model Tenant \{[\s\S]*?(?=\nmodel |\nenum )/)`
   - Test: Tenant model section contains `'clients'` and `'Client[]'`

3. **CRM-01 describe block** ("CRM-01: syncClients action file"):
   - Read `lib/actions/clients.ts` via `safeRead`
   - Test: file exists (`fs.existsSync`)
   - Test: file contains `'use server'`
   - Test: file contains `'syncClients'`
   - Test: file contains `'upsert'` (idempotency)
   - Test: file contains `'tenantId_phone'` (composite unique key in upsert where)
   - Test: file contains `'COMPLETED'` (booking status filter)
   - Test: file contains `'requireAuth'` (auth guard)
   - Test: file contains `'requireRole'` (role guard)

4. **CRM-02 describe block** ("CRM-02: Total visits count"):
   - Read `lib/actions/clients.ts` via `safeRead`
   - Test: file contains `'totalVisits'`
   - Test: file contains `'.length'` (count of bookings array)

5. **CRM-03 describe block** ("CRM-03: Total revenue"):
   - Read `lib/actions/clients.ts` via `safeRead`
   - Test: file contains `'totalRevenue'`
   - Test: file contains `'reduce'` (sum pattern)
   - Test: file contains `'price'` (service price reference)

6. **CRM-04 describe block** ("CRM-04: Last visit date"):
   - Read `lib/actions/clients.ts` via `safeRead`
   - Test: file contains `'lastVisitAt'`
   - Test: file contains `'startsAt'` (booking date field)

7. **CRM-05 describe block** ("CRM-05: Telegram status"):
   - Read `lib/actions/clients.ts` via `safeRead`
   - Test: file contains `'hasTelegram'`
   - Test: file contains `'telegramChatId'` (booking field check)

8. **CRM-01 describe block** ("CRM-01: getClients query action"):
   - Read `lib/actions/clients.ts` via `safeRead`
   - Test: file contains `'getClients'`
   - Test: file contains `'findMany'`
   - Test: file contains `'orderBy'`
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/client-data-surface.test.ts -t "CRM-01: Client model" --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/client-data-surface.test.ts exists and is > 80 lines
    - File contains `safeRead` helper function
    - File contains describe blocks for "CRM-01", "CRM-02", "CRM-03", "CRM-04", "CRM-05"
    - File contains `model Client {` string assertion
    - File contains `@@unique([tenantId, phone])` string assertion
    - File contains `syncClients` string assertion
    - File contains `upsert` string assertion
    - File contains `totalVisits` string assertion
    - File contains `totalRevenue` string assertion
    - File contains `lastVisitAt` string assertion
    - File contains `hasTelegram` string assertion
    - File contains `getClients` string assertion
    - Schema-related tests (CRM-01 Client model) FAIL because Client model does not exist yet
  </acceptance_criteria>
  <done>Test file created with all CRM-01 through CRM-05 static assertions. Schema tests fail (Client model not yet added). Action tests fail (clients.ts not yet created).</done>
</task>

<task type="auto">
  <name>Task 2: Add Client model to Prisma schema and sync database</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    prisma/schema.prisma
    .planning/phases/04-client-data-foundation/04-RESEARCH.md
  </read_first>
  <action>
1. Open `prisma/schema.prisma` and add the Client model AFTER the existing models (before the enums section). Add exactly this model definition:

```prisma
model Client {
  id             String    @id @default(cuid())
  tenantId       String
  phone          String
  name           String
  email          String?
  totalVisits    Int       @default(0)
  totalRevenue   Int       @default(0)
  lastVisitAt    DateTime?
  hasTelegram    Boolean   @default(false)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  tenant         Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@unique([tenantId, phone])
  @@index([tenantId])
}
```

2. Add the back-relation to the Tenant model. Find the Tenant model block (line ~10) and add this line after the `auditLogs AuditLog[]` line (currently line 41):
```
  clients        Client[]
```

3. Run `npx prisma generate` to regenerate the Prisma client types. Do NOT run `prisma db push` (that requires database access which may not be available in executor context -- the executor should note this as a manual step if it fails).

4. Run the CRM-01 schema tests to verify they pass: `npx jest __tests__/client-data-surface.test.ts -t "CRM-01: Client model" --no-coverage`
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/client-data-surface.test.ts -t "CRM-01: Client model" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `model Client {`
    - prisma/schema.prisma contains `@@unique([tenantId, phone])`
    - prisma/schema.prisma contains `@@index([tenantId])` within the Client model
    - prisma/schema.prisma Client model contains `onDelete: Cascade`
    - prisma/schema.prisma Client model contains `totalVisits    Int       @default(0)`
    - prisma/schema.prisma Client model contains `totalRevenue   Int       @default(0)`
    - prisma/schema.prisma Client model contains `hasTelegram    Boolean   @default(false)`
    - prisma/schema.prisma Client model contains `lastVisitAt    DateTime?`
    - prisma/schema.prisma Tenant model contains `clients` and `Client[]`
    - CRM-01 schema tests pass (npx jest -t "CRM-01: Client model" exits 0)
  </acceptance_criteria>
  <done>Client model added to schema.prisma with all required fields, composite unique constraint, Cascade delete, and Tenant back-relation. CRM-01 schema tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/client-data-surface.test.ts -t "CRM-01: Client model" --no-coverage` -- all schema assertion tests pass
- `npx jest __tests__/client-data-surface.test.ts --no-coverage` -- schema tests pass, action tests fail (expected -- Plan 02 will fix)
- `prisma/schema.prisma` contains `model Client {` and `@@unique([tenantId, phone])`
- Tenant model contains `clients Client[]`
</verification>

<success_criteria>
- Client model exists in schema.prisma with all 10 fields (id, tenantId, phone, name, email, totalVisits, totalRevenue, lastVisitAt, hasTelegram, createdAt, updatedAt, tenant relation)
- Composite unique constraint @@unique([tenantId, phone]) present
- Tenant model has clients Client[] back-relation
- Test scaffold covers all 5 CRM requirements with static file assertions
- Schema-related tests pass; action-related tests expected to fail until Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-data-foundation/04-01-SUMMARY.md`
</output>
