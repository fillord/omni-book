# Plan 12-04: Payment modal rewrite + billing-content cleanup + i18n updates

## Goal
Rewrite the payment modal to show a Paylink.kz redirect button with status polling instead of the mock QR + "Simulate Payment" button. Remove the Kaspi Merchant ID / API Key config section from `billing-content.tsx`. Update all i18n keys and references.

## Context
After Plans 12-01 through 12-03, the underlying infrastructure is clean. This plan handles the UI layer:
- `payment-modal.tsx`: Step 2 currently shows a mock SVG QR and "Simulate Payment" button. Replace with a "Оплатить через Paylink" button that opens the redirect URL + auto-polls for webhook confirmation.
- `billing-content.tsx`: Remove Kaspi config section, update `PendingPaymentInfo` type, add Paylink info banner.
- i18n: Replace payment/Kaspi keys with Paylink equivalents.

## Requirements covered
- PIV-09: Payment modal rewrite
- PIV-10: Billing UI cleanup + Neumorphic design consistency

---

## Tasks

### Task 1 — Rewrite `app/dashboard/settings/billing/payment-modal.tsx`

Read the full file first. Then apply the following changes:

**Step 1a — Update `PendingPayment` type:**
```ts
type PendingPayment = {
  id: string
  amount: number
  paylinkUrl: string | null   // CHANGED: was mockQrCode
  expiresAt: string
  planTarget: string
}
```

**Step 1b — Update `PaymentData` type:**
```ts
type PaymentData = {
  id: string
  paylinkUrl: string         // CHANGED: was mockQrCode
  amount: number
  expiresAt: string
}
```

**Step 1c — Remove `simulatePaymentAction` import:**
```ts
// DELETE: import { initiateSubscriptionPayment, simulatePaymentAction } from '@/lib/actions/billing'
// KEEP:   import { initiateSubscriptionPayment } from '@/lib/actions/billing'
```

**Step 1d — Remove the simulate payment button entirely** from Step 2 UI. The mock "Simulate Payment" button (which only appeared when `NEXT_PUBLIC_MOCK_PAYMENTS=true`) must be removed.

**Step 1e — Replace Step 2 QR display with Paylink redirect button + polling:**

Step 2 UI currently shows a mock SVG QR image. Replace with:

```tsx
{/* Step 2: Redirect to Paylink.kz */}
<div className="space-y-5">
  <div className="text-center space-y-2">
    <div className="text-4xl">🔗</div>
    <h3 className="text-base font-semibold text-foreground">
      Оплата через Paylink.kz
    </h3>
    <p className="text-sm text-muted-foreground">
      Нажмите кнопку ниже, чтобы перейти на страницу оплаты. 
      После оплаты подписка активируется автоматически.
    </p>
  </div>

  {/* Amount display */}
  <div className="text-center p-4 rounded-xl bg-[var(--neu-bg)] neu-inset">
    <p className="text-2xl font-bold text-foreground">
      {paymentData?.amount.toLocaleString('ru-RU')} ₸
    </p>
    <p className="text-xs text-muted-foreground mt-1">
      Подписка {paymentData?.planTarget ?? planLabel}
    </p>
  </div>

  {/* Paylink redirect button */}
  <Button
    className="w-full neu-raised bg-[var(--neu-bg)] text-primary font-semibold"
    onClick={() => {
      if (paymentData?.paylinkUrl) {
        window.open(paymentData.paylinkUrl, '_blank', 'noopener,noreferrer')
        startPolling()  // start polling for webhook confirmation
      }
    }}
    disabled={!paymentData?.paylinkUrl}
  >
    Оплатить через Paylink.kz →
  </Button>

  {/* Polling status */}
  {isPolling && (
    <p className="text-xs text-center text-muted-foreground animate-pulse">
      Ожидаем подтверждения оплаты...
    </p>
  )}

  {/* Countdown */}
  <p className="text-xs text-center text-muted-foreground">
    Ссылка действительна ещё {Math.floor(timeLeft / 60)}:{String(timeLeft % 60).padStart(2, '0')}
  </p>

  <Button variant="ghost" className="w-full text-xs" onClick={() => onOpenChange(false)}>
    Закрыть
  </Button>
</div>
```

**Step 1f — Add polling logic:**
Add a `startPolling` function and `isPolling` state. After the user clicks "Оплатить через Paylink", poll the billing page or a dedicated status endpoint every 5 seconds to check if `planStatus` changed to `ACTIVE`:

```ts
const [isPolling, setIsPolling] = useState(false)
const pollingRef = useRef<ReturnType<typeof setInterval> | null>(null)

function startPolling() {
  if (pollingRef.current) return  // already polling
  setIsPolling(true)
  pollingRef.current = setInterval(async () => {
    // Refresh the page — the billing-content Server Component will re-fetch tenant data
    // If planStatus is ACTIVE, billing-content will show the active plan, not the modal
    router.refresh()
  }, 5000)
}

// Cleanup on unmount
useEffect(() => {
  return () => {
    if (pollingRef.current) clearInterval(pollingRef.current)
  }
}, [])
```

**Step 1g — Update Step 1 ("Initiate") response handling:**
In `handleInitiate()`, update to use `paylinkUrl` instead of `mockQrCode`:
```ts
setPaymentData({
  id: res.paymentId!,
  paylinkUrl: res.paylinkUrl ?? '',  // CHANGED
  amount: res.amount!,
  expiresAt: res.expiresAt!,
})
```

### Task 2 — Update `app/dashboard/settings/billing/billing-content.tsx`

**Step 2a — Update `PendingPaymentInfo` type:**
```ts
type PendingPaymentInfo = {
  id: string
  amount: number
  paylinkUrl: string | null   // CHANGED: was mockQrCode
  expiresAt: string
  planTarget: string
}
```

**Step 2b — Update `TenantInfo` type** — remove Kaspi fields:
```ts
type TenantInfo = {
  // DELETE: kaspiMerchantId: string | null
  // DELETE: kaspiApiKey: string | null
  // ... keep all other fields
}
```

**Step 2c — Remove Kaspi state and save action:**
```ts
// DELETE: const [kaspiMerchantId, setKaspiMerchantId] = useState(tenant.kaspiMerchantId ?? '')
// DELETE: const [kaspiApiKey, setKaspiApiKey] = useState(tenant.kaspiApiKey ?? '')
// DELETE: the saveKaspiSettings action and its call
// DELETE: kaspiMerchantId and kaspiApiKey from the updatePaymentSettings call
```

**Step 2d — Remove the entire Kaspi payment configuration section** from the JSX (lines ~239–280):
```tsx
{/* DELETE this entire block: */}
{/* Kaspi payment configuration — PRO+ only */}
<Card className="...">
  <CardHeader>Kaspi Pay</CardHeader>
  ...Kaspi Merchant ID input...
  ...Kaspi API Key input...
</Card>
```

**Step 2e — Add Paylink.kz info section** (Neumorphic, read-only — shows provider info and link to settings):

```tsx
{/* Paylink.kz — Platform Subscription Provider */}
<Card className="rounded-2xl bg-[var(--neu-bg)] neu-raised border-0">
  <CardHeader className="pb-3">
    <CardTitle className="text-sm font-semibold">Оплата подписки через Paylink.kz</CardTitle>
    <CardDescription>
      Оплата тарифов Omni Book производится через платёжный шлюз Paylink.kz
    </CardDescription>
  </CardHeader>
  <CardContent>
    <p className="text-xs text-muted-foreground">
      При выборе тарифа вы будете перенаправлены на защищённую страницу оплаты Paylink.kz. 
      После успешной оплаты подписка активируется автоматически.
    </p>
  </CardContent>
</Card>
```

**Step 2f — Update `<PaymentModal>` prop** to pass `paylinkUrl` instead of `mockQrCode`:
```tsx
<PaymentModal
  isOpen={isOpen}
  onOpenChange={setIsOpen}
  pendingPayment={pendingPayment ? {
    ...pendingPayment,
    paylinkUrl: pendingPayment.paylinkUrl,  // CHANGED
  } : null}
  planLabel={planLabel}
  planPrice={planPrice}
/>
```

### Task 3 — Update data fetching in billing page server component

Find the file that fetches `pendingPayment` and passes it to `BillingContent` (likely `app/dashboard/settings/billing/page.tsx`). Update the select query to use `paylinkUrl` instead of `mockQrCode`:

```ts
const pendingPayment = await basePrisma.platformPayment.findFirst({
  where: { tenantId, status: 'PENDING', expiresAt: { gt: new Date() } },
  select: {
    id: true,
    amount: true,
    paylinkUrl: true,   // CHANGED: was mockQrCode
    expiresAt: true,
    planTarget: true,
  },
})
```

Also update the select on Tenant to remove `kaspiMerchantId` and `kaspiApiKey`.

### Task 4 — Update i18n translations

In `lib/i18n/translations.ts`, update the `payment` namespace:

**Remove keys** (all 3 locales — RU, KZ, EN):
- `waitingForPayment` (booking-level) — no longer needed
- `waitingInstructions` — no longer needed  
- `paymentCountdown` — no longer needed
- `paymentExpired` — no longer needed
- `paymentExpiredSub` — no longer needed
- `depositRequired` — no longer needed in booking form (keep if referenced elsewhere)
- `depositNotice` — no longer needed in booking form

**Add keys** (all 3 locales):
- `whatsappPrepayment`:
  - RU: `'Написать в WhatsApp для предоплаты'`
  - KZ: `'Алдын ала төлем үшін WhatsApp-қа жазу'`
  - EN: `'Message on WhatsApp for prepayment'`

Before deleting any key, grep to confirm it has no other usages:
```bash
grep -r "t('payment', 'depositRequired')\|t('payment', 'depositNotice')\|t('payment', 'waitingForPayment')" --include="*.tsx" . | grep -v node_modules | grep -v ".planning"
```

### Task 5 — Final TypeScript check and cleanup

Run:
```bash
npx tsc --noEmit 2>&1
```

Fix any remaining type errors introduced by the `mockQrCode` → `paylinkUrl` rename across:
- `app/dashboard/settings/billing/page.tsx`
- `app/dashboard/settings/billing/billing-content.tsx`
- `app/dashboard/settings/billing/payment-modal.tsx`
- `lib/actions/billing.ts`

Also check:
```bash
grep -r "mockQrCode\|mockPaylink\|simulatePayment\|kaspiMerchantId\|kaspiApiKey" --include="*.ts" --include="*.tsx" . | grep -v node_modules | grep -v ".planning" | grep -v ".claude/worktrees"
# Expected: empty
```

---

## Verification

```bash
# 1. No Kaspi config section in billing UI
grep -n "kaspiMerchantId\|kaspiApiKey\|Kaspi Merchant\|Kaspi API Key\|Подключите Kaspi" \
  app/dashboard/settings/billing/billing-content.tsx
# Expected: empty

# 2. Payment modal uses paylinkUrl
grep -n "paylinkUrl\|Paylink" app/dashboard/settings/billing/payment-modal.tsx
# Expected: 3+ matches

# 3. No simulate button
grep -n "simulatePayment\|Simulate\|NEXT_PUBLIC_MOCK_PAYMENTS" \
  app/dashboard/settings/billing/payment-modal.tsx
# Expected: empty

# 4. WhatsApp i18n key present
grep -n "whatsappPrepayment" lib/i18n/translations.ts
# Expected: 3 matches (RU, KZ, EN)

# 5. Full TypeScript clean
npx tsc --noEmit 2>&1 | grep "error TS" | wc -l
# Expected: 0
```

## Commit message
```
feat(12-04): rewrite payment modal for Paylink redirect + billing UI cleanup (remove Kaspi config)
```
