---
phase: 12-remove-kaspi-pay-integrate-paylink-kz-whatsapp-prepayment
plan: "12.1-01"
subsystem: ui-cleanup
tags: [kaspi, deposit, service-form, i18n, typescript, cleanup]

# Dependency graph
requires:
  - phase: 12-remove-kaspi-pay-integrate-paylink-kz-whatsapp-prepayment
    provides: Plans 12-01 through 12-04 core Kaspi removal
provides:
  - service-form.tsx without any Kaspi/deposit UI
  - translations.ts with exactly one payment: block per locale (no TS1117 errors)
  - app/api/mock-payment/ directory fully deleted
affects:
  - components/service-form.tsx
  - lib/i18n/translations.ts
  - app/api/mock-payment/simulate/route.ts (deleted)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Gap closure plan pattern: address verification gaps from VERIFICATION.md after main phase execution"

key-files:
  created: []
  modified:
    - components/service-form.tsx
    - lib/i18n/translations.ts
  deleted:
    - app/api/mock-payment/simulate/route.ts

key-decisions:
  - "service-form.tsx deposit fields removed from FormValues type, not just hidden in JSX — prevents type pollution"
  - "Duplicate payment: blocks removed by deleting early single-key blocks, not merging — full blocks already contain whatsappPrepayment"
  - "mock-payment directory removed entirely via git rm — clean deletion with no orphan files"

requirements-completed:
  - PIV-05
  - PIV-08

# Metrics
duration: 10min
completed: 2026-04-04
---

# Phase 12.1 Plan 01: Gap Closure — Service Form Kaspi Cleanup + i18n Fix + Mock Endpoint Deletion Summary

**3 verification gaps closed: Kaspi deposit UI removed from service-form.tsx, duplicate payment: namespace blocks eliminated (TS1117 fixed), mock-payment simulate endpoint deleted**

## Performance

- **Duration:** ~10 min
- **Completed:** 2026-04-04
- **Tasks:** 3 of 3
- **Files modified:** 2 (1 deleted)

## Accomplishments

- Removed `requireDeposit` state, `depositAmount` FormValues field, deposit settings div block (toggle + input), `depositAmountTiyn` computation, and `requireDeposit`/`depositAmount` from `safeParse` call in `components/service-form.tsx`
- Deleted 3 duplicate single-key `payment:` blocks (one per locale) from `lib/i18n/translations.ts` — TS1117 errors resolved; each locale now has exactly one `payment:` block containing all keys including `whatsappPrepayment`
- Deleted `app/api/mock-payment/simulate/route.ts` and parent directory — dead POST endpoint with no active callers

## Task Commits

1. **Task 1: Remove Kaspi deposit UI from service-form.tsx** - `6467ba2` (feat)
2. **Task 2: Fix duplicate payment: namespace in translations.ts** - `0b1c255` (fix)
3. **Task 3: Delete mock-payment simulate endpoint** - `0f32bc7` (feat)

## Files Modified

- `components/service-form.tsx` — deposit toggle, depositAmount field, Kaspi branding text removed
- `lib/i18n/translations.ts` — 9 lines removed (3 duplicate payment: blocks, 3 per locale)
- DELETED: `app/api/mock-payment/simulate/route.ts`

## Verification Results

```
1. PIV-05: grep "Kaspi|requireDeposit|depositAmount" components/service-form.tsx → 0 matches — PASS
2. i18n TS1117: npx tsc --noEmit | grep TS1117 → 0 errors — PASS
3. PIV-08: test ! -d app/api/mock-payment → PASS: directory deleted
4. payment: blocks: 3 (one per locale) — PASS
5. whatsappPrepayment keys: 3 (one per locale) — PASS
```

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all 3 gaps are fully closed.

## Self-Check: PASSED

- `components/service-form.tsx` — exists, verified 0 deposit/Kaspi references
- `lib/i18n/translations.ts` — exists, 3 payment: blocks, 3 whatsappPrepayment keys
- `app/api/mock-payment/` — confirmed deleted
- Commits verified: 6467ba2, 0b1c255, 0f32bc7
