---
phase: 09-online-payment-with-deposit-via-paylink-kz-kaspi
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - lib/actions/payment-settings.ts
  - app/dashboard/settings/billing/billing-content.tsx
  - lib/i18n/translations.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-08

must_haves:
  truths:
    - "Tenant owner can toggle requireDeposit on/off in billing settings"
    - "Tenant owner can set deposit amount in tenge (converted to tiyn on save)"
    - "Tenant owner can enter kaspiMerchantId and kaspiApiKey"
    - "Deposit settings section only visible for PRO+ tenants"
    - "All new UI strings are translated in RU/EN/KZ"
    - "Deposit settings section uses Neumorphism design patterns"
  artifacts:
    - path: "lib/actions/payment-settings.ts"
      provides: "updatePaymentSettings Server Action"
      exports: ["updatePaymentSettings"]
    - path: "app/dashboard/settings/billing/billing-content.tsx"
      provides: "Deposit configuration section in billing page"
      contains: "depositSectionTitle"
    - path: "lib/i18n/translations.ts"
      provides: "Payment-related i18n keys in RU/EN/KZ"
      contains: "depositRequired"
  key_links:
    - from: "app/dashboard/settings/billing/billing-content.tsx"
      to: "lib/actions/payment-settings.ts"
      via: "updatePaymentSettings import"
      pattern: "import.*updatePaymentSettings"
    - from: "app/dashboard/settings/billing/billing-content.tsx"
      to: "lib/i18n/translations.ts"
      via: "useI18n hook for deposit section labels"
      pattern: "depositSectionTitle|depositRequired"
---

<objective>
Add the tenant deposit configuration UI to the billing settings page: a Server Action for updating payment settings, a new "Deposit / Payment" section with toggle, amount input, and API key fields (PRO+ gated), and i18n keys for all new payment-related strings.

Purpose: Allow tenants to configure deposit requirements so the booking flow can conditionally require payment.
Output: Server Action, extended billing page, i18n translations.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-online-payment-with-deposit-via-paylink-kz-kaspi/09-CONTEXT.md
@.planning/phases/09-online-payment-with-deposit-via-paylink-kz-kaspi/09-RESEARCH.md
@.planning/phases/09-online-payment-with-deposit-via-paylink-kz-kaspi/09-01-SUMMARY.md

<interfaces>
<!-- Billing content component current structure: -->
From app/dashboard/settings/billing/billing-content.tsx:
```typescript
type TenantInfo = {
  plan: string
  planStatus: string
  subscriptionExpiresAt: Date | null
}
export function BillingContent({ tenant }: { tenant: TenantInfo })
```

<!-- Server Action pattern from existing billing.ts: -->
From lib/actions/billing.ts:
```typescript
'use server'
// Uses requireAuth, requireRole, revalidatePath pattern
```

<!-- i18n pattern: -->
From lib/i18n/translations.ts:
```typescript
export const translations: Record<Locale, Record<string, Record<string, string>>> = {
  ru: { booking: { ... }, billing: { ... } },
  kz: { booking: { ... }, billing: { ... } },
  en: { booking: { ... }, billing: { ... } },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create updatePaymentSettings Server Action</name>
  <files>lib/actions/payment-settings.ts</files>
  <read_first>
    - lib/actions/billing.ts
    - prisma/schema.prisma
  </read_first>
  <action>
Create `lib/actions/payment-settings.ts` following the exact pattern from `lib/actions/billing.ts`:

```typescript
'use server'

import { auth } from '@/lib/auth'
import { basePrisma } from '@/lib/db'
import { revalidatePath } from 'next/cache'

export async function updatePaymentSettings(data: {
  requireDeposit: boolean
  depositAmount: number   // tenge (UI value — convert to tiyn here)
  kaspiMerchantId?: string
  kaspiApiKey?: string
}) {
  const session = await auth()
  if (!session?.user?.tenantId) {
    return { error: 'Unauthorized' }
  }
  const tenantId = session.user.tenantId

  // Gate: PRO+ only (per D-07d)
  const tenant = await basePrisma.tenant.findUnique({
    where: { id: tenantId },
    select: { plan: true },
  })
  if (!tenant || tenant.plan === 'FREE') {
    return { error: 'Требуется план PRO' }
  }

  await basePrisma.tenant.update({
    where: { id: tenantId },
    data: {
      requireDeposit: data.requireDeposit,
      depositAmount: Math.round(data.depositAmount * 100),  // tenge -> tiyn
      kaspiMerchantId: data.kaspiMerchantId?.trim() || null,
      kaspiApiKey: data.kaspiApiKey?.trim() || null,
    },
  })

  revalidatePath('/dashboard/settings/billing')
  return { success: true }
}
```

IMPORTANT: Check how `auth()` is imported in existing actions (e.g., `lib/actions/billing.ts`). The import path may be `@/lib/auth` or `@/auth` or use `getServerSession`. Match the exact pattern used in the project.

IMPORTANT: `depositAmount` is in tenge from the UI, converted to tiyn (multiply by 100) before saving. This matches the project pattern where `service.price` is stored in tiyn.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && test -f lib/actions/payment-settings.ts && grep -c "updatePaymentSettings" lib/actions/payment-settings.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/actions/payment-settings.ts exists
    - lib/actions/payment-settings.ts contains `'use server'`
    - lib/actions/payment-settings.ts contains `export async function updatePaymentSettings`
    - lib/actions/payment-settings.ts contains `tenant.plan === 'FREE'` (PRO+ gate)
    - lib/actions/payment-settings.ts contains `Math.round(data.depositAmount * 100)` (tenge to tiyn)
    - lib/actions/payment-settings.ts contains `revalidatePath('/dashboard/settings/billing')`
    - lib/actions/payment-settings.ts contains `requireDeposit: data.requireDeposit`
    - lib/actions/payment-settings.ts contains `kaspiMerchantId`
    - lib/actions/payment-settings.ts contains `kaspiApiKey`
  </acceptance_criteria>
  <done>Server Action created with PRO+ gate, tenge-to-tiyn conversion, and path revalidation.</done>
</task>

<task type="auto">
  <name>Task 2: Add deposit section to billing page + i18n keys</name>
  <files>app/dashboard/settings/billing/billing-content.tsx, lib/i18n/translations.ts</files>
  <read_first>
    - app/dashboard/settings/billing/billing-content.tsx
    - lib/i18n/translations.ts
    - lib/actions/payment-settings.ts
  </read_first>
  <action>
**A. Extend `TenantInfo` type in billing-content.tsx:**

Add new fields to the existing `TenantInfo` type:
```typescript
type TenantInfo = {
  plan: string
  planStatus: string
  subscriptionExpiresAt: Date | null
  // Phase 9: deposit config
  requireDeposit: boolean
  depositAmount: number    // stored in tiyn, display in tenge
  kaspiMerchantId: string | null
  kaspiApiKey: string | null
}
```

**B. Add import for the new Server Action:**
```typescript
import { updatePaymentSettings } from '@/lib/actions/payment-settings'
```

**C. Add deposit state hooks inside `BillingContent` function:**
```typescript
const [requireDeposit, setRequireDeposit] = useState(tenant.requireDeposit)
const [depositAmountTenge, setDepositAmountTenge] = useState(tenant.depositAmount / 100)  // tiyn -> tenge for display
const [kaspiMerchantId, setKaspiMerchantId] = useState(tenant.kaspiMerchantId ?? '')
const [kaspiApiKey, setKaspiApiKey] = useState(tenant.kaspiApiKey ?? '')
const [savingDeposit, setSavingDeposit] = useState(false)
```

**D. Add `handleSaveDeposit` function:**
```typescript
async function handleSaveDeposit() {
  setSavingDeposit(true)
  const res = await updatePaymentSettings({
    requireDeposit,
    depositAmount: depositAmountTenge,  // tenge — Server Action converts to tiyn
    kaspiMerchantId: kaspiMerchantId || undefined,
    kaspiApiKey: kaspiApiKey || undefined,
  })
  setSavingDeposit(false)
  if (res?.error) {
    toast.error(res.error)
    return
  }
  toast.success('Настройки депозита сохранены')
  router.refresh()
}
```

**E. Add deposit section JSX AFTER the existing billing content, BEFORE the closing fragment/div:**

Only render if tenant is PRO+ (`isPro` already computed in the component):

```jsx
{isPro && (
  <Card className="bg-[var(--neu-bg)] neu-raised">
    <CardHeader>
      <CardTitle>Оплата / Депозит</CardTitle>
      <CardDescription>Настройте обязательную предоплату при онлайн-записи</CardDescription>
    </CardHeader>
    <CardContent className="space-y-4">
      <div className="flex items-center gap-3">
        <input
          type="checkbox"
          id="requireDeposit"
          checked={requireDeposit}
          onChange={(e) => setRequireDeposit(e.target.checked)}
          className="h-4 w-4 rounded"
        />
        <label htmlFor="requireDeposit" className="text-sm font-medium">
          Требовать депозит при онлайн-записи
        </label>
      </div>

      {requireDeposit && (
        <>
          <div className="space-y-2">
            <label className="text-sm font-medium">Размер депозита (тенге)</label>
            <input
              type="number"
              min={0}
              value={depositAmountTenge}
              onChange={(e) => setDepositAmountTenge(Number(e.target.value))}
              className="w-full rounded-xl bg-[var(--neu-bg)] neu-inset px-3 py-2 text-sm"
              placeholder="1000"
            />
          </div>

          <div className="space-y-2">
            <label className="text-sm font-medium">Kaspi Merchant ID</label>
            <input
              type="text"
              value={kaspiMerchantId}
              onChange={(e) => setKaspiMerchantId(e.target.value)}
              className="w-full rounded-xl bg-[var(--neu-bg)] neu-inset px-3 py-2 text-sm"
              placeholder="Merchant ID"
            />
          </div>

          <div className="space-y-2">
            <label className="text-sm font-medium">Kaspi API Key</label>
            <input
              type="password"
              value={kaspiApiKey}
              onChange={(e) => setKaspiApiKey(e.target.value)}
              className="w-full rounded-xl bg-[var(--neu-bg)] neu-inset px-3 py-2 text-sm"
              placeholder="API Key"
            />
          </div>

          <Button
            onClick={handleSaveDeposit}
            disabled={savingDeposit}
            className="neu-raised"
          >
            {savingDeposit ? 'Сохранение...' : 'Сохранить настройки депозита'}
          </Button>
        </>
      )}
    </CardContent>
  </Card>
)}
```

NOTE: Use Neumorphism classes throughout: `bg-[var(--neu-bg)]`, `neu-raised` on Card, `neu-inset` on inputs (per PAY-08).

**F. Update the billing settings page (the parent server component)** that renders `<BillingContent>` to pass the new tenant fields. Find the page that imports and renders `BillingContent`, and extend the tenant query to include `requireDeposit`, `depositAmount`, `kaspiMerchantId`, `kaspiApiKey`. The page is likely at `app/dashboard/settings/billing/page.tsx`.

**G. Add i18n keys to `lib/i18n/translations.ts`:**

Add to the `billing` section of each locale. If no `billing` section exists, add within the `booking` section or create a `payment` section following the existing nesting pattern.

**RU (in `ru` locale object, billing or payment section):**
```typescript
depositSectionTitle: 'Оплата / Депозит',
requireDepositLabel: 'Требовать депозит при онлайн-записи',
depositAmountLabel: 'Размер депозита (тенге)',
kaspiMerchantIdLabel: 'Kaspi Merchant ID',
kaspiApiKeyLabel: 'Kaspi API Key',
depositProRequired: 'Настройка депозита доступна только на тарифе PRO.',
depositSaved: 'Настройки депозита сохранены',
savingDeposit: 'Сохранение...',
saveDepositSettings: 'Сохранить настройки депозита',
depositRequired: 'Требуется депозит',
depositAmount: 'Сумма депозита',
depositNotice: 'Для подтверждения записи необходима оплата депозита.',
waitingForPayment: 'Ожидание оплаты',
waitingInstructions: 'Мы выставили счет в ваше приложение Kaspi. Оплатите в течение 10 минут.',
paymentCountdown: 'Осталось времени:',
paymentExpired: 'Время ожидания оплаты истекло. Запись отменена.',
paymentExpiredSub: 'Запись автоматически отменена. Вы можете записаться снова.',
```

**EN (in `en` locale object, same section):**
```typescript
depositSectionTitle: 'Payment / Deposit',
requireDepositLabel: 'Require deposit for online bookings',
depositAmountLabel: 'Deposit amount (tenge)',
kaspiMerchantIdLabel: 'Kaspi Merchant ID',
kaspiApiKeyLabel: 'Kaspi API Key',
depositProRequired: 'Deposit settings are available on PRO plan only.',
depositSaved: 'Deposit settings saved',
savingDeposit: 'Saving...',
saveDepositSettings: 'Save deposit settings',
depositRequired: 'Deposit required',
depositAmount: 'Deposit amount',
depositNotice: 'A deposit payment is required to confirm your booking.',
waitingForPayment: 'Waiting for payment',
waitingInstructions: 'We sent an invoice to your Kaspi app. Pay within 10 minutes.',
paymentCountdown: 'Time remaining:',
paymentExpired: 'Payment time expired. Booking cancelled.',
paymentExpiredSub: 'Your booking was automatically cancelled. You can book again.',
```

**KZ (in `kz` locale object, same section):**
```typescript
depositSectionTitle: 'Төлем / Депозит',
requireDepositLabel: 'Онлайн жазылу кезінде депозит талап ету',
depositAmountLabel: 'Депозит сомасы (теңге)',
kaspiMerchantIdLabel: 'Kaspi Merchant ID',
kaspiApiKeyLabel: 'Kaspi API Key',
depositProRequired: 'Депозит баптаулары тек PRO тарифінде қолжетімді.',
depositSaved: 'Депозит баптаулары сақталды',
savingDeposit: 'Сақтау...',
saveDepositSettings: 'Депозит баптауларын сақтау',
depositRequired: 'Депозит қажет',
depositAmount: 'Депозит сомасы',
depositNotice: 'Жазбаны растау үшін депозит төлемі қажет.',
waitingForPayment: 'Төлемді күту',
waitingInstructions: 'Kaspi қосымшаңызға шот жібердік. 10 минут ішінде төлеңіз.',
paymentCountdown: 'Қалған уақыт:',
paymentExpired: 'Төлем уақыты аяқталды. Жазба жойылды.',
paymentExpiredSub: 'Жазбаңыз автоматты түрде жойылды. Қайта жазыла аласыз.',
```
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && grep -c "depositSectionTitle" lib/i18n/translations.ts && grep -c "updatePaymentSettings" app/dashboard/settings/billing/billing-content.tsx && grep -c "neu-inset" app/dashboard/settings/billing/billing-content.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/dashboard/settings/billing/billing-content.tsx contains `import { updatePaymentSettings }`
    - app/dashboard/settings/billing/billing-content.tsx contains `requireDeposit` state
    - app/dashboard/settings/billing/billing-content.tsx contains `depositAmountTenge` state
    - app/dashboard/settings/billing/billing-content.tsx contains `kaspiMerchantId` state
    - app/dashboard/settings/billing/billing-content.tsx contains `kaspiApiKey` state
    - app/dashboard/settings/billing/billing-content.tsx contains `neu-raised` (Neumorphism Card)
    - app/dashboard/settings/billing/billing-content.tsx contains `neu-inset` (Neumorphism inputs)
    - app/dashboard/settings/billing/billing-content.tsx contains `bg-[var(--neu-bg)]`
    - app/dashboard/settings/billing/billing-content.tsx contains `isPro` conditional rendering
    - lib/i18n/translations.ts contains `depositSectionTitle` in all three locale sections (count >= 3)
    - lib/i18n/translations.ts contains `waitingForPayment` in all three locale sections
    - lib/i18n/translations.ts contains `paymentExpired` in all three locale sections
  </acceptance_criteria>
  <done>Billing page has deposit configuration section (PRO+ gated) with Neumorphism design. All i18n keys added in RU/EN/KZ for deposit settings and booking payment flow.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- Deposit section renders only for PRO+ tenants (verify `isPro` gate in code)
- All i18n keys present in all 3 locales
</verification>

<success_criteria>
- Server Action validates PRO+ gate and converts tenge to tiyn
- Billing page shows deposit toggle, amount, and API key fields for PRO tenants
- All new UI uses Neumorphism classes (neu-raised, neu-inset, var(--neu-bg))
- 16 i18n keys added per locale (48 total across RU/EN/KZ)
</success_criteria>

<output>
After completion, create `.planning/phases/09-online-payment-with-deposit-via-paylink-kz-kaspi/09-03-SUMMARY.md`
</output>
