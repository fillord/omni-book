---
phase: 03-dashboard-auth-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/dashboard-sidebar.tsx
  - app/dashboard/settings/billing/billing-content.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-05

must_haves:
  truths:
    - "The dashboard sidebar renders as a visually distinct surface from main content area in dark mode (uses bg-sidebar family tokens)"
    - "billing-content.tsx has zero dark:! force-override classes remaining"
    - "billing-content.tsx has zero bare bg-zinc-*, text-zinc-*, border-zinc-* neutral classes remaining"
    - "The admin panel link in sidebar uses sidebar-accent tokens instead of bg-zinc-900 text-white"
  artifacts:
    - path: "components/dashboard-sidebar.tsx"
      provides: "Sidebar with full sidebar token family"
      contains: "bg-sidebar"
    - path: "app/dashboard/settings/billing/billing-content.tsx"
      provides: "Billing page with no dark:! overrides and no zinc neutral classes"
  key_links:
    - from: "components/dashboard-sidebar.tsx"
      to: "bg-sidebar (CSS variable)"
      via: "Tailwind class on root div and footer div"
      pattern: "bg-sidebar"
    - from: "app/dashboard/settings/billing/billing-content.tsx"
      to: "semantic tokens (bg-muted, text-foreground, border-border)"
      via: "direct Tailwind class replacement"
      pattern: "bg-muted|text-foreground|border-border"
---

<objective>
Remediate `dashboard-sidebar.tsx` (DASH-01, DASH-05) and `billing-content.tsx` (DASH-02). These are the two files with structural dark mode violations: the sidebar uses `bg-background` instead of the sidebar token family, and billing uses 16 `dark:!` force overrides plus additional `dark:` dual-pairs.

Purpose: After this plan, the two most visually critical dashboard surfaces render correctly in dark mode without specificity hacks.
Output: Updated sidebar and billing files with full semantic token coverage.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-auth-surface/03-RESEARCH.md
@.planning/phases/03-dashboard-auth-surface/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Sidebar token family (from globals.css @theme inline bridge — Phase 0 validated) -->
<!--
  bg-sidebar           → near-white blue-tinted (light) / very dark blue (dark)
  text-sidebar-foreground
  border-sidebar-border
  bg-sidebar-accent    → soft blue-tinted (light) / dark accent (dark)
  text-sidebar-accent-foreground
  text-sidebar-primary → indigo (light) / lighter indigo (dark)
-->

<!-- Violation inventory from RESEARCH.md -->
<!-- dashboard-sidebar.tsx:
  Line 86:  bg-background border-r border-border  → bg-sidebar border-r border-sidebar-border
  Line 141: bg-background border-t border-border  → bg-sidebar border-t border-sidebar-border
  Line 164: text-white bg-zinc-900 hover:bg-zinc-800  → text-sidebar-accent-foreground bg-sidebar-accent hover:bg-sidebar-accent/80
  (Line 219 bg-background/90 on mobile toggle button — intentional translucent overlay, preserve as-is)
-->

<!-- billing-content.tsx full replacement table:
  Line 60:   border-indigo-100 dark:border-indigo-900/50 bg-indigo-50/30 dark:bg-indigo-950/20  → keep (intentional brand pair, no !)
  Line 64:   text-indigo-600/80 dark:text-indigo-400                                           → keep (intentional brand)
  Line 66:   text-zinc-900 dark:text-white                                                     → text-foreground
  Line 70:   bg-amber-100 text-amber-800 dark:bg-amber-900/40 dark:text-amber-400              → keep (status color, intentional)
  Line 76:   bg-emerald-100 text-emerald-800 dark:bg-emerald-900/40 dark:text-emerald-400      → keep (status color, intentional)
  Line 82:   bg-orange-100 text-orange-700 dark:bg-orange-900/40 dark:text-orange-400          → keep (status color, intentional)
  Line 88:   bg-red-100 text-red-700 dark:bg-red-900/40 dark:text-red-400                      → keep (status color, intentional)
  Line 101:  border-zinc-200 dark:border-zinc-800                                              → border-border
  Line 107:  dark:text-white (CardTitle)                                                       → remove dark:text-white (CardTitle inherits text-card-foreground)
  Line 118:  bg-zinc-50 dark:!bg-zinc-900 border-zinc-200 dark:!border-zinc-700               → bg-muted border-border
  Line 120:  text-zinc-900 dark:!text-white                                                    → text-foreground
  Line 121:  text-zinc-500 dark:!text-zinc-400                                                 → text-muted-foreground
  Line 133:  text-zinc-700 dark:!text-zinc-300                                                 → text-foreground
  Line 134:  text-indigo-600 dark:!text-indigo-400                                             → text-indigo-600 dark:text-indigo-400 (keep pair, remove ! only)
  Line 148:  dark:bg-zinc-950 dark:border-zinc-800 (DialogContent)                            → remove both (DialogContent is bg-background by default)
  Line 150:  dark:text-white (DialogTitle)                                                     → remove (inherits text-foreground)
  Line 151:  dark:text-zinc-400 (DialogDescription)                                           → text-muted-foreground (on the element itself)
  Line 158:  bg-zinc-50 dark:!bg-zinc-900 border-zinc-200 dark:!border-zinc-800               → bg-muted border-border
  Line 159:  text-zinc-500 dark:!text-zinc-400                                                 → text-muted-foreground
  Line 160:  text-zinc-900 dark:!text-white                                                    → text-foreground
  Line 165:  bg-red-100 dark:!bg-red-900/40                                                   → bg-red-100 dark:bg-red-900/40 (remove ! only)
  Line 166:  text-red-600 dark:!text-red-400                                                   → text-red-600 dark:text-red-400 (remove ! only)
  Line 169:  text-zinc-900 dark:!text-white                                                    → text-foreground
  Line 172:  text-zinc-600 dark:!text-zinc-300                                                 → text-foreground
  Line 175:  text-indigo-600 dark:!text-indigo-400                                             → text-indigo-600 dark:text-indigo-400 (remove ! only)
  Line 178:  text-zinc-500 dark:!text-zinc-400                                                 → text-muted-foreground
  Line 186:  bg-indigo-50 dark:!bg-indigo-950/80 border-indigo-100 dark:!border-indigo-800 text-indigo-800 dark:!text-indigo-200 → bg-indigo-50 dark:bg-indigo-950/80 border-indigo-100 dark:border-indigo-800 text-indigo-800 dark:text-indigo-200 (remove ! on all)
  Line 187:  dark:!text-indigo-400                                                             → dark:text-indigo-400 (remove ! only)
  Line 197:  dark:text-zinc-300 dark:hover:bg-zinc-800 dark:hover:text-white                  → text-muted-foreground hover:bg-muted (remove dark: prefix — single token)
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remediate dashboard-sidebar.tsx (DASH-01 + DASH-05)</name>
  <files>components/dashboard-sidebar.tsx</files>
  <read_first>
    - components/dashboard-sidebar.tsx (current state — read in full before making any changes)
    - .planning/phases/03-dashboard-auth-surface/03-RESEARCH.md (Pattern 3: Sidebar Token Migration, violation inventory lines 86, 141, 164)
  </read_first>
  <action>
Make exactly three class string replacements in `components/dashboard-sidebar.tsx`:

**Replacement 1 — Root div (line ~86):**
Find the div that contains `flex flex-col h-full bg-background border-r border-border` and replace the color-related classes:
- `bg-background` → `bg-sidebar`
- `border-border` → `border-sidebar-border`
Result: `flex flex-col h-full bg-sidebar border-r border-sidebar-border`

**Replacement 2 — Footer section (line ~141):**
Find the div that contains `mt-auto` and also has `bg-background border-t border-border` and replace:
- `bg-background` → `bg-sidebar`
- `border-border` → `border-sidebar-border`
Result: the mt-auto div uses `bg-sidebar border-t border-sidebar-border`

**Replacement 3 — Admin link (line ~164):**
Find the Link element with `text-white bg-zinc-900 hover:bg-zinc-800` and replace those three classes:
- `text-white` → `text-sidebar-accent-foreground`
- `bg-zinc-900` → `bg-sidebar-accent`
- `hover:bg-zinc-800` → `hover:bg-sidebar-accent/80`
Result: `text-sidebar-accent-foreground bg-sidebar-accent hover:bg-sidebar-accent/80`

**DO NOT touch:**
- Line ~219: `bg-background/90` on the mobile toggle button (translucent overlay, not a sidebar surface)
- `ACTIVE_LINK` color map — niche identity colors, intentional (same pattern as BOOK-04)
- `BADGE_CLS` color map — niche badge colors, intentional
- Any other non-color classes (flex, gap, px, py, text-sm, font-semibold, etc.)
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="DASH-01|DASH-05" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bg-sidebar" components/dashboard-sidebar.tsx` returns at least 2 matches (root + footer)
    - `grep "border-sidebar-border" components/dashboard-sidebar.tsx` returns at least 2 matches
    - `grep "bg-sidebar-accent" components/dashboard-sidebar.tsx` returns at least 1 match (admin link)
    - `grep "bg-zinc-900" components/dashboard-sidebar.tsx` returns no matches
    - `grep "text-white bg-zinc-900\|bg-zinc-900.*text-white" components/dashboard-sidebar.tsx` returns no matches
    - `grep "flex flex-col h-full bg-background" components/dashboard-sidebar.tsx` returns no matches
    - DASH-01 and DASH-05 test assertions pass GREEN in `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage`
  </acceptance_criteria>
  <done>dashboard-sidebar.tsx sidebar root div, footer div, and admin link all use sidebar token family. DASH-01 and DASH-05 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Remediate billing-content.tsx (DASH-02)</name>
  <files>app/dashboard/settings/billing/billing-content.tsx</files>
  <read_first>
    - app/dashboard/settings/billing/billing-content.tsx (current state — read in full before making any changes)
    - .planning/phases/03-dashboard-auth-surface/03-RESEARCH.md (Pattern 1: Billing dark:! Specificity Conflict, full replacement table)
  </read_first>
  <action>
Apply the following replacements to `app/dashboard/settings/billing/billing-content.tsx`. Work line by line using the violation inventory from the interfaces section above. Key rules:

**Rule 1 — Force override stripping (dark:! pairs → single semantic token):**
For each `base-class dark:!override` pair where the base is a neutral zinc/slate/gray:
- Remove BOTH the base class AND the dark:! override
- Replace with a single semantic token

Specific replacements for zinc neutral classes:
- `text-zinc-900 dark:!text-white` → `text-foreground`
- `text-zinc-500 dark:!text-zinc-400` → `text-muted-foreground`
- `text-zinc-700 dark:!text-zinc-300` → `text-foreground`
- `text-zinc-600 dark:!text-zinc-300` → `text-foreground`
- `bg-zinc-50 dark:!bg-zinc-900` → `bg-muted`
- `border-zinc-200 dark:!border-zinc-700` → `border-border`
- `border-zinc-200 dark:!border-zinc-800` → `border-border`

**Rule 2 — Non-force dark: zinc pairs (also violations):**
- `border-zinc-200 dark:border-zinc-800` (line 101, Card) → `border-border`
- `dark:bg-zinc-950 dark:border-zinc-800` (line 148, DialogContent) → remove both (DialogContent uses bg-background by default, no override needed)
- `dark:text-white` alone on CardTitle (line 107) → remove (CardTitle inherits text-card-foreground)
- `dark:text-zinc-400` alone on DialogDescription (line 151) → replace element className with `text-muted-foreground` (no dark: needed)
- `dark:text-zinc-300 dark:hover:bg-zinc-800 dark:hover:text-white` (line 197, cancel button) → `text-muted-foreground hover:bg-muted` (no dark: needed)

**Rule 3 — Brand/status pairs: remove ! prefix ONLY (keep the dual-pair):**
- `text-indigo-600 dark:!text-indigo-400` → `text-indigo-600 dark:text-indigo-400`
- `bg-red-100 dark:!bg-red-900/40` → `bg-red-100 dark:bg-red-900/40`
- `text-red-600 dark:!text-red-400` → `text-red-600 dark:text-red-400`
- `bg-indigo-50 dark:!bg-indigo-950/80` → `bg-indigo-50 dark:bg-indigo-950/80`
- `border-indigo-100 dark:!border-indigo-800` → `border-indigo-100 dark:border-indigo-800`
- `text-indigo-800 dark:!text-indigo-200` → `text-indigo-800 dark:text-indigo-200`
- `dark:!text-indigo-400` on `<strong>` → `dark:text-indigo-400`

**Rule 4 — Preserve these untouched (intentional status color dual-pairs, no !):**
- `bg-amber-100 text-amber-800 dark:bg-amber-900/40 dark:text-amber-400` (lines 70)
- `bg-emerald-100 text-emerald-800 dark:bg-emerald-900/40 dark:text-emerald-400` (line 76)
- `bg-orange-100 text-orange-700 dark:bg-orange-900/40 dark:text-orange-400` (line 82)
- `bg-red-100 text-red-700 dark:bg-red-900/40 dark:text-red-400` (line 88)
- `border-indigo-100 dark:border-indigo-900/50 bg-indigo-50/30 dark:bg-indigo-950/20` (line 60, plan card)
- `text-indigo-600/80 dark:text-indigo-400` (line 64, plan label)
- `bg-indigo-600 text-white hover:bg-indigo-700` (line 143, subscribe button) — intentional brand CTA
- `bg-emerald-600 hover:bg-emerald-700 text-white` (line 205, confirm button) — intentional brand CTA

After all replacements, verify no `dark:!` remains and no bare `bg-zinc-*`, `text-zinc-*`, `border-zinc-*` remains.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="DASH-02" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dark:!" app/dashboard/settings/billing/billing-content.tsx` returns 0
    - `grep -c "bg-zinc-" app/dashboard/settings/billing/billing-content.tsx` returns 0
    - `grep -c "text-zinc-" app/dashboard/settings/billing/billing-content.tsx` returns 0
    - `grep -c "border-zinc-" app/dashboard/settings/billing/billing-content.tsx` returns 0
    - File still contains `bg-indigo-50` (brand indigo block preserved)
    - File still contains `bg-red-100` (status red block preserved)
    - File still contains `bg-amber-100` (status amber block preserved)
    - DASH-02 test assertions pass GREEN in `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage`
  </acceptance_criteria>
  <done>billing-content.tsx has zero dark:! force overrides. Zero bare zinc neutral classes. Brand/status dual-pairs preserved without !. DASH-02 tests GREEN.</done>
</task>

</tasks>

<verification>
`npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage` — DASH-01, DASH-02, and DASH-05 describe blocks all GREEN.
`npx jest --no-coverage` — full suite still passes (no regressions).
</verification>

<success_criteria>
DASH-01: sidebar root and footer use `bg-sidebar border-sidebar-border`. Admin link uses `bg-sidebar-accent text-sidebar-accent-foreground`.
DASH-02: zero `dark:!` in billing-content.tsx. Zero bare zinc neutral classes. Status/brand dual-pairs preserved (without !).
DASH-05: `bg-zinc-900 text-white` removed from sidebar admin link.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-auth-surface/03-02-SUMMARY.md`
</output>
