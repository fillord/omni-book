---
phase: 03-dashboard-auth-surface
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/analytics-dashboard.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "analytics-dashboard.tsx CartesianGrid grid lines are visible in dark mode (use CSS variable, not hardcoded #f4f4f5)"
    - "Tooltip cursor highlight is visible in dark mode (uses var(--color-muted), not #f4f4f5)"
    - "Axis tick labels are readable in dark mode (use var(--color-muted-foreground), not #a1a1aa)"
    - "Tooltip border is visible in dark mode (uses var(--color-border), not #e4e4e7)"
    - "EmptyState text is readable in dark mode (uses text-muted-foreground, not text-zinc-400)"
    - "staff-manager.tsx, services-manager.tsx, resources-manager.tsx confirmed clean — no neutral violations"
  artifacts:
    - path: "components/analytics-dashboard.tsx"
      provides: "Analytics charts with CSS variable Recharts props"
      contains: "var(--color-border)"
  key_links:
    - from: "components/analytics-dashboard.tsx"
      to: "var(--color-border)"
      via: "CartesianGrid stroke prop (string)"
      pattern: "stroke=\"var\\(--color-border\\)\""
    - from: "components/analytics-dashboard.tsx"
      to: "var(--color-muted)"
      via: "Tooltip cursor fill prop"
      pattern: "fill: 'var\\(--color-muted\\)'"
---

<objective>
Remediate `analytics-dashboard.tsx` (DASH-03): replace hardcoded hex values in Recharts SVG props with CSS variable strings, and replace `text-zinc-400` with `text-muted-foreground` in EmptyState. Audit and document that manager components are already clean (DASH-04).

Purpose: Chart elements (grid lines, cursor, axis ticks, tooltip borders) become visible in dark mode when the CSS theme variables are respected.
Output: Updated analytics-dashboard.tsx with all Recharts inline props using CSS variable references. DASH-04 confirmed clean via test pass (no source changes needed).
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-auth-surface/03-RESEARCH.md
@.planning/phases/03-dashboard-auth-surface/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Recharts CSS variable approach (from RESEARCH.md Pattern 2) -->
<!-- Recharts SVG props accept CSS variable strings directly. No wrapper component needed. -->
<!--
  stroke prop (string):  "#f4f4f5"   →  "var(--color-border)"
  fill prop (string):    "#f4f4f5"   →  "var(--color-muted)" (for background highlight areas like cursor)
  fill in tick object:   '#a1a1aa'   →  'var(--color-muted-foreground)'
  fill in tick object:   '#52525b'   →  'var(--color-muted-foreground)'
  contentStyle.border:   '#e4e4e7'   →  'var(--color-border)'
  RevenueChart cursor:   stroke: '#e4e4e7'  → stroke: 'var(--color-border)'
-->

<!-- Complete violation inventory from grep of analytics-dashboard.tsx -->
<!--
  Line 87:   className="... text-zinc-400"                              → text-muted-foreground
  Line 226:  stroke="#f4f4f5"                                           → stroke="var(--color-border)"
  Line 229:  tick={{ fontSize: 11, fill: '#a1a1aa' }}                   → tick={{ fontSize: 11, fill: 'var(--color-muted-foreground)' }}
  Line 235:  tick={{ fontSize: 11, fill: '#a1a1aa' }}                   → tick={{ fontSize: 11, fill: 'var(--color-muted-foreground)' }}
  Line 241:  cursor={{ fill: '#f4f4f5' }}                               → cursor={{ fill: 'var(--color-muted)' }}
  Line 269:  stroke="#f4f4f5"                                           → stroke="var(--color-border)"
  Line 272:  tick={{ fontSize: 11, fill: '#a1a1aa' }}                   → tick={{ fontSize: 11, fill: 'var(--color-muted-foreground)' }}
  Line 278:  tick={{ fontSize: 11, fill: '#a1a1aa' }}                   → tick={{ fontSize: 11, fill: 'var(--color-muted-foreground)' }}
  Line 284:  cursor={{ stroke: '#e4e4e7', strokeWidth: 1 }}             → cursor={{ stroke: 'var(--color-border)', strokeWidth: 1 }}
  Line 320:  stroke="#f4f4f5"                                           → stroke="var(--color-border)"
  Line 323:  tick={{ fontSize: 11, fill: '#a1a1aa' }}                   → tick={{ fontSize: 11, fill: 'var(--color-muted-foreground)' }}
  Line 331:  tick={{ fontSize: 12, fill: '#52525b' }}                   → tick={{ fontSize: 12, fill: 'var(--color-muted-foreground)' }}
  Line 337:  cursor={{ fill: '#f4f4f5' }}                               → cursor={{ fill: 'var(--color-muted)' }}
  Line 339:  contentStyle={{ borderRadius: 12, border: '1px solid #e4e4e7', fontSize: 13 }}  → contentStyle={{ borderRadius: 12, border: '1px solid var(--color-border)', fontSize: 13 }}
  Line 374:  contentStyle={{ borderRadius: 12, border: '1px solid #e4e4e7', fontSize: 13 }}  → contentStyle={{ borderRadius: 12, border: '1px solid var(--color-border)', fontSize: 13 }}
-->

<!-- DO NOT CHANGE (intentional brand/data-viz colors per REQUIREMENTS.md Out of Scope): -->
<!--
  PIE_COLORS array hex values
  fill="#22c55e" (confirmed bookings bar — green brand)
  fill="#ef4444" (cancelled bookings bar — red brand)
  fill={nicheColor} (niche brand color — dynamic)
  fill="url(#revenueGrad)" (gradient fill — intentional)
  NICHE_COLOR hex values
  SummaryCard iconBg props: "bg-blue-50 text-blue-600" etc. — functional metric color coding
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded hex values in analytics-dashboard.tsx (DASH-03)</name>
  <files>components/analytics-dashboard.tsx</files>
  <read_first>
    - components/analytics-dashboard.tsx (current state — read in full before making any changes)
    - .planning/phases/03-dashboard-auth-surface/03-RESEARCH.md (Pattern 2: Recharts CSS Variable Props, Pattern 3 pitfalls)
  </read_first>
  <action>
Make the following targeted replacements in `components/analytics-dashboard.tsx`. Apply every replacement in the violation inventory from the interfaces section above.

**Step 1 — Tailwind class replacement (1 occurrence):**
Line ~87: In the EmptyState div, change `text-zinc-400` to `text-muted-foreground`.

**Step 2 — CartesianGrid stroke replacements (3 occurrences):**
Every `stroke="#f4f4f5"` on a CartesianGrid → `stroke="var(--color-border)"`

**Step 3 — Axis tick fill replacements (multiple occurrences):**
Every `tick={{ fontSize: 11, fill: '#a1a1aa' }}` → `tick={{ fontSize: 11, fill: 'var(--color-muted-foreground)' }}`
Every `tick={{ fontSize: 12, fill: '#52525b' }}` → `tick={{ fontSize: 12, fill: 'var(--color-muted-foreground)' }}`

**Step 4 — Tooltip cursor replacements:**
Every `cursor={{ fill: '#f4f4f5' }}` → `cursor={{ fill: 'var(--color-muted)' }}`
The revenue chart area: `cursor={{ stroke: '#e4e4e7', strokeWidth: 1 }}` → `cursor={{ stroke: 'var(--color-border)', strokeWidth: 1 }}`

**Step 5 — contentStyle border replacements (2 occurrences):**
Every `contentStyle={{ borderRadius: 12, border: '1px solid #e4e4e7', fontSize: 13 }}` →
`contentStyle={{ borderRadius: 12, border: '1px solid var(--color-border)', fontSize: 13 }}`

**DO NOT change:**
- `fill="#22c55e"`, `fill="#ef4444"` (bar chart brand colors)
- `fill={nicheColor}` (niche brand color variable)
- `fill="url(#revenueGrad)"` (gradient fill reference)
- `PIE_COLORS` array values
- `NICHE_COLOR` constant
- `iconBg` props on SummaryCard components (blue/green/purple/red metric colors)
- Any layout, sizing, or non-color classes
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="DASH-03" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#f4f4f5' components/analytics-dashboard.tsx` returns 0
    - `grep -c '#a1a1aa' components/analytics-dashboard.tsx` returns 0
    - `grep -c '#52525b' components/analytics-dashboard.tsx` returns 0
    - `grep -c '#e4e4e7' components/analytics-dashboard.tsx` returns 0
    - `grep -c 'text-zinc-400' components/analytics-dashboard.tsx` returns 0
    - `grep "var(--color-border)" components/analytics-dashboard.tsx` returns at least 4 matches (3 CartesianGrid + 2 contentStyle + 1 cursor)
    - `grep "var(--color-muted-foreground)" components/analytics-dashboard.tsx` returns at least 5 matches (axis ticks)
    - `grep "#22c55e" components/analytics-dashboard.tsx` still returns 1 (preserved)
    - DASH-03 test assertions pass GREEN in `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage`
  </acceptance_criteria>
  <done>All Recharts SVG hex values replaced with CSS variable strings. EmptyState text uses text-muted-foreground. Brand bar colors and PIE_COLORS preserved. DASH-03 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm manager components are clean (DASH-04)</name>
  <files></files>
  <read_first>
    - components/staff-manager.tsx (audit read — confirm no violations before asserting clean)
    - components/services-manager.tsx (audit read — confirm no violations)
    - components/resources-manager.tsx (audit read — confirm no violations)
  </read_first>
  <action>
This is an audit-and-confirm task. Research confirmed via grep that all three manager files are already clean:
- `components/staff-manager.tsx` — zero `bg-white`, `bg-zinc-*`, `bg-slate-*`, `text-zinc-*`, `text-slate-*`, `dark:!` violations
- `components/services-manager.tsx` — same, zero violations
- `components/resources-manager.tsx` — same, zero violations

**Action:** Read each file briefly to confirm the grep finding is accurate. No source changes are expected or needed.

If, on reading, you discover any neutral violations that grep missed (e.g., inside a template literal string or JSX attribute), make the replacement using the established pattern:
- `bg-white` → `bg-background` or `bg-card` as appropriate
- `bg-zinc-*` → `bg-muted` (for subtle background) or `bg-card`
- `text-zinc-*` → `text-foreground` or `text-muted-foreground`
- `border-zinc-*` → `border-border`

The DASH-04 tests in the test scaffold assert zero violations in all three files via regex. The test passing GREEN confirms the audit result.

After reading: run the test to confirm.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="DASH-04" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage --testNamePattern="DASH-04"` exits 0
    - All three manager file tests pass GREEN (zero neutral violations confirmed)
    - If any violations were found and fixed: `grep -c "bg-zinc-\|text-zinc-\|bg-slate-\|text-slate-\|bg-white" components/staff-manager.tsx components/services-manager.tsx components/resources-manager.tsx` returns 0
  </acceptance_criteria>
  <done>DASH-04 audit complete. All three manager files confirmed clean. DASH-04 tests GREEN.</done>
</task>

</tasks>

<verification>
`npx jest __tests__/dashboard-auth-surface.test.ts --no-coverage` — DASH-03 and DASH-04 describe blocks GREEN.
`npx jest --no-coverage` — full suite passes.
</verification>

<success_criteria>
DASH-03: All hardcoded hex values in Recharts props replaced with CSS variable strings. EmptyState uses text-muted-foreground. contentStyle borders use var(--color-border).
DASH-04: All three manager files confirmed clean — zero neutral violations. Tests serve as documented audit proof.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-auth-surface/03-03-SUMMARY.md`
</output>
