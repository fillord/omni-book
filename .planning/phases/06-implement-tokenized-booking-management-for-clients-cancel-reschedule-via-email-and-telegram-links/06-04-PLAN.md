---
phase: 06-implement-tokenized-booking-management-for-clients-cancel-reschedule-via-email-and-telegram-links
plan: 04
type: execute
wave: 3
depends_on: [06-01]
files_modified:
  - lib/email/resend.ts
  - app/api/bookings/route.ts
  - lib/i18n/translations.ts
autonomous: true
requirements: [TOK-07]
must_haves:
  truths:
    - "Email booking confirmation includes a management link with the manageToken"
    - "Telegram booking confirmation includes a management link with the manageToken"
    - "Management link format is https://{domain}/manage/{manageToken}"
    - "i18n keys exist for management page UI strings in RU, EN, KZ"
  artifacts:
    - path: "lib/email/resend.ts"
      provides: "Updated email template with management link"
      contains: "manageToken"
    - path: "app/api/bookings/route.ts"
      provides: "Updated Telegram confirmation with management link"
      contains: "manage/"
    - path: "lib/i18n/translations.ts"
      provides: "Translation keys for management page"
      contains: "manage"
  key_links:
    - from: "app/api/bookings/route.ts"
      to: "lib/email/resend.ts"
      via: "passes manageToken to sendBookingConfirmation"
      pattern: "manageToken"
    - from: "app/api/bookings/route.ts"
      to: "lib/telegram.ts"
      via: "Telegram message includes /manage/ link"
      pattern: "manage/"
---

<objective>
Update email and Telegram booking confirmation messages to include the tokenized management link, and add i18n translation keys for the management page.

Purpose: Clients need to receive the management link at booking time so they can later cancel or reschedule. i18n keys enable future localization of the management page.
Output: Updated email template, updated Telegram message, new translation keys in RU/EN/KZ.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-implement-tokenized-booking-management-for-clients-cancel-reschedule-via-email-and-telegram-links/06-CONTEXT.md
@.planning/phases/06-implement-tokenized-booking-management-for-clients-cancel-reschedule-via-email-and-telegram-links/06-RESEARCH.md
@.planning/phases/06-implement-tokenized-booking-management-for-clients-cancel-reschedule-via-email-and-telegram-links/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/email/resend.ts (BookingEmailData interface):
```typescript
export interface BookingEmailData {
  guestName: string
  guestEmail: string | null | undefined
  tenantName: string
  serviceName: string
  resourceName: string
  startsAt: Date | string
  timezone?: string
}
```

From app/api/bookings/route.ts (POST handler, lines 155-181):
```typescript
// Fire-and-forget confirmation email
if (booking.guestEmail && booking.guestName) {
  sendBookingConfirmation({
    guestName:    booking.guestName,
    guestEmail:   booking.guestEmail,
    tenantName:   tenant.name,
    serviceName:  service?.name ?? '',
    resourceName: resource?.name ?? '',
    startsAt:     booking.startsAt,
  }).catch(console.error)
}

// Fire-and-forget Telegram notification to business owner
const chatId = (tenant as unknown as { telegramChatId?: string | null }).telegramChatId ?? null
if (chatId) {
  const startsAt = new Date(booking.startsAt)
  const dateStr = format(startsAt, 'd MMMM yyyy', { locale: ru })
  const timeStr = format(startsAt, 'HH:mm')
  const msg = [
    '🔔 <b>Новая запись!</b>',
    `👤 Клиент: ${booking.guestName}`,
    `📅 Дата: ${dateStr}`,
    `⏰ Время: ${timeStr}`,
    `🛠 Услуга: ${service?.name ?? 'Не указана'}`,
  ].join('\n')
  sendTelegramMessage(chatId, msg).catch(console.error)
}
```

From lib/i18n/translations.ts (structure pattern):
```typescript
export const translations: Record<Locale, Record<string, Record<string, string>>> = {
  ru: {
    common: { ... },
    landing: { ... },
    // ... other sections
  },
  kz: { ... },
  en: { ... },
}
```

From lib/booking/engine.ts (createBooking return value):
- Returns the created booking object which includes `manageToken` (added in Plan 01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update email template and Telegram message to include management link</name>
  <files>lib/email/resend.ts, app/api/bookings/route.ts</files>
  <read_first>
    - lib/email/resend.ts (full file — current email template HTML and BookingEmailData interface)
    - app/api/bookings/route.ts (full file — POST handler with email send and Telegram notification)
  </read_first>
  <action>
**File 1: `lib/email/resend.ts`**

1. Add `manageToken` to the `BookingEmailData` interface:
```typescript
export interface BookingEmailData {
  guestName: string
  guestEmail: string | null | undefined
  tenantName: string
  serviceName: string
  resourceName: string
  startsAt: Date | string
  timezone?: string
  manageToken?: string | null  // Token for public booking management page
}
```

2. In the `sendBookingConfirmation` function, inside the email HTML template, add a new table row after the "Время" row. Only include it if `data.manageToken` is truthy:

```typescript
${data.manageToken ? `
  <tr>
    <td style="padding:6px 0;color:#6b7280">Управление записью</td>
    <td style="padding:6px 0">
      <a href="${process.env.NEXT_PUBLIC_APP_URL || 'https://omni-book.site'}/manage/${data.manageToken}" style="color:#4299e1;text-decoration:underline">
        Отменить или перенести
      </a>
    </td>
  </tr>
` : ''}
```

Use `process.env.NEXT_PUBLIC_APP_URL` as the base URL with fallback to `https://omni-book.site`.

**File 2: `app/api/bookings/route.ts`**

1. In the POST handler, update the `sendBookingConfirmation` call to include `manageToken`:

Find the existing call (around line 157):
```typescript
sendBookingConfirmation({
  guestName:    booking.guestName,
  guestEmail:   booking.guestEmail,
  tenantName:   tenant.name,
  serviceName:  service?.name ?? '',
  resourceName: resource?.name ?? '',
  startsAt:     booking.startsAt,
}).catch(console.error)
```

Add `manageToken`:
```typescript
sendBookingConfirmation({
  guestName:    booking.guestName,
  guestEmail:   booking.guestEmail,
  tenantName:   tenant.name,
  serviceName:  service?.name ?? '',
  resourceName: resource?.name ?? '',
  startsAt:     booking.startsAt,
  manageToken:  booking.manageToken,
}).catch(console.error)
```

2. In the Telegram notification block (around line 173), add the management link to the message array. Insert it as the last line before `.join('\n')`:

```typescript
const appUrl = process.env.NEXT_PUBLIC_APP_URL || 'https://omni-book.site'
const msg = [
  '🔔 <b>Новая запись!</b>',
  `👤 Клиент: ${booking.guestName}`,
  `📅 Дата: ${dateStr}`,
  `⏰ Время: ${timeStr}`,
  `🛠 Услуга: ${service?.name ?? 'Не указана'}`,
  ...(booking.manageToken ? [`🔗 Управление: ${appUrl}/manage/${booking.manageToken}`] : []),
].join('\n')
```

Note: The Telegram management link is included in the notification to the BUSINESS OWNER. The client receives the management link via their confirmation email (if they provided an email) and via the Telegram confirmation to the client (if they have a Telegram chat). The Telegram message in the booking route currently goes to the business owner — adding the link there lets the owner also access the management page. This is per the CONTEXT.md requirement: "Update Telegram confirmation message to include the same management link".
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/booking-manage-surface.test.ts -t "TOK-07" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/email/resend.ts interface BookingEmailData contains `manageToken`
    - lib/email/resend.ts contains `/manage/` in the email HTML template
    - lib/email/resend.ts contains `Управление записью` (table row label)
    - lib/email/resend.ts contains `Отменить или перенести` (link text)
    - app/api/bookings/route.ts sendBookingConfirmation call includes `manageToken: booking.manageToken`
    - app/api/bookings/route.ts Telegram msg array contains `manage/` (management link)
    - TOK-07 tests pass in booking-manage-surface.test.ts
  </acceptance_criteria>
  <done>Email confirmation includes a "Управление записью" row with a link to /manage/{token}. Telegram notification includes a management link. Both use NEXT_PUBLIC_APP_URL with fallback. TOK-07 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n translation keys for management page</name>
  <files>lib/i18n/translations.ts</files>
  <read_first>
    - lib/i18n/translations.ts (full file — to understand the structure and find the right insertion point for a new section)
    - components/booking-manage-page.tsx (to see which strings are used and could be translated)
  </read_first>
  <action>
Add a new `manage` section to all three locales (ru, kz, en) in the `translations` object in `lib/i18n/translations.ts`.

Add these keys to `ru.manage`:
```typescript
manage: {
  pageTitle: 'Управление записью',
  bookingDetails: 'Детали записи',
  service: 'Услуга',
  resource: 'Специалист',
  dateTime: 'Дата и время',
  status: 'Статус',
  cancel: 'Отменить запись',
  reschedule: 'Перенести',
  confirmReschedule: 'Подтвердить перенос',
  cancelledSuccess: 'Запись отменена',
  rescheduledSuccess: 'Запись перенесена',
  cutoffMessage: 'Для отмены или переноса, пожалуйста, свяжитесь с нами напрямую',
  contactPhone: 'Телефон для связи',
  noSlots: 'Нет доступных слотов на эту дату',
  selectNewTime: 'Выберите новое время',
  back: 'Назад',
  statusConfirmed: 'Подтверждена',
  statusCancelled: 'Отменена',
  statusCompleted: 'Завершена',
  statusPending: 'Ожидает',
  statusNoShow: 'Не явился',
  alreadyCancelled: 'Запись уже отменена',
  alreadyCompleted: 'Запись уже завершена',
},
```

Add equivalent keys to `en.manage`:
```typescript
manage: {
  pageTitle: 'Manage Booking',
  bookingDetails: 'Booking Details',
  service: 'Service',
  resource: 'Specialist',
  dateTime: 'Date and Time',
  status: 'Status',
  cancel: 'Cancel Booking',
  reschedule: 'Reschedule',
  confirmReschedule: 'Confirm Reschedule',
  cancelledSuccess: 'Booking cancelled',
  rescheduledSuccess: 'Booking rescheduled',
  cutoffMessage: 'To cancel or reschedule, please contact us directly',
  contactPhone: 'Contact phone',
  noSlots: 'No available slots for this date',
  selectNewTime: 'Select a new time',
  back: 'Back',
  statusConfirmed: 'Confirmed',
  statusCancelled: 'Cancelled',
  statusCompleted: 'Completed',
  statusPending: 'Pending',
  statusNoShow: 'No show',
  alreadyCancelled: 'Booking is already cancelled',
  alreadyCompleted: 'Booking is already completed',
},
```

Add equivalent keys to `kz.manage`:
```typescript
manage: {
  pageTitle: 'Жазбаны басқару',
  bookingDetails: 'Жазба мәліметтері',
  service: 'Қызмет',
  resource: 'Маман',
  dateTime: 'Күні мен уақыты',
  status: 'Мәртебесі',
  cancel: 'Жазбаны болдырмау',
  reschedule: 'Ауыстыру',
  confirmReschedule: 'Ауыстыруды растау',
  cancelledSuccess: 'Жазба болдырылмады',
  rescheduledSuccess: 'Жазба ауыстырылды',
  cutoffMessage: 'Болдырмау немесе ауыстыру үшін бізге тікелей хабарласыңыз',
  contactPhone: 'Байланыс телефоны',
  noSlots: 'Бұл күнге бос орын жоқ',
  selectNewTime: 'Жаңа уақыт таңдаңыз',
  back: 'Артқа',
  statusConfirmed: 'Расталды',
  statusCancelled: 'Болдырылмады',
  statusCompleted: 'Аяқталды',
  statusPending: 'Күтуде',
  statusNoShow: 'Келмеді',
  alreadyCancelled: 'Жазба бұрыннан болдырылмаған',
  alreadyCompleted: 'Жазба бұрыннан аяқталған',
},
```

Insert the `manage` section after the last existing section in each locale object. Ensure the file continues to export the same `translations` constant.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx tsc --noEmit 2>&1 | tail -5 && node -e "const t = require('./lib/i18n/translations'); console.log('ru keys:', Object.keys(t.translations.ru.manage || {}).length); console.log('en keys:', Object.keys(t.translations.en.manage || {}).length); console.log('kz keys:', Object.keys(t.translations.kz.manage || {}).length);" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - lib/i18n/translations.ts contains a `manage` section in `ru` with at least 20 keys
    - lib/i18n/translations.ts contains a `manage` section in `en` with at least 20 keys
    - lib/i18n/translations.ts contains a `manage` section in `kz` with at least 20 keys
    - All three locale manage sections have the same set of keys
    - `npx tsc --noEmit` passes (no type errors from translations)
    - The Russian key `cutoffMessage` contains `Для отмены или переноса`
  </acceptance_criteria>
  <done>Translation keys for the management page added in all three locales (RU, EN, KZ) with 22 keys each covering page title, booking details, action labels, status labels, success messages, and error states.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/booking-manage-surface.test.ts -t "TOK-07" --no-coverage` — pass
- `npx jest __tests__/booking-manage-surface.test.ts --no-coverage` — ALL tests pass (TOK-01 through TOK-07)
- `npx tsc --noEmit` — no type errors
- `npx jest --no-coverage` — full test suite green (no regressions)
</verification>

<success_criteria>
- Email template includes management link when manageToken is present
- Telegram notification includes management link
- i18n keys exist for all management page strings in RU, EN, KZ
- All TOK-01 through TOK-07 tests pass
- Full test suite has no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-implement-tokenized-booking-management-for-clients-cancel-reschedule-via-email-and-telegram-links/06-04-SUMMARY.md`
</output>
