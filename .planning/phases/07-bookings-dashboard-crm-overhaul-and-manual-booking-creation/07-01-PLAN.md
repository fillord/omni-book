---
phase: 07-bookings-dashboard-crm-overhaul-and-manual-booking-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/bookings-crm-surface.test.ts
autonomous: true
requirements:
  - CRM-B01
  - CRM-B02
  - CRM-B03
  - CRM-B04
  - CRM-B05
  - CRM-B06
  - CRM-B07
  - CRM-B08
  - CRM-B09
  - CRM-B10
  - CRM-B11
  - CRM-B12

must_haves:
  truths:
    - "Test file exists with 12 describe blocks covering CRM-B01 through CRM-B12"
    - "All tests initially fail or skip because target files do not yet exist"
    - "jest __tests__/bookings-crm-surface.test.ts runs without crashes"
  artifacts:
    - path: "__tests__/bookings-crm-surface.test.ts"
      provides: "Static assertion test scaffold for all 12 CRM requirements"
      min_lines: 80
  key_links: []
---

<objective>
Create the Wave 0 test scaffold for Phase 7. This file contains static file assertion tests (fs.readFileSync + regex) for all 12 CRM-B requirements. Tests will fail initially and turn green as Plans 02-04 implement the production code.

Purpose: Establish the validation contract before any implementation begins, following the project's test-scaffold-first pattern.
Output: `__tests__/bookings-crm-surface.test.ts` with 12 describe blocks
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bookings-dashboard-crm-overhaul-and-manual-booking-creation/07-CONTEXT.md
@.planning/phases/07-bookings-dashboard-crm-overhaul-and-manual-booking-creation/07-RESEARCH.md
@.planning/phases/07-bookings-dashboard-crm-overhaul-and-manual-booking-creation/07-VALIDATION.md

<interfaces>
<!-- Existing test pattern from __tests__/booking-manage-surface.test.ts -->
```typescript
import fs from "fs";
import path from "path";

const ROOT = path.resolve(__dirname, "..");

const safeRead = (relPath: string): string => {
  const full = path.join(ROOT, relPath);
  if (!fs.existsSync(full)) return "";
  return fs.readFileSync(full, "utf-8");
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test scaffold with 12 CRM-B requirement stubs</name>
  <files>__tests__/bookings-crm-surface.test.ts</files>
  <action>
Create `__tests__/bookings-crm-surface.test.ts` following the established project test pattern (safeRead helper, fs.readFileSync + regex assertions).

Use the standard `safeRead` helper (same as booking-manage-surface.test.ts). Create 12 describe blocks, one per requirement:

**CRM-B01** — Read `components/bookings-dashboard.tsx`. Assert it contains a default exclusion of CANCELLED status. Test that the file matches a pattern showing default statuses do NOT include 'CANCELLED' (e.g., regex for initial status state that excludes CANCELLED, or a `showCancelled` state defaulting to `false`).

**CRM-B02** — Read `components/bookings-dashboard.tsx`. Assert it contains the string `showCancelled` (toggle state) AND contains a chip/button for showing cancelled bookings (match a pattern like `Отменено` or the i18n key `showCancelled`).

**CRM-B03** — Read `components/bookings-dashboard.tsx`. Assert it contains `sticky` AND `top-0` AND `z-10` (day-group sticky header classes). Also assert it contains a day-grouping function or pattern (e.g., `groupBookingsByDay` or `Map<string`).

**CRM-B04** — Read `components/bookings-dashboard.tsx`. Assert it contains `font-semibold` AND `text-lg` applied to time display in booking rows (matching the UI-SPEC requirement for bold/large time).

**CRM-B05** — Read `components/bookings-dashboard.tsx`. Assert it contains import of `ManualBookingSheet` from a relative path. Also assert `components/manual-booking-sheet.tsx` file exists via `fs.existsSync`.

**CRM-B06** — Read `lib/actions/bookings.ts`. Assert it contains `createManualBooking` AND `'use server'`.

**CRM-B07** — Read `lib/actions/bookings.ts`. Assert it contains `manageToken: null` (explicit null, not randomUUID). Also assert it does NOT match `manageToken.*randomUUID` or `manageToken.*crypto`.

**CRM-B08** — Read `components/manual-booking-sheet.tsx`. Assert it contains import from `@/components/ui/sheet` (Sheet component usage).

**CRM-B09** — Read `components/bookings-dashboard.tsx`. Assert it contains `neu-raised` (booking row cards). Read `components/manual-booking-sheet.tsx`. Assert it contains both `neu-raised` and `neu-inset` (sheet form elements).

**CRM-B10** — Read `lib/i18n/translations.ts`. Assert each locale section (ru, kz, en) contains both `newBooking` and `tomorrow` keys within the `dashboard` section.

**CRM-B11** — Read `lib/actions/bookings.ts`. Assert it contains both `requireAuth` and `requireRole`.

**CRM-B12** — Read `components/manual-booking-sheet.tsx`. Assert it contains `/api/bookings/slots` (slot endpoint URL) AND contains all four params: `tenantSlug`, `resourceId`, `serviceId`, `date`.

Each test should use simple `.toContain()` or `.toMatch()` assertions. Tests that read files which do not yet exist will return `""` from `safeRead` and fail naturally — this is expected for Wave 0.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/bookings-crm-surface.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists at __tests__/bookings-crm-surface.test.ts with 12 describe blocks (CRM-B01 through CRM-B12). All tests run without crashes (exit cleanly). Tests fail because production files do not yet contain the required patterns.</done>
</task>

</tasks>

<verification>
- `jest __tests__/bookings-crm-surface.test.ts --no-coverage` runs without crashes
- All 12 describe blocks present (grep for CRM-B01 through CRM-B12)
- Tests fail with assertion errors (not file read errors) thanks to safeRead helper
</verification>

<success_criteria>
- Test scaffold file committed
- 12 requirement test blocks present and runnable
- No test framework errors (only assertion failures expected)
</success_criteria>

<output>
After completion, create `.planning/phases/07-bookings-dashboard-crm-overhaul-and-manual-booking-creation/07-01-SUMMARY.md`
</output>
