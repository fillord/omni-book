---
phase: 10-saas-monetization-enterprise-tier-platform-payments
plan: 04
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - app/dashboard/settings/billing/billing-content.tsx
  - app/dashboard/settings/billing/enterprise-calculator.tsx
  - app/dashboard/settings/billing/payment-modal.tsx
  - lib/i18n/translations.ts
  - lib/actions/billing.ts
autonomous: true
requirements: [MON-04, MON-05, MON-08]

must_haves:
  truths:
    - "Billing page shows an interactive slider (1-200 resources) that calculates Enterprise pricing in real-time using formula monthly = base + resources * pricePerResource, yearly = monthly * 10"
    - "Request Enterprise CTA sends Telegram notification to admin with resource count and calculated price, sets planStatus to PENDING"
    - "Billing page shows a two-step payment modal: Step 1 initiates payment, Step 2 shows mock QR + countdown + Simulate Payment button"
    - "Simulate Payment button only appears when NEXT_PUBLIC_MOCK_PAYMENTS is true"
    - "If pendingPayment prop is non-null on page load, modal opens directly to Step 2 (resume after refresh)"
    - "Hardcoded 10 000 price and card number 4400 4303 8983 0552 are removed from billing-content.tsx"
  artifacts:
    - path: "app/dashboard/settings/billing/billing-content.tsx"
      provides: "Refactored billing page consuming subscriptionPlans, pendingPayment, enterprisePlan props"
    - path: "app/dashboard/settings/billing/enterprise-calculator.tsx"
      provides: "Neumorphic slider-based Enterprise pricing calculator"
      contains: "EnterpriseCalculator"
    - path: "app/dashboard/settings/billing/payment-modal.tsx"
      provides: "Two-step payment modal with mock QR and countdown"
      contains: "PaymentModal"
    - path: "lib/i18n/translations.ts"
      provides: "billing section i18n keys for calculator, payment modal, enterprise inquiry in RU/EN/KZ"
    - path: "lib/actions/billing.ts"
      provides: "requestEnterpriseInquiry Server Action"
      exports: ["requestEnterpriseInquiry"]
  key_links:
    - from: "app/dashboard/settings/billing/billing-content.tsx"
      to: "app/dashboard/settings/billing/enterprise-calculator.tsx"
      via: "imports and renders EnterpriseCalculator with enterprisePlan prop"
      pattern: "EnterpriseCalculator"
    - from: "app/dashboard/settings/billing/billing-content.tsx"
      to: "app/dashboard/settings/billing/payment-modal.tsx"
      via: "imports and renders PaymentModal with pendingPayment and subscriptionPlans props"
      pattern: "PaymentModal"
    - from: "app/dashboard/settings/billing/enterprise-calculator.tsx"
      to: "lib/actions/billing.ts"
      via: "calls requestEnterpriseInquiry with resourceCount and calculatedPrice"
      pattern: "requestEnterpriseInquiry"
    - from: "app/dashboard/settings/billing/payment-modal.tsx"
      to: "lib/actions/billing.ts"
      via: "calls initiateSubscriptionPayment to create payment"
      pattern: "initiateSubscriptionPayment"
---

<objective>
Build the three client-side billing page features: Enterprise pricing calculator with interactive slider (MON-04), Enterprise inquiry flow with Telegram notification (MON-05), and the two-step waiting-for-payment modal replacing the manual card-transfer dialog (MON-08). Also remove all hardcoded pricing and card number remnants from billing-content.tsx.

Purpose: These are the user-facing billing interactions that make the monetization system functional. Plan 02 built the backend (Server Actions, payment adapter, page.tsx data fetching). This plan consumes those props on the client side.

Output: Refactored billing-content.tsx, two new extracted components (EnterpriseCalculator, PaymentModal), requestEnterpriseInquiry Server Action, i18n keys.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-saas-monetization-enterprise-tier-platform-payments/10-CONTEXT.md
@.planning/phases/10-saas-monetization-enterprise-tier-platform-payments/10-RESEARCH.md
@.planning/phases/10-saas-monetization-enterprise-tier-platform-payments/10-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 02 output that this plan consumes. -->

From app/dashboard/settings/billing/page.tsx (modified in Plan 02):
The server component fetches and passes these props to BillingContent:
```typescript
<BillingContent
  tenant={tenantInfo}
  subscriptionPlans={subscriptionPlans}
  pendingPayment={pendingPayment ? {
    id: pendingPayment.id,
    amount: pendingPayment.amount,
    mockQrCode: pendingPayment.mockQrCode,
    expiresAt: pendingPayment.expiresAt.toISOString(),
    planTarget: pendingPayment.planTarget,
  } : null}
  enterprisePlan={enterprisePlan ? {
    priceMonthly: enterprisePlan.priceMonthly,
    pricePerResource: enterprisePlan.pricePerResource,
  } : null}
/>
```

From lib/actions/billing.ts (added in Plan 02):
```typescript
export async function initiateSubscriptionPayment(plan: Plan = 'PRO'): Promise<{
  success: boolean
  paymentId?: string
  mockQrCode?: string
  amount?: number
  expiresAt?: string
  error?: string
}>
```

Existing actions in lib/actions/billing.ts:
```typescript
export async function requestProActivation()  // deprecated but kept
export async function renewSubscription()     // deprecated but kept
```

From lib/telegram.ts:
```typescript
export async function sendTelegramMessage(chatId: string, text: string): Promise<void>
```
Admin chat ID: `process.env.ADMIN_TELEGRAM_CHAT_ID`

From lib/i18n/translations.ts:
- Structure: `translations: Record<Locale, Record<string, Record<string, string>>>`
- Locale type: `'ru' | 'kz' | 'en'`
- Existing sections include `payment` (for client booking payments) and sidebar label `billing` in `section`

From app/dashboard/settings/billing/billing-content.tsx (current):
```typescript
type TenantInfo = {
  plan: string
  planStatus: string
  subscriptionExpiresAt: Date | null
  kaspiMerchantId: string | null
  kaspiApiKey: string | null
}
export function BillingContent({ tenant }: { tenant: TenantInfo })
```
- Line 174: hardcoded `10 000 ₸` in upgrade card
- Line 213: hardcoded `10 000 ₸` in dialog
- Lines 232-243: hardcoded card number `4400 4303 8983 0552` block to remove
- Uses Neumorphism: `neu-raised`, `neu-inset`, `bg-[var(--neu-bg)]`

Neumorphism patterns:
- Cards: `className="neu-raised bg-[var(--neu-bg)] rounded-xl p-6"`
- Inputs/containers: `className="neu-inset bg-[var(--neu-bg)]"`
- Buttons: `className="neu-raised"` on shadcn Button or custom div
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n keys + requestEnterpriseInquiry Server Action + EnterpriseCalculator + PaymentModal components</name>
  <files>
    lib/i18n/translations.ts
    lib/actions/billing.ts
    app/dashboard/settings/billing/enterprise-calculator.tsx
    app/dashboard/settings/billing/payment-modal.tsx
  </files>
  <read_first>
    lib/i18n/translations.ts
    lib/actions/billing.ts
    lib/telegram.ts
    lib/db.ts
    lib/auth/guards.ts
    app/dashboard/settings/billing/billing-content.tsx
  </read_first>
  <action>
**Step 1 — i18n keys (per D-09):**

Add a `billing` section to each locale in `lib/i18n/translations.ts`. Place it after the existing `payment` section in each locale block. Add these keys:

**Russian (ru):**
```typescript
billing: {
  payWithKaspi: 'Оплатить через Kaspi',
  waitingForPayment: 'Ожидание оплаты подписки',
  scanQr: 'Отсканируйте QR-код или оплатите по ссылке',
  simulatePayment: 'Симулировать оплату',
  paymentExpired: 'Время оплаты истекло',
  enterpriseCalculator: 'Калькулятор Enterprise',
  resourceCount: 'Количество ресурсов',
  monthlyPrice: 'Ежемесячно',
  yearlyPrice: 'Ежегодно (скидка 2 мес.)',
  requestEnterprise: 'Запросить Enterprise',
  enterpriseRequested: 'Заявка на Enterprise отправлена',
  paymentAmount: 'Сумма к оплате',
  perMonth: '/ месяц',
  initiatePayment: 'Перейти к оплате',
  paymentProcessing: 'Обработка...',
  resumePayment: 'У вас есть незавершённый платёж',
},
```

**Kazakh (kz):**
```typescript
billing: {
  payWithKaspi: 'Kaspi арқылы төлеу',
  waitingForPayment: 'Жазылым төлемін күту',
  scanQr: 'QR-кодты сканерлеңіз немесе сілтеме бойынша төлеңіз',
  simulatePayment: 'Төлемді симуляциялау',
  paymentExpired: 'Төлем уақыты аяқталды',
  enterpriseCalculator: 'Enterprise калькуляторы',
  resourceCount: 'Ресурстар саны',
  monthlyPrice: 'Ай сайын',
  yearlyPrice: 'Жыл сайын (2 ай жеңілдік)',
  requestEnterprise: 'Enterprise сұрау',
  enterpriseRequested: 'Enterprise сұранысы жіберілді',
  paymentAmount: 'Төлем сомасы',
  perMonth: '/ ай',
  initiatePayment: 'Төлемге өту',
  paymentProcessing: 'Өңдеу...',
  resumePayment: 'Аяқталмаған төлеміңіз бар',
},
```

**English (en):**
```typescript
billing: {
  payWithKaspi: 'Pay with Kaspi',
  waitingForPayment: 'Waiting for subscription payment',
  scanQr: 'Scan the QR code or pay via the link',
  simulatePayment: 'Simulate Payment',
  paymentExpired: 'Payment time expired',
  enterpriseCalculator: 'Enterprise Calculator',
  resourceCount: 'Number of resources',
  monthlyPrice: 'Monthly',
  yearlyPrice: 'Yearly (2 months free)',
  requestEnterprise: 'Request Enterprise',
  enterpriseRequested: 'Enterprise inquiry submitted',
  paymentAmount: 'Amount due',
  perMonth: '/ month',
  initiatePayment: 'Proceed to payment',
  paymentProcessing: 'Processing...',
  resumePayment: 'You have a pending payment',
},
```

**Step 2 — requestEnterpriseInquiry Server Action (per D-03d, MON-05):**

Add to `lib/actions/billing.ts` (which already has `'use server'` directive, `initiateSubscriptionPayment`, and the deprecated actions):

```typescript
export async function requestEnterpriseInquiry(
  resourceCount: number,
  calculatedMonthlyPrice: number
): Promise<{ success: boolean; error?: string }> {
  try {
    const session = await requireAuth()
    requireRole(session, ['OWNER'])
    const tenantId = session.user.tenantId!

    // Set planStatus to PENDING
    const tenant = await basePrisma.tenant.update({
      where: { id: tenantId },
      data: { planStatus: 'PENDING' },
      select: { name: true },
    })

    // Send Telegram to admin
    const adminChatId = process.env.ADMIN_TELEGRAM_CHAT_ID
    if (adminChatId) {
      const { sendTelegramMessage } = await import('@/lib/telegram')
      await sendTelegramMessage(
        adminChatId,
        `<b>Заявка на Enterprise</b>\n\nКомпания: ${tenant.name}\nРесурсы: ${resourceCount}\nРасчётная цена: ${calculatedMonthlyPrice.toLocaleString()} ₸/мес`
      ).catch(console.error)
    }

    revalidatePath('/dashboard/settings/billing')
    return { success: true }
  } catch {
    return { success: false, error: 'Failed to submit enterprise inquiry' }
  }
}
```

Import `sendTelegramMessage` dynamically (or at the top — match the existing import style in the file). Ensure `basePrisma`, `requireAuth`, `requireRole`, `revalidatePath` are imported (they should already be from Plan 02's `initiateSubscriptionPayment`).

**Step 3 — EnterpriseCalculator component (per D-03a, D-03b, D-03c, MON-04):**

Create `app/dashboard/settings/billing/enterprise-calculator.tsx`:

```typescript
'use client'

import { useState, useTransition } from 'react'
import { toast } from 'sonner'
import { requestEnterpriseInquiry } from '@/lib/actions/billing'

type Props = {
  enterprisePlan: {
    priceMonthly: number  // base price (-1 for dynamic, but calculator uses it as base; if -1, use 0 as base)
    pricePerResource: number
  }
  isPending: boolean  // tenant planStatus === 'PENDING'
}

export function EnterpriseCalculator({ enterprisePlan, isPending }: Props) {
  const [resourceCount, setResourceCount] = useState(10)
  const [isPendingTransition, startTransition] = useTransition()

  const base = enterprisePlan.priceMonthly < 0 ? 0 : enterprisePlan.priceMonthly
  const monthly = base + resourceCount * enterprisePlan.pricePerResource
  const yearly = monthly * 10

  async function handleRequestEnterprise() {
    startTransition(async () => {
      const res = await requestEnterpriseInquiry(resourceCount, monthly)
      if (res.error) {
        toast.error(res.error)
      } else {
        toast.success('Заявка на Enterprise отправлена!')
      }
    })
  }

  return (/* JSX below */)
}
```

JSX structure:
- Outer wrapper: `<div className="neu-inset bg-[var(--neu-bg)] rounded-xl p-6 space-y-6">`
- Title: `<h3 className="text-lg font-semibold text-foreground">` with text "Enterprise" and a calculator icon from lucide-react (use `Calculator` or `Sliders`)
- Slider section:
  - Label: `<label className="text-sm font-medium text-muted-foreground">` showing "Количество ресурсов: {resourceCount}"
  - HTML range input: `<input type="range" min={1} max={200} value={resourceCount} onChange={e => setResourceCount(Number(e.target.value))} className="w-full accent-indigo-600" />`
  - Min/max labels: flex row with "1" on left, "200" on right, `text-xs text-muted-foreground`
- Price display section: `<div className="flex flex-col sm:flex-row gap-4">`
  - Monthly box: `<div className="flex-1 neu-raised bg-[var(--neu-bg)] rounded-xl p-4 text-center">` with `<p className="text-sm text-muted-foreground">Ежемесячно</p>` and `<p className="text-2xl font-bold text-foreground">{monthly.toLocaleString()} ₸</p>`
  - Yearly box: same structure with "Ежегодно (скидка 2 мес.)" and `{yearly.toLocaleString()} ₸`
- CTA button: `<button onClick={handleRequestEnterprise} disabled={isPending || isPendingTransition} className="w-full h-12 rounded-xl neu-raised bg-[var(--neu-bg)] text-orange-500 font-medium active:neu-inset transition-all disabled:opacity-50 [filter:drop-shadow(0_0_8px_theme(colors.orange.400/40%))]">`
  - Text: isPending ? "Заявка отправлена" : "Запросить Enterprise"

**Step 4 — PaymentModal component (per D-06a, D-06b, D-08c, MON-08):**

Create `app/dashboard/settings/billing/payment-modal.tsx`:

```typescript
'use client'

import { useState, useEffect, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { toast } from 'sonner'
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'
import { initiateSubscriptionPayment } from '@/lib/actions/billing'
import { ShieldCheck, Clock } from 'lucide-react'

type PendingPayment = {
  id: string
  amount: number
  mockQrCode: string | null
  expiresAt: string  // ISO string
  planTarget: string
}

type Props = {
  isOpen: boolean
  onOpenChange: (open: boolean) => void
  pendingPayment: PendingPayment | null
  planLabel: string  // "PRO" or plan displayName
  planPrice: number  // priceMonthly from subscriptionPlans
}
```

Component state:
- `step`: `1 | 2` — Step 1 is initiation, Step 2 is waiting
- `paymentData`: `{ id: string; mockQrCode: string; amount: number; expiresAt: string } | null`
- `timeLeft`: number (seconds remaining in countdown)
- `isPending`: boolean via `useTransition`

On mount / when `pendingPayment` prop changes:
- If `pendingPayment` is non-null AND modal opens: set `step = 2`, populate `paymentData` from prop (per D-08c — resume on refresh)
- If `pendingPayment` is null: set `step = 1`

Countdown timer (useEffect when step === 2 and paymentData exists):
- Calculate seconds remaining: `Math.max(0, Math.floor((new Date(paymentData.expiresAt).getTime() - Date.now()) / 1000))`
- `setInterval` every 1 second decrementing `timeLeft`
- When `timeLeft` reaches 0, show expired message

Step 1 JSX:
- DialogHeader with title "Оплата подписки {planLabel}"
- Price display: `<div className="neu-inset bg-[var(--neu-bg)] rounded-xl p-4 text-center"><p className="text-sm text-muted-foreground">Сумма к оплате:</p><p className="text-3xl font-bold text-foreground">{planPrice.toLocaleString()} ₸</p></div>`
- Button: `<Button onClick={handleInitiate} disabled={isPending} className="w-full neu-raised">` with text "Оплатить через Kaspi"
- `handleInitiate`: calls `initiateSubscriptionPayment('PRO')`, on success sets `paymentData` from response, sets `step = 2`

Step 2 JSX:
- DialogHeader with title "Ожидание оплаты подписки"
- QR display: `<div className="flex justify-center"><img src={paymentData.mockQrCode} alt="QR Code" className="w-48 h-48 rounded-xl" /></div>` (mockQrCode is a data:image/svg+xml URI)
- Amount: show `paymentData.amount.toLocaleString() ₸`
- Countdown: `<div className="neu-inset bg-[var(--neu-bg)] rounded-xl p-3 text-center">` showing formatted time left as `MM:SS` — format: `${Math.floor(timeLeft / 60)}:${String(timeLeft % 60).padStart(2, '0')}`
- If `timeLeft <= 0`: show "Время оплаты истекло" message, hide QR
- "Симулировать оплату" button: ONLY shown when `process.env.NEXT_PUBLIC_MOCK_PAYMENTS === 'true'`:
  ```tsx
  {process.env.NEXT_PUBLIC_MOCK_PAYMENTS === 'true' && timeLeft > 0 && (
    <Button
      variant="outline"
      onClick={handleSimulate}
      className="w-full neu-raised text-sm"
    >
      Симулировать оплату
    </Button>
  )}
  ```
- `handleSimulate`: calls `fetch('/api/mock-payment/simulate', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ paymentId: paymentData.id }) })` — note: the simulate endpoint uses Bearer auth, but from the client the call goes through the Next.js server, so pass the secret via a Server Action instead. Better approach: create a thin wrapper `simulatePaymentAction` in billing.ts:
  ```typescript
  // In lib/actions/billing.ts — add:
  export async function simulatePaymentAction(paymentId: string): Promise<{ success: boolean; error?: string }> {
    try {
      const { processPlatformPayment } = await import('@/lib/platform-payment')
      const result = await processPlatformPayment(paymentId)
      if (!result.success) return { success: false, error: result.error }
      revalidatePath('/dashboard/settings/billing')
      return { success: true }
    } catch {
      return { success: false, error: 'Simulation failed' }
    }
  }
  ```
  Then the PaymentModal calls `simulatePaymentAction(paymentData.id)` directly as a Server Action. On success: toast.success, router.refresh(), close modal.

Use Neumorphism throughout: `neu-raised`, `neu-inset`, `bg-[var(--neu-bg)]`.
  </action>
  <verify>
    <automated>ls -la app/dashboard/settings/billing/enterprise-calculator.tsx app/dashboard/settings/billing/payment-modal.tsx lib/actions/billing.ts && grep -c "billing:" lib/i18n/translations.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/dashboard/settings/billing/enterprise-calculator.tsx` exists
    - `grep "'use client'" app/dashboard/settings/billing/enterprise-calculator.tsx` matches
    - `grep "EnterpriseCalculator" app/dashboard/settings/billing/enterprise-calculator.tsx` matches
    - `grep "resourceCount" app/dashboard/settings/billing/enterprise-calculator.tsx` matches
    - `grep "pricePerResource" app/dashboard/settings/billing/enterprise-calculator.tsx` matches
    - `grep "type=\"range\"" app/dashboard/settings/billing/enterprise-calculator.tsx` matches (slider input)
    - `grep "monthly.*10" app/dashboard/settings/billing/enterprise-calculator.tsx` matches (yearly = monthly * 10)
    - `grep "requestEnterpriseInquiry" app/dashboard/settings/billing/enterprise-calculator.tsx` matches
    - `grep "neu-raised\|neu-inset" app/dashboard/settings/billing/enterprise-calculator.tsx` matches
    - `app/dashboard/settings/billing/payment-modal.tsx` exists
    - `grep "'use client'" app/dashboard/settings/billing/payment-modal.tsx` matches
    - `grep "PaymentModal" app/dashboard/settings/billing/payment-modal.tsx` matches
    - `grep "initiateSubscriptionPayment" app/dashboard/settings/billing/payment-modal.tsx` matches
    - `grep "NEXT_PUBLIC_MOCK_PAYMENTS" app/dashboard/settings/billing/payment-modal.tsx` matches
    - `grep "mockQrCode" app/dashboard/settings/billing/payment-modal.tsx` matches
    - `grep "timeLeft\|countdown\|setInterval" app/dashboard/settings/billing/payment-modal.tsx` matches (countdown timer)
    - `grep "step.*2\|step === 2\|step ===" app/dashboard/settings/billing/payment-modal.tsx` matches (two-step flow)
    - `grep "export async function requestEnterpriseInquiry" lib/actions/billing.ts` matches
    - `grep "sendTelegramMessage" lib/actions/billing.ts` matches (Telegram notification for enterprise inquiry)
    - `grep "planStatus.*PENDING" lib/actions/billing.ts` matches (sets PENDING on enterprise request)
    - `grep "export async function simulatePaymentAction" lib/actions/billing.ts` matches
    - `grep -c "billing:" lib/i18n/translations.ts` returns 3 (one per locale: ru, kz, en)
    - `grep "payWithKaspi" lib/i18n/translations.ts` matches
    - `grep "enterpriseCalculator" lib/i18n/translations.ts` matches
    - `grep "simulatePayment" lib/i18n/translations.ts` matches
    - `grep "resourceCount" lib/i18n/translations.ts` matches
  </acceptance_criteria>
  <done>EnterpriseCalculator component renders interactive slider with real-time pricing. PaymentModal renders two-step payment flow with mock QR, countdown, and simulate button. requestEnterpriseInquiry sends Telegram and sets PENDING. All i18n keys added in RU/KZ/EN.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor billing-content.tsx to consume new props and components</name>
  <files>
    app/dashboard/settings/billing/billing-content.tsx
  </files>
  <read_first>
    app/dashboard/settings/billing/billing-content.tsx
    app/dashboard/settings/billing/enterprise-calculator.tsx
    app/dashboard/settings/billing/payment-modal.tsx
    app/dashboard/settings/billing/page.tsx
    lib/actions/billing.ts
  </read_first>
  <action>
**Per D-08a-d, D-03a, D-01d (MON-04, MON-05, MON-08):** Refactor `billing-content.tsx` to:

1. Remove hardcoded pricing and card number
2. Accept new props from page.tsx
3. Integrate EnterpriseCalculator and PaymentModal components

**Step 1 — Update the TenantInfo type and component props:**

Replace the current props signature:
```typescript
type SubscriptionPlanInfo = {
  plan: string
  displayName: string
  priceMonthly: number
  priceYearly: number
  features: string[]
}

type PendingPaymentInfo = {
  id: string
  amount: number
  mockQrCode: string | null
  expiresAt: string
  planTarget: string
}

type EnterprisePlanInfo = {
  priceMonthly: number
  pricePerResource: number
}

type Props = {
  tenant: TenantInfo
  subscriptionPlans: SubscriptionPlanInfo[]
  pendingPayment: PendingPaymentInfo | null
  enterprisePlan: EnterprisePlanInfo | null
}

export function BillingContent({ tenant, subscriptionPlans, pendingPayment, enterprisePlan }: Props) {
```

Keep the existing `TenantInfo` type unchanged.

**Step 2 — Add imports for new components:**
```typescript
import { EnterpriseCalculator } from './enterprise-calculator'
import { PaymentModal } from './payment-modal'
import { initiateSubscriptionPayment } from '@/lib/actions/billing'
```

Remove the import of `Copy` from lucide-react (no longer needed). Keep all other existing imports.

**Step 3 — Replace the upgrade card pricing section (lines ~172-193):**

Replace the hardcoded `10 000 ₸` and feature list with dynamic data from `subscriptionPlans`. Find the PRO plan from the array:
```typescript
const proPlan = subscriptionPlans.find(p => p.plan === 'PRO')
```

In the upgrade card's price display area, replace:
- `10 000 ₸` with `{proPlan?.priceMonthly.toLocaleString() ?? '10 000'} ₸`
- The hardcoded feature list `["До 20 ресурсов...", ...]` with `{proPlan?.features.map((feature, i) => (...))}` — mapping the `features` array from the DB-backed SubscriptionPlan

**Step 4 — Replace the Dialog (payment modal) entirely:**

Remove the entire `<Dialog>` block (lines ~195-278) — this is the old manual card transfer dialog with the hardcoded card number `4400 4303 8983 0552` and "Я оплатил" button.

Replace it with the `PaymentModal` component:
```tsx
<PaymentModal
  isOpen={isOpen}
  onOpenChange={setIsOpen}
  pendingPayment={pendingPayment}
  planLabel={isFree ? 'PRO' : tenant.plan}
  planPrice={proPlan?.priceMonthly ?? 10000}
/>
```

The trigger button stays in billing-content (the "Выбрать PRO" / "Продлить подписку" button). Update it to use `onClick={() => setIsOpen(true)}` directly instead of wrapping in `DialogTrigger`.

Replace:
```tsx
<Dialog open={isOpen} onOpenChange={setIsOpen}>
  <DialogTrigger>
    <div role="button" ...>
      <ShieldCheck size={18} />
      {isFree ? 'Выбрать PRO' : 'Продлить подписку'}
    </div>
  </DialogTrigger>
  <DialogContent>...</DialogContent>
</Dialog>
```

With:
```tsx
<button
  onClick={() => setIsOpen(true)}
  className="inline-flex w-full sm:w-auto h-12 px-8 items-center justify-center gap-2 whitespace-nowrap rounded-md text-base font-medium cursor-pointer neu-raised bg-[var(--neu-bg)] text-orange-500 active:neu-inset transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 [filter:drop-shadow(0_0_8px_theme(colors.orange.400/40%))]"
>
  <ShieldCheck size={18} />
  {isFree ? 'Выбрать PRO' : 'Продлить подписку'}
</button>

<PaymentModal
  isOpen={isOpen}
  onOpenChange={setIsOpen}
  pendingPayment={pendingPayment}
  planLabel={isFree ? 'PRO' : tenant.plan}
  planPrice={proPlan?.priceMonthly ?? 10000}
/>
```

**Step 5 — Remove the old `handlePaymentConfirm` function entirely.** It called `requestProActivation()` / `renewSubscription()`. The PaymentModal now handles payment initiation via `initiateSubscriptionPayment`. The old `handlePaymentConfirm` function (lines ~56-75) is dead code — delete it.

Also remove the import of `requestProActivation, renewSubscription` from `@/lib/actions/billing` (these are now deprecated and the component no longer calls them). Keep `updatePaymentSettings` import if still used for the Kaspi config section.

**Step 6 — Add EnterpriseCalculator section:**

After the upgrade card (`showUpgradeCard` block) and before the Kaspi payment config card, add:

```tsx
{enterprisePlan && (
  <EnterpriseCalculator
    enterprisePlan={enterprisePlan}
    isPending={isPending}
  />
)}
```

**Step 7 — Auto-open modal on pending payment (per D-08c):**

Add a `useEffect` that opens the modal if `pendingPayment` is non-null:
```typescript
useEffect(() => {
  if (pendingPayment) {
    setIsOpen(true)
  }
}, [pendingPayment])
```

This ensures when the user refreshes the page with a pending payment, the modal opens directly to Step 2 (the PaymentModal component handles step selection based on the `pendingPayment` prop).

**Step 8 — Remove the `Copy` icon import from lucide-react** (the card copy block is removed). Keep `Check, CreditCard, Clock, CheckCircle2, ShieldCheck, Zap, XCircle, AlertTriangle` as they are used elsewhere in the component.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="phase-10" --no-coverage 2>&1 | head -80</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "10 000" app/dashboard/settings/billing/billing-content.tsx` returns 0 (no hardcoded price)
    - `grep -c "4400" app/dashboard/settings/billing/billing-content.tsx` returns 0 (no hardcoded card number)
    - `grep -c "Перевод на карту" app/dashboard/settings/billing/billing-content.tsx` returns 0 (old payment dialog removed)
    - `grep -c "Я оплатил" app/dashboard/settings/billing/billing-content.tsx` returns 0 (old button removed)
    - `grep -c "handlePaymentConfirm" app/dashboard/settings/billing/billing-content.tsx` returns 0 (dead code removed)
    - `grep "EnterpriseCalculator" app/dashboard/settings/billing/billing-content.tsx` matches (component imported and rendered)
    - `grep "PaymentModal" app/dashboard/settings/billing/billing-content.tsx` matches (component imported and rendered)
    - `grep "subscriptionPlans" app/dashboard/settings/billing/billing-content.tsx` matches (new prop consumed)
    - `grep "pendingPayment" app/dashboard/settings/billing/billing-content.tsx` matches (new prop consumed)
    - `grep "enterprisePlan" app/dashboard/settings/billing/billing-content.tsx` matches (new prop consumed)
    - `grep "priceMonthly" app/dashboard/settings/billing/billing-content.tsx` matches (dynamic pricing)
    - `grep "Copy" app/dashboard/settings/billing/billing-content.tsx` does NOT match (removed import)
    - `grep "requestProActivation\|renewSubscription" app/dashboard/settings/billing/billing-content.tsx` does NOT match (deprecated imports removed)
    - `grep "useEffect" app/dashboard/settings/billing/billing-content.tsx` matches (auto-open modal on pending payment)
    - MON-02 test assertions for billing-content.tsx pass (no `10 000 ₸`, no `4400`)
  </acceptance_criteria>
  <done>billing-content.tsx refactored: hardcoded pricing and card number removed, EnterpriseCalculator rendered for Enterprise tier with slider, PaymentModal replaces manual transfer dialog, auto-opens on pending payment, all pricing from DB via subscriptionPlans prop.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="phase-10" --no-coverage` — All MON-01 through MON-09 assertions should pass
- `grep -c "10 000" app/dashboard/settings/billing/billing-content.tsx` returns 0
- `grep -c "4400" app/dashboard/settings/billing/billing-content.tsx` returns 0
- `grep "EnterpriseCalculator" app/dashboard/settings/billing/billing-content.tsx` — matches
- `grep "PaymentModal" app/dashboard/settings/billing/billing-content.tsx` — matches
- `grep "NEXT_PUBLIC_MOCK_PAYMENTS" app/dashboard/settings/billing/payment-modal.tsx` — matches
- `grep "type=\"range\"" app/dashboard/settings/billing/enterprise-calculator.tsx` — matches
- `grep -c "billing:" lib/i18n/translations.ts` returns 3
</verification>

<success_criteria>
- Enterprise calculator shows interactive slider (1-200) with real-time monthly/yearly pricing using formula monthly = base + resources * pricePerResource, yearly = monthly * 10
- "Request Enterprise" CTA sends Telegram to admin with resource count and calculated price, sets planStatus to PENDING
- Payment modal has two-step flow: Step 1 (initiate) and Step 2 (mock QR + countdown + Simulate Payment button)
- "Simulate Payment" button only visible when NEXT_PUBLIC_MOCK_PAYMENTS=true
- Modal auto-opens to Step 2 on page load if pendingPayment exists
- All hardcoded pricing (10 000) and card number (4400) removed from billing-content.tsx
- i18n keys added for all billing UI strings in RU/KZ/EN
</success_criteria>

<output>
After completion, create `.planning/phases/10-saas-monetization-enterprise-tier-platform-payments/10-04-SUMMARY.md`
</output>
