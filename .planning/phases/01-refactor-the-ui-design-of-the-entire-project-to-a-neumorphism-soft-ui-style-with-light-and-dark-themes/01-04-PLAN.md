---
phase: 01-neumorphism-refactor
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - components/landing/HeroSection.tsx
  - components/landing/Navbar.tsx
  - components/theme-toggle.tsx
autonomous: false
requirements: [NEU-12, NEU-13, NEU-14]

must_haves:
  truths:
    - "HeroSection uses bg-background instead of gradient for section background"
    - "Footer.tsx retains bg-zinc-900 and intentional comment"
    - "All 215+ existing tests pass alongside new Neumorphism tests"
    - "ThemeToggle uses Neumorphism shadow styling instead of bg-muted hover"
  artifacts:
    - path: "components/landing/HeroSection.tsx"
      provides: "Neumorphism-adapted hero section"
      contains: "bg-background"
    - path: "components/theme-toggle.tsx"
      provides: "Neumorphism-styled theme toggle"
      contains: "neu-raised"
  key_links:
    - from: "components/landing/HeroSection.tsx section"
      to: "app/globals.css --background"
      via: "bg-background class"
      pattern: "bg-background"
    - from: "components/theme-toggle.tsx"
      to: "app/globals.css .neu-raised"
      via: "CSS class reference"
      pattern: "neu-raised"
---

<objective>
Adapt the landing page hero section, Navbar, and ThemeToggle for Neumorphism, then run full regression.

Purpose: HeroSection's gradient background is incompatible with Neumorphism (shadows need a flat surface). Navbar border needs Neumorphism treatment. ThemeToggle's hover:bg-muted becomes invisible after token remapping (Pitfall 9). This plan also serves as the final regression gate — all 215+ tests must pass.

Output: Updated HeroSection.tsx, Navbar.tsx, theme-toggle.tsx. Full test suite green.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-CONTEXT.md
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-RESEARCH.md

<interfaces>
<!-- Current HeroSection.tsx section tag (line 11) -->
```tsx
<section className="relative overflow-hidden bg-gradient-to-br from-indigo-50 via-white to-violet-50 dark:from-zinc-900 dark:via-zinc-900 dark:to-zinc-800 py-24 md:py-36">
```

<!-- Current Navbar.tsx header tag (line 28) -->
```tsx
<header className="sticky top-0 z-50 bg-background/90 backdrop-blur border-b border-border">
```

<!-- Current ThemeToggle.tsx (two button variants) -->
```tsx
// iconOnly mode:
className={`flex h-8 w-8 items-center justify-center rounded-md text-muted-foreground hover:bg-muted hover:text-foreground transition-colors ${className}`}

// full mode:
className={`flex items-center gap-2.5 rounded-md px-3 py-2 text-sm text-muted-foreground hover:bg-muted hover:text-foreground transition-colors w-full ${className}`}
```

<!-- Neumorphism utility classes from Plan 01 -->
```css
.neu-raised { box-shadow: 10px 10px 20px var(--neu-shadow-dark), -10px -10px 20px var(--neu-shadow-light); }
.neu-btn { /* raised + hover accent + active inset */ }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt HeroSection and Navbar for Neumorphism</name>
  <files>components/landing/HeroSection.tsx, components/landing/Navbar.tsx</files>
  <read_first>
    - components/landing/HeroSection.tsx (full file — need to see gradient, blobs, floating icons)
    - components/landing/Navbar.tsx (full file — need to see border and mobile menu)
    - components/landing/Footer.tsx (read to confirm bg-zinc-900 + intentional comment — DO NOT modify)
    - __tests__/landing-surface.test.ts (understand existing test assertions that must still pass)
  </read_first>
  <action>
**HeroSection.tsx changes:**

1. **Main section tag:** Replace the gradient background with `bg-background`.
Change from:
```tsx
<section className="relative overflow-hidden bg-gradient-to-br from-indigo-50 via-white to-violet-50 dark:from-zinc-900 dark:via-zinc-900 dark:to-zinc-800 py-24 md:py-36">
```
to:
```tsx
<section className="relative overflow-hidden bg-background py-24 md:py-36">
```
This removes the multi-color gradient that conflicts with Neumorphism's flat surface requirement. `bg-background` resolves to `var(--neu-bg)` automatically.

2. **Gradient blobs (decorative):** Keep the two decorative blob divs but adapt their colors to use Neumorphism accent tones:
- First blob (top-right): Change `bg-indigo-100/60 dark:bg-indigo-900/20` to `bg-neu-accent/10` (uses the @theme inline mapping for neu-accent)
- Second blob (bottom-left): Change `bg-violet-100/60 dark:bg-violet-900/20` to `bg-neu-accent/5`
- Keep `blur-3xl`, `rounded-full`, positioning, and animation classes unchanged

3. **Floating decorative icons:** Keep all four floating icon divs unchanged — their `text-indigo-400`, `text-violet-400` etc. colors are decorative accents at low opacity, not surface colors. They complement the Neumorphism look.

4. **Badge span:** Change `bg-indigo-100 dark:bg-indigo-900/40 text-indigo-700 dark:text-indigo-300` to `neu-raised bg-background text-neu-accent`. This makes the badge a raised Neumorphism chip.

5. **Gradient text span (heroTitleAccent):** Keep `text-transparent bg-clip-text bg-gradient-to-r from-indigo-600 to-violet-600 dark:from-indigo-400 dark:to-violet-400` — this is text decoration, not a surface. The gradient text effect is fine within Neumorphism.

6. **CTA buttons area (if present further in file):** If there are CTA Link/Button elements, ensure they inherit Neumorphism styling from Button component changes already made in Plan 02.

**Navbar.tsx changes:**

1. **Header tag:** Change from:
```tsx
<header className="sticky top-0 z-50 bg-background/90 backdrop-blur border-b border-border">
```
to:
```tsx
<header className="sticky top-0 z-50 bg-background/90 backdrop-blur neu-raised">
```
Key change: Remove `border-b border-border` (borders are replaced by Neumorphism shadow). Add `neu-raised` for the elevated navbar shadow effect. Keep `bg-background/90 backdrop-blur` for the frosted glass effect which complements Neumorphism.

**Footer.tsx — DO NOT MODIFY:**
- Read to confirm `bg-zinc-900` and the word `intentional` are present
- This file is intentionally excluded from Neumorphism surface treatment
- NEU-12 test in landing-surface.test.ts already guards this

**IMPORTANT:** After editing HeroSection.tsx, verify that the existing `landing-surface.test.ts` LAND-01 assertions still pass. The test checks for absence of `bg-zinc-*` neutral classes in CORE_COMPONENTS. Since we are replacing gradients with `bg-background` (not `bg-zinc-*`), this should pass. But `from-indigo-50 via-white to-violet-50` being removed means the test's whitelist check should still pass.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --testPathPattern="(neumorphism-surface|landing-surface)" --verbose 2>&1 | grep -E "(NEU-12|NEU-14|PASS|FAIL)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - components/landing/HeroSection.tsx main section contains `bg-background` not `bg-gradient-to-br`
    - components/landing/HeroSection.tsx does NOT contain `from-indigo-50 via-white to-violet-50` on the section element
    - components/landing/HeroSection.tsx badge span contains `neu-raised`
    - components/landing/Navbar.tsx header contains `neu-raised` not `border-b border-border`
    - components/landing/Footer.tsx still contains `bg-zinc-900` and `intentional` (unmodified)
    - NEU-14 test passes (HeroSection bg-background)
    - NEU-12 test passes (Footer preserved)
    - Existing landing-surface.test.ts passes (51 tests)
  </acceptance_criteria>
  <done>HeroSection uses bg-background flat surface. Navbar uses .neu-raised instead of border. Footer preserved. NEU-12, NEU-14 pass. All existing landing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Neumorphism to ThemeToggle and run full regression</name>
  <files>components/theme-toggle.tsx</files>
  <read_first>
    - components/theme-toggle.tsx (current state)
    - components/public-theme-toggle.tsx (wrapper — verify it just passes iconOnly, no changes needed)
  </read_first>
  <action>
**theme-toggle.tsx changes:**

Per Pitfall 9: After token remapping, `hover:bg-muted` becomes invisible because `--muted` is now `var(--neu-bg)` (same as background). Replace with Neumorphism shadow-based hover.

1. **Skeleton placeholder (line 21):** Change `bg-muted` to `bg-background neu-raised`. (Skeleton pill gets Neumorphism look.)

2. **iconOnly button (line 35):** Change className from:
```
flex h-8 w-8 items-center justify-center rounded-md text-muted-foreground hover:bg-muted hover:text-foreground transition-colors
```
to:
```
flex h-8 w-8 items-center justify-center rounded-lg text-muted-foreground neu-raised hover:text-neu-accent transition-all
```
Changes: `rounded-md` to `rounded-lg` (consistent with increased --radius). Remove `hover:bg-muted hover:text-foreground`. Add `neu-raised` for Neumorphism raised surface. Add `hover:text-neu-accent` for accent hover. Change `transition-colors` to `transition-all` (to include box-shadow transition on active press).

3. **Full-width button (line 47):** Change className from:
```
flex items-center gap-2.5 rounded-md px-3 py-2 text-sm text-muted-foreground hover:bg-muted hover:text-foreground transition-colors w-full
```
to:
```
flex items-center gap-2.5 rounded-lg px-3 py-2 text-sm text-muted-foreground neu-raised hover:text-neu-accent transition-all w-full
```
Same pattern: remove `hover:bg-muted hover:text-foreground`, add `neu-raised hover:text-neu-accent`, `rounded-md` to `rounded-lg`, `transition-colors` to `transition-all`.

**public-theme-toggle.tsx — DO NOT MODIFY:**
It simply renders `<ThemeToggle iconOnly />` — no changes needed.

**Full regression:**
After all changes, run `npx jest` (the full suite including all 215+ existing tests plus new neumorphism-surface tests). All must pass for phase completion.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - components/theme-toggle.tsx iconOnly button contains `neu-raised`
    - components/theme-toggle.tsx iconOnly button contains `hover:text-neu-accent`
    - components/theme-toggle.tsx does NOT contain `hover:bg-muted` (old invisible hover)
    - components/theme-toggle.tsx full button contains `neu-raised`
    - components/theme-toggle.tsx contains `rounded-lg` (not rounded-md)
    - All neumorphism-surface tests pass (NEU-01 through NEU-14 minus NEU-12/13 which use other test files)
    - `npx jest` full suite exits with code 0 (NEU-13: all tests pass)
  </acceptance_criteria>
  <done>ThemeToggle uses Neumorphism raised shadow with accent hover. Full test suite (215+ existing + new neumorphism-surface tests) passes green. NEU-13 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Neumorphism UI</name>
  <files>none</files>
  <action>
Human verifies the complete Neumorphism visual system across all application surfaces.

What was built: globals.css CSS foundation, all shadcn/ui components (Button, Input, Card, Dialog, Select, DropdownMenu, Sheet, Badge), HeroSection, Navbar, and ThemeToggle. Both light and dark themes work via token remapping.

Steps to verify:
1. Run `npm run dev` (or `npx next dev`)
2. Open http://localhost:3000 (landing page)
   - Cards show raised shadow effect (dual light+dark shadows)
   - Navbar has raised shadow instead of bottom border
   - Hero section has flat bg-background surface (no gradient)
   - Hero decorative blobs still visible at low opacity
3. Click theme toggle (sun/moon icon in navbar)
   - Smooth 300ms transition (no flash/abrupt change)
   - Dark mode uses dark Neumorphism palette (#1e1e24 bg, green accent)
   - Light mode uses light Neumorphism palette (#e0e5ec bg, blue accent)
4. Click any button on the page
   - Default state shows raised shadow
   - On mouse press (:active), shadow flips to inset (pressed-in effect)
   - On hover, text changes to accent color
5. Navigate to /dashboard (or any auth-protected page)
   - Dashboard cards have raised shadow
   - Input fields appear inset (recessed)
   - Sidebar has Neumorphism surface
6. Open any dropdown/select/dialog
   - Popup surfaces have raised shadow (no thin ring border)
   - Select triggers appear inset
7. Check Footer at bottom of landing page
   - Footer retains dark bg-zinc-900 surface (intentionally excluded from Neumorphism)
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User confirms Neumorphism visual system looks correct across all surfaces in both light and dark themes.</done>
</task>

</tasks>

<verification>
- `npx jest` — ALL tests pass (215+ existing + new neumorphism-surface)
- Visual verification via checkpoint (landing, dashboard, booking, dialogs)
- Theme toggle smooth transition confirmed
</verification>

<success_criteria>
- HeroSection uses bg-background, not gradient, for main section surface
- Footer.tsx retains bg-zinc-900 + intentional comment (unchanged)
- ThemeToggle uses .neu-raised with accent hover instead of invisible bg-muted hover
- All 215+ existing tests pass (NEU-13)
- Visual checkpoint confirms Neumorphism appearance in both themes
</success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-04-SUMMARY.md`
</output>
