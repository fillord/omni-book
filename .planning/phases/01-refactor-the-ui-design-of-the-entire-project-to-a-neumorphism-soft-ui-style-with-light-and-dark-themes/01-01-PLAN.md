---
phase: 01-neumorphism-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/neumorphism-surface.test.ts
  - app/globals.css
autonomous: true
requirements: [NEU-01, NEU-02, NEU-03, NEU-10]

must_haves:
  truths:
    - "Neumorphism CSS variables are defined for both light and dark themes"
    - "Existing shadcn tokens remap to Neumorphism values automatically"
    - "Reusable .neu-raised, .neu-inset, .neu-btn utility classes exist"
    - "Theme toggle produces smooth 300ms transitions on bg, color, box-shadow"
  artifacts:
    - path: "__tests__/neumorphism-surface.test.ts"
      provides: "Static file assertions for all NEU requirements"
      contains: "neumorphism-surface"
    - path: "app/globals.css"
      provides: "Neumorphism CSS variables, token remapping, utility classes, global transition"
      contains: "--neu-bg"
  key_links:
    - from: "app/globals.css :root"
      to: "app/globals.css @theme inline"
      via: "--color-neu-accent: var(--neu-accent)"
      pattern: "--color-neu-accent"
    - from: "app/globals.css --background"
      to: "all bg-background usages across codebase"
      via: "var(--neu-bg) token remapping"
      pattern: "--background:\\s*var\\(--neu-bg\\)"
---

<objective>
Create the Neumorphism test scaffold and CSS foundation layer.

Purpose: The test file (Wave 0 Nyquist requirement) must exist before any implementation so all subsequent plans have automated verification. The CSS variables + utility classes in globals.css are the foundation that propagates through the entire codebase via token remapping.

Output: `__tests__/neumorphism-surface.test.ts` with static file assertions for NEU-01 through NEU-11 and NEU-14. Updated `app/globals.css` with Neumorphism variables, token overrides, utility classes, and global transition rule.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-CONTEXT.md
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-RESEARCH.md
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-VALIDATION.md

<interfaces>
<!-- Existing test pattern from __tests__/landing-surface.test.ts -->
```typescript
import fs from "fs";
import path from "path";

const readComponent = (name: string) =>
  fs.readFileSync(path.join(SOME_DIR, name), "utf-8");

// Pattern: fs.readFileSync + regex/string assertions
// expect(source).toContain("someString");
// expect(source).toMatch(/someRegex/);
// expect(source).not.toMatch(/antiPattern/);
```

<!-- Current globals.css structure (key sections) -->
```css
/* Line 5: @custom-variant dark (&:is(.dark *)); */
/* Lines 15-51: :root { ... existing OKLch tokens ... } */
/* Lines 53-81: .dark { ... existing OKLch tokens ... } */
/* Lines 83-123: @theme inline { ... color mappings ... } */
/* Lines 125-135: @layer base { * { @apply border-border ... } body { ... } } */
/* Lines 137+: Micro-animations, hover effects */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create neumorphism-surface test scaffold</name>
  <files>__tests__/neumorphism-surface.test.ts</files>
  <read_first>
    - __tests__/landing-surface.test.ts (pattern reference for fs.readFileSync + regex assertions)
    - app/globals.css (current state to understand what will change)
    - components/ui/button.tsx (current className to write assertions against)
    - components/ui/input.tsx (current className)
    - components/ui/card.tsx (current className — ring-1 ring-foreground/10 must be absent after changes)
    - components/ui/dialog.tsx (current className)
    - components/ui/select.tsx (current className)
    - components/ui/dropdown-menu.tsx (current className)
    - components/landing/HeroSection.tsx (gradient classes to verify removal)
    - components/booking-calendar.tsx (RESOURCE_PALETTE to verify preservation)
  </read_first>
  <action>
Create `__tests__/neumorphism-surface.test.ts` following the exact pattern from `landing-surface.test.ts` (import fs, path; readFile helper; describe blocks).

The test file must contain these test groups:

**NEU-01: Neumorphism variables defined in globals.css**
- Read `app/globals.css`
- Assert `:root` block contains `--neu-bg: #e0e5ec`
- Assert `:root` block contains `--neu-shadow-light: #ffffff`
- Assert `:root` block contains `--neu-shadow-dark: #a3b1c6`
- Assert `:root` block contains `--neu-accent: #4299e1`
- Assert `:root` block contains `--neu-text: #4a5568`
- Assert `.dark` block contains `--neu-bg: #1e1e24`
- Assert `.dark` block contains `--neu-shadow-light: #2c2c35`
- Assert `.dark` block contains `--neu-shadow-dark: #101013`
- Assert `.dark` block contains `--neu-accent: #10b981`
- Assert `.dark` block contains `--neu-text: #d1d5db`

**NEU-02: Token remapping**
- Assert globals.css contains `--background: var(--neu-bg)` (present in both :root and .dark — check at least once)
- Assert globals.css contains `--card: var(--neu-bg)`
- Assert globals.css contains `--popover: var(--neu-bg)`
- Assert globals.css contains `--border: transparent` or `--border: transparent`-like value
- Assert globals.css contains `--input: var(--neu-bg)`

**NEU-03: Utility classes defined**
- Assert globals.css contains `.neu-raised`
- Assert globals.css contains `box-shadow:` with `var(--neu-shadow-dark)` and `var(--neu-shadow-light)` in the `.neu-raised` context
- Assert globals.css contains `.neu-inset`
- Assert globals.css contains `inset` in the `.neu-inset` context
- Assert globals.css contains `.neu-btn`

**NEU-04: Button uses neu-btn**
- Read `components/ui/button.tsx`
- Assert contains `neu-btn` (in default variant or base class)

**NEU-05: Input uses neu-inset**
- Read `components/ui/input.tsx`
- Assert contains `neu-inset`
- Assert contains `border-transparent`
- Assert does NOT contain `border border-input` (the old pattern) — use regex `/border\s+border-input/` to match the exact old pattern

**NEU-06: Card uses neu-raised, no ring**
- Read `components/ui/card.tsx`
- Assert contains `neu-raised`
- Assert does NOT contain `ring-1 ring-foreground/10`

**NEU-07: Dialog uses neu-raised, no ring**
- Read `components/ui/dialog.tsx`
- Assert DialogContent contains `neu-raised`
- Assert does NOT contain `ring-1 ring-foreground/10`

**NEU-08: Select trigger inset, content raised**
- Read `components/ui/select.tsx`
- Assert contains `neu-inset` (trigger)
- Assert contains `neu-raised` (content)
- Assert does NOT contain `shadow-md ring-1 ring-foreground/10` in the content section

**NEU-09: DropdownMenu uses neu-raised**
- Read `components/ui/dropdown-menu.tsx`
- Assert contains `neu-raised`
- Assert does NOT contain `shadow-md ring-1 ring-foreground/10`

**NEU-10: Global transition rule**
- Assert globals.css contains `transition:` with `background-color` and `box-shadow` and `300ms`
- Assert does NOT contain `transition: all` (anti-pattern from Pitfall 10)

**NEU-11: RESOURCE_PALETTE preserved**
- Read `components/booking-calendar.tsx`
- Assert contains `RESOURCE_PALETTE`

**NEU-14: HeroSection uses bg-background**
- Read `components/landing/HeroSection.tsx`
- Assert contains `bg-background` on the main section element
- Assert does NOT contain `bg-gradient-to-br from-indigo-50` (the old gradient on the section bg)

All tests should initially FAIL (RED state) since no implementation exists yet. This is expected.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --testPathPattern="neumorphism-surface" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/neumorphism-surface.test.ts exists and contains `import fs from "fs"`
    - File contains `describe("NEU-01` or similar describe block names
    - File contains assertions for `--neu-bg: #e0e5ec`
    - File contains assertions for `neu-raised`
    - File contains assertions for `neu-inset`
    - File contains assertions for `neu-btn`
    - File contains assertions for `RESOURCE_PALETTE`
    - File contains assertions for `bg-background` (HeroSection)
    - `npx jest --testPathPattern="neumorphism-surface"` runs (tests may fail since implementation is not done yet — that is correct RED state)
  </acceptance_criteria>
  <done>Test file runs via Jest. Tests for NEU-01/02/03/10 will fail (no globals.css changes yet). Tests for NEU-04 through NEU-09, NEU-11, NEU-14 will also fail (component changes not yet made). This is the expected RED state before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Neumorphism CSS foundation into globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (MUST read current state before editing)
    - __tests__/neumorphism-surface.test.ts (just created — understand what assertions to satisfy)
  </read_first>
  <action>
Edit `app/globals.css` with the following precise changes:

**1. In the `:root` block (after line 14, before existing token definitions):** Add Neumorphism palette variables:
```css
  /* Neumorphism palette */
  --neu-bg: #e0e5ec;
  --neu-text: #4a5568;
  --neu-shadow-light: #ffffff;
  --neu-shadow-dark: #a3b1c6;
  --neu-accent: #4299e1;
```

**2. In the `:root` block:** Override existing tokens to use Neumorphism values. Replace the existing OKLch values for these specific tokens:
- `--background: var(--neu-bg);` (was `oklch(1 0 0)`)
- `--foreground: var(--neu-text);` (was `oklch(0.15 0.01 250)`)
- `--card: var(--neu-bg);` (was `oklch(1 0 0)`)
- `--card-foreground: var(--neu-text);` (was `oklch(0.15 0.01 250)`)
- `--popover: var(--neu-bg);` (was `oklch(1 0 0)`)
- `--popover-foreground: var(--neu-text);` (was `oklch(0.15 0.01 250)`)
- `--primary: var(--neu-accent);` (was `oklch(0.45 0.18 260)`)
- `--border: transparent;` (was `oklch(0.9 0.01 250)`)
- `--input: var(--neu-bg);` (was `oklch(0.9 0.01 250)`)
- `--muted: var(--neu-bg);` (for CardFooter surface consistency per Pitfall 5)
- `--sidebar: var(--neu-bg);` (per Research Open Question 1 recommendation)
- `--sidebar-border: transparent;` (consistent with --border)
- `--ring: var(--neu-accent);` (focus rings use accent)

Keep `--primary-foreground`, `--secondary`, `--secondary-foreground`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--destructive`, `--chart-*`, and remaining sidebar tokens at their current OKLch values.

**3. Increase border radius:** Change `--radius: 0.5rem;` to `--radius: 0.875rem;` (14px, per Research recommendation for Neumorphism aesthetics).

**4. In the `.dark` block:** Add Neumorphism palette variables:
```css
  /* Neumorphism palette */
  --neu-bg: #1e1e24;
  --neu-text: #d1d5db;
  --neu-shadow-light: #2c2c35;
  --neu-shadow-dark: #101013;
  --neu-accent: #10b981;
```

**5. In the `.dark` block:** Override same tokens:
- `--background: var(--neu-bg);` (was `oklch(0.12 0.01 250)`)
- `--foreground: var(--neu-text);` (was `oklch(0.98 0 0)`)
- `--card: var(--neu-bg);` (was `oklch(0.16 0.01 250)`)
- `--card-foreground: var(--neu-text);`
- `--popover: var(--neu-bg);`
- `--popover-foreground: var(--neu-text);`
- `--primary: var(--neu-accent);`
- `--border: transparent;`
- `--input: var(--neu-bg);`
- `--muted: var(--neu-bg);`
- `--sidebar: var(--neu-bg);`
- `--sidebar-border: transparent;`
- `--ring: var(--neu-accent);`

Keep dark-mode `--primary-foreground`, `--secondary`, `--secondary-foreground`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--destructive`, `--chart-*`, and remaining sidebar tokens at their current OKLch values.

**6. In the `@theme inline` block:** Add two new entries at the top:
```css
  --color-neu-accent: var(--neu-accent);
  --color-neu-bg: var(--neu-bg);
```

**7. In the `@layer base` block:** Add Neumorphism utility classes and global transition AFTER the existing `body { ... }` rule but still inside `@layer base`:
```css
  /* Neumorphism utility classes */
  .neu-raised {
    box-shadow: 10px 10px 20px var(--neu-shadow-dark), -10px -10px 20px var(--neu-shadow-light);
  }

  .neu-inset {
    box-shadow: inset 5px 5px 10px var(--neu-shadow-dark), inset -5px -5px 10px var(--neu-shadow-light);
  }

  .neu-btn {
    box-shadow: 10px 10px 20px var(--neu-shadow-dark), -10px -10px 20px var(--neu-shadow-light);
    background-color: var(--neu-bg);
  }

  .neu-btn:hover {
    color: var(--neu-accent);
  }

  .neu-btn:active {
    box-shadow: inset 5px 5px 10px var(--neu-shadow-dark), inset -5px -5px 10px var(--neu-shadow-light);
  }

  /* Global theme transition — explicit properties only (NOT transition: all — see Pitfall 10) */
  * {
    transition: background-color 300ms ease-in-out, color 300ms ease-in-out, box-shadow 300ms ease-in-out;
  }
```

NOTE: The existing `* { @apply border-border outline-ring/50; }` in `@layer base` will now resolve `border-border` to `border-transparent` since `--border` is now `transparent`. This is correct behavior. Do NOT remove this line.

NOTE: The global `transition` on `*` must use explicit properties (background-color, color, box-shadow) — NOT `transition: all` which would conflict with existing animations (Pitfall 10).
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --testPathPattern="neumorphism-surface" --verbose 2>&1 | grep -E "(PASS|FAIL|NEU-0[123]|NEU-10)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains `--neu-bg: #e0e5ec` inside `:root`
    - app/globals.css contains `--neu-bg: #1e1e24` inside `.dark`
    - app/globals.css contains `--background: var(--neu-bg)`
    - app/globals.css contains `--card: var(--neu-bg)`
    - app/globals.css contains `--popover: var(--neu-bg)`
    - app/globals.css contains `--border: transparent`
    - app/globals.css contains `--input: var(--neu-bg)`
    - app/globals.css contains `--radius: 0.875rem`
    - app/globals.css contains `.neu-raised` with `box-shadow:` and `var(--neu-shadow-dark)`
    - app/globals.css contains `.neu-inset` with `inset`
    - app/globals.css contains `.neu-btn`
    - app/globals.css contains `transition: background-color 300ms ease-in-out`
    - app/globals.css does NOT contain `transition: all`
    - app/globals.css contains `--color-neu-accent: var(--neu-accent)` in @theme inline
    - NEU-01, NEU-02, NEU-03, NEU-10 test assertions pass
  </acceptance_criteria>
  <done>globals.css contains all Neumorphism variables, token remappings, utility classes, and global transition. Tests NEU-01, NEU-02, NEU-03, NEU-10 pass green. Tests NEU-04 through NEU-09 still fail (component edits not yet done).</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="neumorphism-surface"` — NEU-01, NEU-02, NEU-03, NEU-10 pass
- `npx jest --testPathPattern="landing-surface"` — existing 51 tests still pass (token remapping is backward-compatible with test assertions since they check className strings, not CSS values)
</verification>

<success_criteria>
- Neumorphism test scaffold exists with assertions for all NEU requirements
- globals.css contains --neu-* variables in both :root and .dark
- Existing shadcn tokens (--background, --card, --popover, --border, --input) remapped to var(--neu-bg) / transparent
- .neu-raised, .neu-inset, .neu-btn utility classes defined in @layer base
- Global transition rule (300ms, explicit properties) applied to *
- --radius increased to 0.875rem
- @theme inline has --color-neu-accent and --color-neu-bg entries
</success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-01-SUMMARY.md`
</output>
