---
phase: 01-neumorphism-refactor
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components/ui/dialog.tsx
  - components/ui/select.tsx
  - components/ui/dropdown-menu.tsx
  - components/ui/sheet.tsx
  - components/ui/badge.tsx
autonomous: true
requirements: [NEU-07, NEU-08, NEU-09, NEU-11]

must_haves:
  truths:
    - "Dialog popup appears raised with dual-shadow, no ring border"
    - "Select trigger appears inset, select dropdown appears raised"
    - "Dropdown menu content appears raised with dual-shadow"
    - "RESOURCE_PALETTE in booking-calendar.tsx is untouched"
  artifacts:
    - path: "components/ui/dialog.tsx"
      provides: "Neumorphism dialog surfaces"
      contains: "neu-raised"
    - path: "components/ui/select.tsx"
      provides: "Neumorphism select trigger (inset) and content (raised)"
      contains: "neu-inset"
    - path: "components/ui/dropdown-menu.tsx"
      provides: "Neumorphism dropdown surfaces"
      contains: "neu-raised"
  key_links:
    - from: "components/ui/dialog.tsx DialogContent"
      to: "app/globals.css .neu-raised"
      via: "CSS class reference"
      pattern: "neu-raised"
    - from: "components/ui/select.tsx SelectContent"
      to: "app/globals.css .neu-raised"
      via: "CSS class reference"
      pattern: "neu-raised"
---

<objective>
Apply Neumorphism styling to popup/overlay UI components and verify booking calendar preservation.

Purpose: Dialog, Select, DropdownMenu, Sheet, and Badge complete the shadcn/ui component layer. These floating surfaces replace `ring-1 ring-foreground/10` and `shadow-md` with `.neu-raised`. NEU-11 is verified passively (no changes to booking-calendar.tsx).

Output: Updated dialog.tsx, select.tsx, dropdown-menu.tsx, sheet.tsx, badge.tsx with Neumorphism classes.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-CONTEXT.md
@.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-RESEARCH.md

<interfaces>
<!-- From app/globals.css (created by Plan 01) -->
```css
.neu-raised {
  box-shadow: 10px 10px 20px var(--neu-shadow-dark), -10px -10px 20px var(--neu-shadow-light);
}
.neu-inset {
  box-shadow: inset 5px 5px 10px var(--neu-shadow-dark), inset -5px -5px 10px var(--neu-shadow-light);
}
```

<!-- Current dialog.tsx DialogContent className -->
```
"fixed top-1/2 left-1/2 z-50 grid w-full max-w-[calc(100%-2rem)] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-xl bg-background p-4 text-sm ring-1 ring-foreground/10 duration-100 outline-none sm:max-w-sm data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95"
```

<!-- Current select.tsx SelectTrigger className -->
```
"flex w-fit items-center justify-between gap-1.5 rounded-lg border border-input bg-transparent py-2 pr-2 pl-2.5 text-sm whitespace-nowrap transition-colors outline-none select-none ... dark:bg-input/30 dark:hover:bg-input/50 ..."
```

<!-- Current select.tsx SelectContent className -->
```
"relative isolate z-50 max-h-(--available-height) w-(--anchor-width) min-w-36 origin-(--transform-origin) overflow-x-hidden overflow-y-auto rounded-lg bg-popover text-popover-foreground shadow-md ring-1 ring-foreground/10 ..."
```

<!-- Current dropdown-menu.tsx DropdownMenuContent className -->
```
"z-50 max-h-(--available-height) w-(--anchor-width) min-w-32 origin-(--transform-origin) overflow-x-hidden overflow-y-auto rounded-lg bg-popover p-1 text-popover-foreground shadow-md ring-1 ring-foreground/10 ..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Neumorphism to Dialog and Sheet components</name>
  <files>components/ui/dialog.tsx, components/ui/sheet.tsx</files>
  <read_first>
    - components/ui/dialog.tsx (current state)
    - components/ui/sheet.tsx (current state)
    - app/globals.css (verify utility classes exist)
  </read_first>
  <action>
**dialog.tsx changes:**

1. **DialogContent (DialogPrimitive.Popup) className:** Replace `ring-1 ring-foreground/10` with `neu-raised`. The full className becomes:
```
"fixed top-1/2 left-1/2 z-50 grid w-full max-w-[calc(100%-2rem)] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-xl bg-background p-4 text-sm neu-raised duration-100 outline-none sm:max-w-sm data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95 data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95"
```

2. **DialogFooter className:** Replace `bg-muted/50` with `bg-background` (same fix as CardFooter — surface consistency).
Change from:
```
"-mx-4 -mb-4 flex flex-col-reverse gap-2 rounded-b-xl border-t bg-muted/50 p-4 sm:flex-row sm:justify-end"
```
to:
```
"-mx-4 -mb-4 flex flex-col-reverse gap-2 rounded-b-xl border-t border-transparent bg-background p-4 sm:flex-row sm:justify-end"
```

**sheet.tsx changes:**

1. **SheetContent (SheetPrimitive.Popup) className:** The current class has `shadow-lg` and border classes (`data-[side=bottom]:border-t`, `data-[side=left]:border-r`, etc.). Replace the side borders with Neumorphism:
- Remove all `data-[side=*]:border-*` classes (4 occurrences: border-t, border-r, border-l, border-b)
- Replace `shadow-lg` with `neu-raised`
- The full className should become the same string but with `shadow-lg` replaced by `neu-raised` and the four `data-[side=*]:border-*` fragments removed
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --testPathPattern="neumorphism-surface" --verbose 2>&1 | grep -E "NEU-07"</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/dialog.tsx contains `neu-raised` in DialogContent className
    - components/ui/dialog.tsx does NOT contain `ring-1 ring-foreground/10`
    - components/ui/dialog.tsx DialogFooter contains `bg-background` not `bg-muted/50`
    - components/ui/sheet.tsx contains `neu-raised` replacing `shadow-lg`
    - components/ui/sheet.tsx does NOT contain `data-[side=bottom]:border-t` (borders removed)
    - NEU-07 test passes
  </acceptance_criteria>
  <done>Dialog uses .neu-raised, no ring. DialogFooter uses bg-background. Sheet uses .neu-raised, side borders removed. NEU-07 passes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Neumorphism to Select and DropdownMenu components</name>
  <files>components/ui/select.tsx, components/ui/dropdown-menu.tsx</files>
  <read_first>
    - components/ui/select.tsx (current state)
    - components/ui/dropdown-menu.tsx (current state)
    - app/globals.css (verify utility classes)
  </read_first>
  <action>
**select.tsx changes:**

1. **SelectTrigger className:** Replace border/bg pattern with Neumorphism inset. In the className string:
- Replace `border border-input bg-transparent` with `neu-inset border-transparent bg-[var(--neu-bg)]`
- Remove `dark:bg-input/30 dark:hover:bg-input/50` (dark overrides no longer needed)
- Remove `dark:aria-invalid:border-destructive/50 dark:aria-invalid:ring-destructive/40` (dark overrides)

2. **SelectContent (SelectPrimitive.Popup) className:** Replace shadow + ring with Neumorphism raised.
- Replace `shadow-md ring-1 ring-foreground/10` with `neu-raised`
- Keep all animation classes, bg-popover, text-popover-foreground, rounded-lg, and overflow classes intact

3. **DropdownMenuSubContent className:** This uses `shadow-lg ring-1 ring-foreground/10`. Replace with `neu-raised` (remove both shadow-lg and ring-1 ring-foreground/10).

**dropdown-menu.tsx changes:**

1. **DropdownMenuContent (MenuPrimitive.Popup) className:** Replace shadow + ring with Neumorphism raised.
- Replace `shadow-md ring-1 ring-foreground/10` with `neu-raised`
- Keep all animation classes, bg-popover, text-popover-foreground, rounded-lg, overflow, and p-1 intact

2. **DropdownMenuSubContent className (if separate):** Already handled via the DropdownMenuContent wrapper — verify the subcontent className in select.tsx also uses `neu-raised` instead of `shadow-lg ring-1 ring-foreground/10`.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --testPathPattern="neumorphism-surface" --verbose 2>&1 | grep -E "(NEU-08|NEU-09)"</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/select.tsx SelectTrigger contains `neu-inset` and `border-transparent`
    - components/ui/select.tsx SelectContent contains `neu-raised`
    - components/ui/select.tsx does NOT contain `shadow-md ring-1 ring-foreground/10`
    - components/ui/select.tsx does NOT contain `dark:bg-input/30`
    - components/ui/dropdown-menu.tsx DropdownMenuContent contains `neu-raised`
    - components/ui/dropdown-menu.tsx does NOT contain `shadow-md ring-1 ring-foreground/10`
    - NEU-08 and NEU-09 tests pass
  </acceptance_criteria>
  <done>Select trigger uses .neu-inset, select content and dropdown menu content use .neu-raised. All shadow-md + ring patterns removed. NEU-08 and NEU-09 pass.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Neumorphism to Badge and verify RESOURCE_PALETTE preservation</name>
  <files>components/ui/badge.tsx</files>
  <read_first>
    - components/ui/badge.tsx (current state)
    - components/booking-calendar.tsx (verify RESOURCE_PALETTE exists — DO NOT modify this file)
  </read_first>
  <action>
**badge.tsx changes:**

The badge base class in `badgeVariants` cva currently has `border border-transparent` which is already correct for Neumorphism. Minimal changes needed:

1. In the base class string, after `border border-transparent`, add `neu-raised` to give badges a subtle raised appearance.

2. In the `outline` variant, change:
```
"border-border text-foreground [a]:hover:bg-muted [a]:hover:text-muted-foreground"
```
to:
```
"border-transparent text-foreground neu-inset [a]:hover:text-neu-accent"
```
(Outline badges become inset rather than bordered.)

3. Remove `dark:bg-destructive/20 dark:focus-visible:ring-destructive/40` from destructive variant and `dark:hover:bg-muted/50` from ghost variant (Neumorphism tokens handle dark mode).

4. Remove `dark:aria-invalid:ring-destructive/40` from base class (dark override no longer needed).

**RESOURCE_PALETTE verification:**
- Read `components/booking-calendar.tsx` to confirm `RESOURCE_PALETTE` is present
- Do NOT modify booking-calendar.tsx — NEU-11 requires it to be preserved unchanged
- The test assertion in neumorphism-surface.test.ts simply checks that `RESOURCE_PALETTE` string exists in the file
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest --testPathPattern="neumorphism-surface" --verbose 2>&1 | grep -E "NEU-11"</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/badge.tsx contains `neu-raised` in base class
    - components/ui/badge.tsx outline variant contains `neu-inset`
    - components/ui/badge.tsx does NOT contain `dark:bg-destructive/20`
    - components/booking-calendar.tsx still contains `RESOURCE_PALETTE` (unchanged)
    - NEU-11 test passes
  </acceptance_criteria>
  <done>Badge has Neumorphism classes. RESOURCE_PALETTE preserved in booking-calendar.tsx. NEU-11 passes.</done>
</task>

</tasks>

<verification>
- `npx jest --testPathPattern="neumorphism-surface"` — NEU-07, NEU-08, NEU-09, NEU-11 now pass
- `npx jest` — full suite regression check
</verification>

<success_criteria>
- Dialog, Sheet, Select, DropdownMenu, Badge all use Neumorphism classes
- No ring-1 ring-foreground/10 or shadow-md remains in modified components
- All dark: overrides removed from modified components
- RESOURCE_PALETTE untouched in booking-calendar.tsx
- NEU-07, NEU-08, NEU-09, NEU-11 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-refactor-the-ui-design-of-the-entire-project-to-a-neumorphism-soft-ui-style-with-light-and-dark-themes/01-03-SUMMARY.md`
</output>
