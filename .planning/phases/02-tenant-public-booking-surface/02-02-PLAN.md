---
phase: 02-tenant-public-booking-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - components/tenant-public-page.tsx
autonomous: true
requirements:
  - BOOK-01
  - BOOK-04
  - BOOK-05

must_haves:
  truths:
    - "tenant-public-page.tsx has zero hardcoded zinc/slate dual-class pairs"
    - "Root container uses bg-background (not bg-white or bg-zinc-950)"
    - "Niche brand accent colors (blue-600, pink-600, orange-600, green-600) are untouched"
    - "Footer remains bg-zinc-900 with intentional comment"
    - "COLORS.heroBtn bg-white values are preserved"
  artifacts:
    - path: "components/tenant-public-page.tsx"
      provides: "Fully remediated tenant public page"
      contains: "bg-background"
  key_links:
    - from: "components/tenant-public-page.tsx"
      to: "globals.css token system"
      via: "Tailwind utility classes"
      pattern: "bg-background|bg-card|text-foreground|text-muted-foreground|border-border"
---

<objective>
Remediate all hardcoded zinc/slate color classes in `components/tenant-public-page.tsx` with semantic shadcn/ui tokens.

Purpose: BOOK-01 (34+ dual-class pairs), BOOK-04 (preserve niche accents), BOOK-05 (root bg-background). This is the heaviest file in Phase 2.

Output: `tenant-public-page.tsx` with zero zinc/slate neutrals outside the intentional footer and COLORS map.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-public-booking-surface/02-RESEARCH.md
@.planning/phases/02-tenant-public-booking-surface/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded zinc/slate classes in tenant-public-page.tsx with semantic tokens</name>
  <files>components/tenant-public-page.tsx</files>
  <read_first>
    components/tenant-public-page.tsx
    .planning/phases/02-tenant-public-booking-surface/02-RESEARCH.md
    __tests__/booking-surface.test.ts
  </read_first>
  <action>
Open `components/tenant-public-page.tsx` and make the following replacements. Work section by section. DO NOT touch any classes inside the `COLORS` constant object (niche brand accents). DO NOT touch the footer `bg-zinc-900 text-zinc-400` (intentional fixed dark surface).

**Root container (~line 206):**
- `bg-white text-slate-900 dark:bg-zinc-950 dark:text-slate-50` --> `bg-background text-foreground`

**Sticky header (~line 209):**
- `bg-white dark:bg-zinc-900` --> `bg-card`
- `border-zinc-200 dark:border-zinc-800` --> `border-border`

**Header business name (~line 220):**
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

**Header nav links (~lines 229, 234, 239):**
- `text-zinc-400 hover:text-zinc-700` --> `text-muted-foreground hover:text-foreground`

**Contact info bar (~line 313):**
- Keep `${colors.border} ${colors.light}` (brand accents for light mode)
- Replace `dark:border-zinc-800 dark:bg-zinc-900/50` --> `dark:border-border dark:bg-card/50`

**Services scroll cards (~line 395):**
- `dark:bg-zinc-900 dark:border-zinc-800` --> `dark:bg-card dark:border-border`

**Service card title (~line 397):**
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

**Service card description (~line 399):**
- `text-zinc-500 dark:text-zinc-400` --> `text-muted-foreground`

**Duration badge (~line 407):**
- `bg-white dark:bg-zinc-800 text-zinc-500 dark:text-zinc-400 border border-zinc-200 dark:border-zinc-700` --> `bg-background text-muted-foreground border-border`

**Gallery placeholder (~lines 424-425):**
- `dark:border-zinc-800 dark:bg-zinc-900/50` --> `dark:border-border dark:bg-card/50`
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

**Menu placeholder (~lines 437-438):**
- Same pattern as gallery: `dark:border-zinc-800 dark:bg-zinc-900/50` --> `dark:border-border dark:bg-card/50`
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

**Pricing section (~line 447):**
- `border-zinc-200 dark:border-zinc-800` --> `border-border`
- `divide-zinc-100 dark:divide-zinc-800` --> `divide-border`

**Pricing rows (~line 449):**
- `bg-white dark:bg-zinc-900 hover:bg-zinc-50 dark:hover:bg-zinc-800` --> `bg-card hover:bg-muted`

**Pricing service name (~line 451):**
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

**Pricing description (~line 453):**
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

**Pricing price (~line 462):**
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

**Booking section container (~line 471):**
- `dark:bg-zinc-900 dark:border-zinc-800` --> `dark:bg-card dark:border-border`

**Booking section title (~line 472):**
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

**Booking section subtitle (~line 473):**
- `text-zinc-500 dark:text-zinc-400` --> `text-muted-foreground`

**Booking helper text (~lines 479, 480):**
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`
- `text-zinc-300 dark:text-zinc-500` --> `text-muted-foreground`

**Footer (~line 498):**
- DO NOT change `bg-zinc-900 text-zinc-400` -- this is intentional fixed dark footer
- ADD a JS comment above the footer JSX: `{/* intentional: fixed dark footer surface -- brand design choice, dark in both modes */}`

**SectionHeading sub-component (~line 573):**
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

**ResourceCard sub-component (~line 609):**
- `border-zinc-100 dark:border-zinc-800 bg-white dark:bg-zinc-900 hover:border-zinc-200 dark:hover:border-zinc-700 hover:shadow-zinc-100 dark:hover:shadow-zinc-900/50` --> `border-border bg-card hover:border-border/80 hover:shadow-md`

**ResourceCard name (~line 623):**
- `text-zinc-900 dark:text-slate-50` --> `text-foreground`

**ResourceCard specialties (~line 625):**
- `text-zinc-500 dark:text-zinc-400` --> `text-muted-foreground`

**ResourceCard meta (~line 649):**
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

**InfoBlock sub-component (~line 670):**
- `text-zinc-400 dark:text-zinc-500` --> `text-muted-foreground`

**InfoBlock value (~lines 672, 676):**
- `text-zinc-800 dark:text-slate-200` --> `text-foreground`

After all replacements, run the test suite to verify BOOK-01, BOOK-04, BOOK-05 assertions pass.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/booking-surface.test.ts --no-coverage -t "BOOK-01|BOOK-04|BOOK-05" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `components/tenant-public-page.tsx` does NOT contain `bg-zinc-` except on the footer line (which contains `bg-zinc-900` alongside text containing "footer" or preceded by "intentional" comment)
    - `components/tenant-public-page.tsx` does NOT contain `dark:bg-zinc-`
    - `components/tenant-public-page.tsx` does NOT contain `text-zinc-`
    - `components/tenant-public-page.tsx` does NOT contain `border-zinc-`
    - `components/tenant-public-page.tsx` does NOT contain `text-slate-`
    - `components/tenant-public-page.tsx` does NOT contain `bg-slate-`
    - `components/tenant-public-page.tsx` does NOT contain `divide-zinc-`
    - `components/tenant-public-page.tsx` contains `bg-background` (BOOK-05 root container)
    - `components/tenant-public-page.tsx` contains `bg-card` (header, service cards, pricing)
    - `components/tenant-public-page.tsx` contains `text-foreground` (headings)
    - `components/tenant-public-page.tsx` contains `text-muted-foreground` (secondary text)
    - `components/tenant-public-page.tsx` contains `border-border` (borders)
    - `components/tenant-public-page.tsx` contains `bg-blue-600` (niche accent preserved)
    - `components/tenant-public-page.tsx` contains `bg-pink-600` (niche accent preserved)
    - `components/tenant-public-page.tsx` contains `bg-orange-600` (niche accent preserved)
    - `components/tenant-public-page.tsx` contains `bg-green-600` (niche accent preserved)
    - `components/tenant-public-page.tsx` contains `intentional` (footer comment)
    - BOOK-01, BOOK-04, BOOK-05 tests pass in `npx jest __tests__/booking-surface.test.ts --no-coverage`
  </acceptance_criteria>
  <done>All 34+ hardcoded zinc/slate dual-class pairs in tenant-public-page.tsx replaced with single semantic tokens. Root container uses bg-background (BOOK-05). All niche brand accents preserved (BOOK-04). Footer intentionally preserved with comment. BOOK-01, BOOK-04, BOOK-05 tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/booking-surface.test.ts --no-coverage -t "BOOK-01|BOOK-04|BOOK-05"` all pass
- `grep -c "zinc\|slate" components/tenant-public-page.tsx` returns count that only includes footer line and COLORS map entries
- `grep "bg-background" components/tenant-public-page.tsx` returns at least one match (root container)
</verification>

<success_criteria>
- Zero hardcoded zinc/slate neutral classes in tenant-public-page.tsx JSX (outside COLORS map and footer)
- Root container uses bg-background text-foreground
- All 4 niche accent color families present in COLORS map
- Footer preserved as intentional fixed dark surface with comment
- BOOK-01, BOOK-04, BOOK-05 test assertions pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-tenant-public-booking-surface/02-02-SUMMARY.md`
</output>
