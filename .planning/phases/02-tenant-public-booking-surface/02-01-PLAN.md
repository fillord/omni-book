---
phase: 02-tenant-public-booking-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/booking-surface.test.ts
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
  - BOOK-03
  - BOOK-04
  - BOOK-05

must_haves:
  truths:
    - "Test file exists and runs with npx jest"
    - "All 5 BOOK requirements have at least one test case"
    - "Tests fail RED against current un-remediated source files"
  artifacts:
    - path: "__tests__/booking-surface.test.ts"
      provides: "Static file scan assertions for BOOK-01 through BOOK-05"
      min_lines: 80
  key_links:
    - from: "__tests__/booking-surface.test.ts"
      to: "components/tenant-public-page.tsx"
      via: "fs.readFileSync"
      pattern: "readComponent.*tenant-public-page"
    - from: "__tests__/booking-surface.test.ts"
      to: "components/booking-form.tsx"
      via: "fs.readFileSync"
      pattern: "readComponent.*booking-form"
    - from: "__tests__/booking-surface.test.ts"
      to: "components/booking-calendar.tsx"
      via: "fs.readFileSync"
      pattern: "readComponent.*booking-calendar"
---

<objective>
Create the test scaffold for Phase 2 booking surface requirements (BOOK-01 through BOOK-05).

Purpose: Tests must exist BEFORE remediation so we can confirm RED state (failures against current code) then GREEN state after fixes. Follows same pattern as Phase 1 `__tests__/landing-surface.test.ts`.

Output: `__tests__/booking-surface.test.ts` with static file scan assertions for all 5 requirements.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tenant-public-booking-surface/02-RESEARCH.md

<interfaces>
<!-- Phase 1 test pattern to follow exactly -->
From __tests__/landing-surface.test.ts:
```typescript
import fs from "fs";
import path from "path";

const LANDING_DIR = path.resolve(__dirname, "..", "components", "landing");
const readComponent = (name: string) =>
  fs.readFileSync(path.join(LANDING_DIR, name), "utf-8");
```
Pattern: fs.readFileSync static content assertions with regex negative lookbehind for `dark:` prefix handling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create booking-surface.test.ts with BOOK-01 through BOOK-05 assertions</name>
  <files>__tests__/booking-surface.test.ts</files>
  <read_first>
    __tests__/landing-surface.test.ts
    components/tenant-public-page.tsx
    components/booking-form.tsx
    components/booking-calendar.tsx
  </read_first>
  <action>
Create `__tests__/booking-surface.test.ts` following the exact pattern from `landing-surface.test.ts`. Use `fs.readFileSync` with `path.resolve(__dirname, '..', 'components')` base path.

Helper function:
```typescript
const COMPONENTS_DIR = path.resolve(__dirname, '..', 'components');
const readComponent = (name: string) =>
  fs.readFileSync(path.join(COMPONENTS_DIR, name), 'utf-8');
```

**BOOK-01 tests** (tenant-public-page.tsx):
- Assert NO `bg-white` appears OUTSIDE the COLORS object. Strategy: split file content at the closing `}` of the COLORS constant (or use a regex that matches `bg-white` only in JSX lines, not inside string literals within the COLORS map). Simplest approach: assert that any `bg-white` occurrence is preceded by a quote character on the same line (meaning it is inside a string literal in COLORS). Alternative: count occurrences — after remediation, `bg-white` should ONLY appear inside the COLORS map heroBtn values. Assert `bg-white` count equals the number of niche heroBtn entries (4 niches = 4 occurrences).
- Assert NO `bg-zinc-*` classes (pattern: `/(?<!dark:)bg-zinc-\d+/` — but actually after remediation there should be zero `bg-zinc-*` anywhere including `dark:bg-zinc-*` since all dual pairs are replaced with single semantic tokens). Use pattern: `/bg-zinc-\d+/` to catch any remaining zinc backgrounds.
- Assert NO `bg-slate-*` backgrounds: `/bg-slate-\d+/`
- Assert NO `dark:bg-zinc-*` dual pairs: `/dark:bg-zinc-\d+/`
- Assert NO `text-zinc-*` neutrals: `/text-zinc-\d+/` and `/text-slate-\d+/`
- Assert NO `border-zinc-*` neutrals: `/border-zinc-\d+/`
- Exception: `bg-zinc-900` in the footer line is intentional. Use a scoped test: assert the ONLY occurrence of `bg-zinc-900` in the file is on a line also containing "footer" (case-insensitive) or preceded by an "intentional" comment.

**BOOK-02 tests** (booking-form.tsx):
- Assert NO `bg-white` appears in the file. (booking-form has no intentional bg-white exceptions — the SuccessScreen green circle uses `bg-green-100` not `bg-white`.)
- Assert NO `bg-zinc-*` classes: `/bg-zinc-\d+/`
- Assert NO `border-zinc-*` classes: `/border-zinc-\d+/`
- Assert NO `text-zinc-*` or `text-slate-*` classes: `/text-zinc-\d+/`, `/text-slate-\d+/`
- Assert NO `dark:bg-zinc-*` or `dark:border-zinc-*` or `dark:text-zinc-*` dual pairs
- Assert NO `hover:bg-zinc-*` hover neutrals: `/hover:bg-zinc-\d+/`

**BOOK-03 tests** (booking-calendar.tsx):
- Assert NO `bg-gray-400` appears: exact string match `.not.toContain('bg-gray-400')`
- Assert NO bare `bg-zinc-*`, `text-zinc-*`, `border-zinc-*` neutrals (these should not exist in calendar)
- Assert RESOURCE_PALETTE comment exists: `.toContain('intentional')` (the intentional functional palette comment)

**BOOK-04 tests** (brand accent preservation):
- Assert `tenant-public-page.tsx` contains `bg-blue-600` (blue niche accent)
- Assert `tenant-public-page.tsx` contains `bg-pink-600` (pink niche accent)
- Assert `tenant-public-page.tsx` contains `bg-orange-600` (orange niche accent)
- Assert `tenant-public-page.tsx` contains `bg-green-600` (green niche accent)
- Assert `booking-form.tsx` contains `bg-blue-600` (BOOKING_COLORS)
- Assert `booking-form.tsx` contains `bg-pink-600`
- Assert `booking-form.tsx` contains `bg-orange-600`
- Assert `booking-form.tsx` contains `bg-green-600`

**BOOK-05 tests** (root container):
- Assert `tenant-public-page.tsx` contains `bg-background` (the semantic root canvas)
- Assert `tenant-public-page.tsx` does NOT match `min-h-screen` on a line with `bg-white` or `bg-zinc` or `bg-slate` (the root container must use bg-background, not hardcoded)

Organize tests in `describe` blocks: one per BOOK-XX requirement, matching the Phase 1 convention.
  </action>
  <verify>
    <automated>cd /home/yola/projects/sites/omni-book && npx jest __tests__/booking-surface.test.ts --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `__tests__/booking-surface.test.ts` exists and is >80 lines
    - File contains `describe("BOOK-01`
    - File contains `describe("BOOK-02`
    - File contains `describe("BOOK-03`
    - File contains `describe("BOOK-04`
    - File contains `describe("BOOK-05`
    - File contains `fs.readFileSync`
    - File contains `tenant-public-page.tsx`
    - File contains `booking-form.tsx`
    - File contains `booking-calendar.tsx`
    - `npx jest __tests__/booking-surface.test.ts --no-coverage` exits (either pass or fail — test file is syntactically valid and runs)
  </acceptance_criteria>
  <done>Test file runs with npx jest. All 5 BOOK requirements have describe blocks with assertions. Tests expected to mostly FAIL (RED) against current un-remediated source — this confirms they are testing real violations.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/booking-surface.test.ts --no-coverage` runs without syntax errors
- Test output shows describe blocks for BOOK-01 through BOOK-05
- BOOK-04 (brand accent preservation) tests should PASS even before remediation (accents already present)
- BOOK-01, BOOK-02, BOOK-03, BOOK-05 tests should mostly FAIL (violations still in source)
</verification>

<success_criteria>
- `__tests__/booking-surface.test.ts` exists with >80 lines
- All 5 BOOK-XX requirements covered with at least one test each
- Test file executes without syntax errors via `npx jest`
</success_criteria>

<output>
After completion, create `.planning/phases/02-tenant-public-booking-surface/02-01-SUMMARY.md`
</output>
