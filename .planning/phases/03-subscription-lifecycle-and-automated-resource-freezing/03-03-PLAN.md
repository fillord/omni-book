---
phase: 03-subscription-lifecycle-and-automated-resource-freezing
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/dashboard/settings/billing/billing-content.tsx
  - app/dashboard/settings/billing/page.tsx
  - lib/actions/billing.ts
  - lib/actions/admin.ts
  - app/admin/tenants/[tenantId]/page.tsx
  - app/admin/tenants/[tenantId]/activate-subscription-form.tsx
  - lib/i18n/translations.ts
autonomous: true
requirements: [SUB-04, SUB-05, SUB-06]

must_haves:
  truths:
    - "Billing page shows subscription expiry date when plan is PRO/ACTIVE"
    - "Billing page shows prominent EXPIRED alert with frozen data warning"
    - "Tenant can request renewal via renewSubscription action (sets PENDING + Telegram)"
    - "Super-admin can activate subscription (sets PRO/ACTIVE, 30-day expiry, unfreezes all)"
    - "Admin tenant detail page has an Activate Subscription button"
  artifacts:
    - path: "app/dashboard/settings/billing/billing-content.tsx"
      provides: "Expiry date display + EXPIRED alert block"
      contains: "subscriptionExpiresAt"
    - path: "lib/actions/billing.ts"
      provides: "renewSubscription server action"
      contains: "renewSubscription"
    - path: "lib/actions/admin.ts"
      provides: "activateSubscription server action with unfreeze"
      contains: "activateSubscription"
    - path: "app/admin/tenants/[tenantId]/activate-subscription-form.tsx"
      provides: "Client component for activate subscription button"
      contains: "activateSubscription"
    - path: "app/admin/tenants/[tenantId]/page.tsx"
      provides: "Admin page with activate subscription UI"
      contains: "ActivateSubscription"
  key_links:
    - from: "lib/actions/admin.ts"
      to: "prisma.resource.updateMany"
      via: "bulk unfreeze on activation"
      pattern: "isFrozen: false"
    - from: "lib/actions/admin.ts"
      to: "prisma.tenant.update"
      via: "set PRO + ACTIVE + 30-day expiry"
      pattern: "subscriptionExpiresAt"
    - from: "billing-content.tsx"
      to: "renewSubscription"
      via: "import and call from billing action"
      pattern: "renewSubscription"
    - from: "app/admin/tenants/[tenantId]/page.tsx"
      to: "activate-subscription-form.tsx"
      via: "client component import"
      pattern: "ActivateSubscriptionForm"
---

<objective>
Enhance the billing page with expiry date display and EXPIRED alert, add renewSubscription action, implement super-admin activateSubscription with bulk unfreeze, and wire the admin tenant detail page with activation UI.

Purpose: Complete the subscription lifecycle — tenants see expiry status and can request renewal; super-admin can activate subscriptions and unfreeze all frozen data.
Output: Updated billing page, new/updated server actions, admin activation UI.
</objective>

<execution_context>
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/workflows/execute-plan.md
@/home/yola/projects/sites/omni-book/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subscription-lifecycle-and-automated-resource-freezing/03-CONTEXT.md
@.planning/phases/03-subscription-lifecycle-and-automated-resource-freezing/03-RESEARCH.md
@.planning/phases/03-subscription-lifecycle-and-automated-resource-freezing/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs -->

From app/dashboard/settings/billing/billing-content.tsx (current):
```typescript
type TenantInfo = {
  plan: string
  planStatus: string
}
// MUST be extended to include subscriptionExpiresAt: Date | null

export function BillingContent({ tenant }: { tenant: TenantInfo }) {
  // ...
}
```

From app/dashboard/settings/billing/page.tsx (current):
```typescript
// Currently passes full tenant object to BillingContent
// The tenant query returns ALL fields (no select clause)
const tenant = await prisma.tenant.findUnique({
  where: { id: session.user.tenantId }
})
// So subscriptionExpiresAt is already available — just need to update TenantInfo type
```

From lib/actions/billing.ts (current):
```typescript
export async function requestProActivation() {
  // Sets planStatus to PENDING + Telegram notification
  // Pattern to replicate for renewSubscription
}
```

From lib/actions/admin.ts (current):
```typescript
async function ensureSuperAdmin() { ... }
const PLAN_DEFAULT_MAX_RESOURCES: Record<Plan, number> = { FREE: 1, PRO: 20, ENTERPRISE: 100 }
export async function updateTenantPlan(tenantId: string, plan: Plan, planStatus: PlanStatus) { ... }
export async function banTenant(tenantId: string) { ... }
export async function deleteTenant(tenantId: string) { ... }
```

From lib/actions/audit-log.ts:
```typescript
export type AuditEventType = 'login' | 'plan_upgrade' | 'plan_downgrade' | 'service_deleted' | 'resource_deleted' | 'staff_deleted'
```

From app/admin/tenants/[tenantId]/send-notification-form.tsx (pattern to replicate):
```typescript
// Client component pattern: 'use client', useTransition, calls server action, toast feedback
```

From app/admin/tenants/[tenantId]/page.tsx (current):
```typescript
// Server component. Fetches tenant, services, resources, staff.
// Currently selects: id, name, slug, plan, planStatus, email, phone, createdAt, niche
// MUST add subscriptionExpiresAt to select
// Has <SendNotificationForm tenantId={tenantId} /> at bottom
// Add <ActivateSubscriptionForm> in similar position
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Billing page expiry display, EXPIRED alert, and renewSubscription action</name>
  <files>
    app/dashboard/settings/billing/billing-content.tsx
    app/dashboard/settings/billing/page.tsx
    lib/actions/billing.ts
  </files>
  <read_first>
    app/dashboard/settings/billing/billing-content.tsx
    app/dashboard/settings/billing/page.tsx
    lib/actions/billing.ts
  </read_first>
  <action>
**1. Update TenantInfo type in billing-content.tsx:**

Change from:
```typescript
type TenantInfo = {
  plan: string
  planStatus: string
}
```
to:
```typescript
type TenantInfo = {
  plan: string
  planStatus: string
  subscriptionExpiresAt: Date | null
}
```

**2. Add expiry date display in billing-content.tsx:**

Inside the "Current Plan Card" section, after the plan name and status badges (after the `</div>` that closes the flex items-center gap-3 div), add:

```tsx
{isPro && !isExpiredOrCanceled && tenant.subscriptionExpiresAt && (
  <p className="text-sm text-muted-foreground mt-2">
    Подписка активна до: {new Date(tenant.subscriptionExpiresAt).toLocaleDateString('ru-RU', {
      day: 'numeric', month: 'long', year: 'numeric'
    })}
  </p>
)}
```

**3. Add EXPIRED alert block in billing-content.tsx:**

Before the `{showUpgradeCard && (` block, add a new alert block:

```tsx
{isExpiredOrCanceled && (
  <div className="neu-inset bg-[var(--neu-bg)] rounded-xl p-5 space-y-2">
    <div className="flex items-center gap-2 text-orange-500">
      <AlertTriangle size={20} />
      <h3 className="font-semibold text-base">Ваша подписка истекла</h3>
    </div>
    <p className="text-sm text-muted-foreground">
      Ваши данные заморожены. Продлите подписку PRO, чтобы восстановить полный доступ к ресурсам и услугам.
    </p>
  </div>
)}
```

`AlertTriangle` is already imported in billing-content.tsx.

**4. Add renewSubscription action in lib/actions/billing.ts:**

Add a new exported function after `requestProActivation`:

```typescript
export async function renewSubscription() {
  try {
    const session = await requireAuth()
    requireRole(session, ['OWNER'])
    const tenantId = session?.user?.tenantId

    if (!tenantId) {
      throw new Error('Tenant ID не найден в сессии')
    }

    const tenant = await basePrisma.tenant.findUnique({
      where: { id: tenantId }
    })

    if (!tenant) throw new Error('Бизнес не найден')

    if (tenant.planStatus === PlanStatus.PENDING) {
      return { success: false, error: 'Заявка уже находится в обработке' }
    }

    // Set to PRO + PENDING (requires super-admin confirmation)
    await basePrisma.tenant.update({
      where: { id: tenantId },
      data: {
        plan: 'PRO',
        planStatus: PlanStatus.PENDING,
      },
    })

    // Notify superadmin via Telegram (fire-and-forget)
    const adminChatId = process.env.ADMIN_TELEGRAM_CHAT_ID
    if (adminChatId) {
      const msg = [
        '🔄 <b>Заявка на продление PRO!</b>',
        `🏢 Компания: ${tenant.name}`,
        `📅 Подписка истекла: ${tenant.subscriptionExpiresAt?.toLocaleDateString('ru-RU') ?? 'неизвестно'}`,
      ].join('\n')
      sendTelegramMessage(adminChatId, msg).catch(console.error)
    }

    revalidatePath('/dashboard/settings/billing')
    return { success: true }
  } catch (error: unknown) {
    return { success: false, error: error instanceof Error ? error.message : 'Произошла ошибка при продлении' }
  }
}
```

**5. Wire renewSubscription in billing-content.tsx:**

Add import:
```typescript
import { requestProActivation, renewSubscription } from "@/lib/actions/billing"
```

In the `handlePaymentConfirm` function, detect if this is a renewal vs first activation:
```typescript
async function handlePaymentConfirm() {
  setLoading(true)
  const res = isExpiredOrCanceled
    ? await renewSubscription()
    : await requestProActivation()
  // ... rest stays the same
}
```

**6. billing/page.tsx — NO changes needed:**
The page already passes the full `tenant` object to `BillingContent` (no `select` clause on the query), so `subscriptionExpiresAt` is already included. The `TenantInfo` type extension in billing-content.tsx is sufficient.
  </action>
  <verify>
    <automated>npx jest __tests__/subscription-lifecycle-surface.test.ts -t "billing" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `billing-content.tsx` TenantInfo type includes `subscriptionExpiresAt: Date | null`
    - `billing-content.tsx` contains `Подписка активна до:` string
    - `billing-content.tsx` contains `Ваша подписка истекла` string in EXPIRED alert
    - `billing-content.tsx` contains `neu-inset bg-[var(--neu-bg)]` on the EXPIRED alert block
    - `billing-content.tsx` imports `renewSubscription` from billing actions
    - `lib/actions/billing.ts` exports `renewSubscription` function
    - `lib/actions/billing.ts` renewSubscription contains `planStatus: PlanStatus.PENDING`
    - `lib/actions/billing.ts` renewSubscription contains `sendTelegramMessage` call
    - `lib/actions/billing.ts` renewSubscription contains `Заявка на продление PRO` in Telegram message
  </acceptance_criteria>
  <done>Billing page shows expiry date for active PRO, prominent EXPIRED alert, and renewal action routes through renewSubscription with Telegram notification.</done>
</task>

<task type="auto">
  <name>Task 2: Super-admin activateSubscription action with bulk unfreeze</name>
  <files>
    lib/actions/admin.ts
  </files>
  <read_first>
    lib/actions/admin.ts
    lib/actions/audit-log.ts
  </read_first>
  <action>
Add `activateSubscription` exported function to `lib/actions/admin.ts`. Place it after the existing `updateTenantPlan` function.

```typescript
export async function activateSubscription(tenantId: string) {
  try {
    await ensureSuperAdmin()

    const expiresAt = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000) // 30 days from now

    await basePrisma.$transaction([
      basePrisma.tenant.update({
        where: { id: tenantId },
        data: {
          plan: 'PRO',
          planStatus: 'ACTIVE',
          subscriptionExpiresAt: expiresAt,
          maxResources: PLAN_DEFAULT_MAX_RESOURCES.PRO, // 20
        },
      }),
      basePrisma.resource.updateMany({
        where: { tenantId, isFrozen: true },
        data: { isFrozen: false },
      }),
      basePrisma.service.updateMany({
        where: { tenantId, isFrozen: true },
        data: { isFrozen: false },
      }),
    ])

    // Audit log (fire-and-forget)
    createAuditLog(tenantId, 'plan_upgrade', {
      activatedBy: 'superadmin',
      newExpiry: expiresAt.toISOString(),
    })

    revalidatePath(`/admin/tenants/${tenantId}`)
    revalidatePath('/admin/tenants')
    revalidatePath('/dashboard/settings/billing')
    return { success: true }
  } catch (error: unknown) {
    return { error: error instanceof Error ? error.message : 'Ошибка активации подписки' }
  }
}
```

Key details:
- Uses `basePrisma.$transaction([...])` for atomic tenant update + bulk unfreeze of both resources and services
- Sets `maxResources` to `PLAN_DEFAULT_MAX_RESOURCES.PRO` (which is 20, already defined in admin.ts)
- Sets `subscriptionExpiresAt` to 30 days from now: `new Date(Date.now() + 30 * 24 * 60 * 60 * 1000)`
- `isFrozen: false` on both resource.updateMany and service.updateMany
- Audit log uses existing `'plan_upgrade'` event type with `{ activatedBy: 'superadmin', newExpiry }` details
- Revalidates admin tenant page, tenant list, AND billing page
- `createAuditLog` is already imported at the top of admin.ts (verified)
  </action>
  <verify>
    <automated>npx jest __tests__/subscription-lifecycle-surface.test.ts -t "admin" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `lib/actions/admin.ts` contains `export async function activateSubscription(tenantId: string)`
    - `lib/actions/admin.ts` contains `basePrisma.$transaction([` for atomic operation
    - `lib/actions/admin.ts` contains `isFrozen: false` (unfreeze logic) at least twice (resource + service)
    - `lib/actions/admin.ts` contains `subscriptionExpiresAt: expiresAt`
    - `lib/actions/admin.ts` contains `PLAN_DEFAULT_MAX_RESOURCES.PRO` or `maxResources: 20`
    - `lib/actions/admin.ts` contains `createAuditLog(tenantId, 'plan_upgrade'`
    - `lib/actions/admin.ts` contains `activatedBy: 'superadmin'` in audit details
  </acceptance_criteria>
  <done>activateSubscription action atomically activates PRO, sets 30-day expiry, unfreezes all resources/services, and creates audit log.</done>
</task>

<task type="auto">
  <name>Task 3: Admin tenant detail page — activation UI + i18n</name>
  <files>
    app/admin/tenants/[tenantId]/activate-subscription-form.tsx
    app/admin/tenants/[tenantId]/page.tsx
    lib/i18n/translations.ts
  </files>
  <read_first>
    app/admin/tenants/[tenantId]/page.tsx
    app/admin/tenants/[tenantId]/send-notification-form.tsx
    lib/i18n/translations.ts
  </read_first>
  <action>
**1. Create `app/admin/tenants/[tenantId]/activate-subscription-form.tsx`:**

Follow the same client component pattern as `send-notification-form.tsx`:

```tsx
'use client'

import { useState, useTransition } from 'react'
import { Button } from '@/components/ui/button'
import { ShieldCheck } from 'lucide-react'
import { toast } from 'sonner'
import { activateSubscription } from '@/lib/actions/admin'

export function ActivateSubscriptionForm({ tenantId }: { tenantId: string }) {
  const [isPending, startTransition] = useTransition()
  const [confirmed, setConfirmed] = useState(false)

  function handleActivate() {
    if (!confirmed) {
      setConfirmed(true)
      return
    }
    startTransition(async () => {
      const result = await activateSubscription(tenantId)
      if (result.error) {
        toast.error(result.error)
      } else {
        toast.success('Подписка активирована на 30 дней!')
        setConfirmed(false)
      }
    })
  }

  return (
    <div className="flex items-center gap-3">
      <Button
        onClick={handleActivate}
        disabled={isPending}
        className={confirmed
          ? 'bg-emerald-600 hover:bg-emerald-700 text-white'
          : ''
        }
      >
        <ShieldCheck className="h-4 w-4 mr-2" />
        {isPending
          ? 'Активация...'
          : confirmed
            ? 'Подтвердить активацию PRO (30 дней)'
            : 'Активировать подписку PRO'
        }
      </Button>
      {confirmed && (
        <Button variant="ghost" onClick={() => setConfirmed(false)} disabled={isPending}>
          Отмена
        </Button>
      )}
    </div>
  )
}
```

Uses a two-click confirmation pattern (first click shows confirm, second click activates) — same UX safety pattern as delete actions.

**2. Update `app/admin/tenants/[tenantId]/page.tsx`:**

a) Add import at the top:
```typescript
import { ActivateSubscriptionForm } from './activate-subscription-form'
```

b) Add `subscriptionExpiresAt: true` to the tenant select query (in the `basePrisma.tenant.findUnique` select object, after `niche: true`):
```typescript
select: {
  id: true,
  name: true,
  slug: true,
  plan: true,
  planStatus: true,
  email: true,
  phone: true,
  createdAt: true,
  niche: true,
  subscriptionExpiresAt: true,  // <-- add this
},
```

c) Add the activate subscription block BEFORE the "Send Notification" block (before `{/* Send Notification */}`):

```tsx
{/* Activate Subscription (super-admin) */}
<div className="neu-raised bg-[var(--neu-bg)] rounded-xl p-6 space-y-3">
  <h2 className="text-base font-semibold text-foreground">Управление подпиской</h2>
  {tenant.subscriptionExpiresAt && (
    <p className="text-sm text-muted-foreground">
      Подписка до: {new Date(tenant.subscriptionExpiresAt).toLocaleDateString('ru-RU', {
        day: 'numeric', month: 'long', year: 'numeric'
      })}
    </p>
  )}
  <p className="text-sm text-muted-foreground">
    Текущий статус: {statusLabels[tenant.planStatus] ?? tenant.planStatus}
  </p>
  <ActivateSubscriptionForm tenantId={tenantId} />
</div>
```

**3. Add i18n translations in `lib/i18n/translations.ts`:**

Add a `subscription` key to each locale under a new section. Find where translations end for each locale and add:

For `ru`:
```typescript
subscription: {
  frozen: 'Заморожен',
  frozenTooltip: 'Разморозьте подписку PRO, чтобы управлять этим объектом',
  expired: 'Ваша подписка истекла',
  expiredDescription: 'Ваши данные заморожены. Продлите подписку PRO, чтобы восстановить полный доступ.',
  activeUntil: 'Подписка активна до',
  staffLocked: 'Управление персоналом недоступно — подписка истекла.',
  renewRequest: 'Заявка на продление PRO',
  activated: 'Подписка активирована',
},
```

For `en`:
```typescript
subscription: {
  frozen: 'Frozen',
  frozenTooltip: 'Upgrade to PRO to manage this item',
  expired: 'Your subscription has expired',
  expiredDescription: 'Your data is frozen. Renew PRO to restore full access.',
  activeUntil: 'Subscription active until',
  staffLocked: 'Staff management unavailable — subscription expired.',
  renewRequest: 'PRO renewal request',
  activated: 'Subscription activated',
},
```

For `kz`:
```typescript
subscription: {
  frozen: 'Мұздатылған',
  frozenTooltip: 'Бұл нысанды басқару үшін PRO жазылымын жаңартыңыз',
  expired: 'Жазылым мерзімі аяқталды',
  expiredDescription: 'Деректеріңіз мұздатылған. Толық қолжетімділікті қалпына келтіру үшін PRO жазылымын жаңартыңыз.',
  activeUntil: 'Жазылым белсенді',
  staffLocked: 'Қызметкерлерді басқару қолжетімсіз — жазылым мерзімі аяқталды.',
  renewRequest: 'PRO жазылымын жаңарту өтінімі',
  activated: 'Жазылым белсендірілді',
},
```

Note: The frozen badges and alerts in Plans 02/03 use hardcoded Russian strings per the CONTEXT.md specification. The i18n entries are added here for future localization. Current UI implementation uses hardcoded strings as specified.
  </action>
  <verify>
    <automated>npx jest __tests__/subscription-lifecycle-surface.test.ts -t "admin" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/admin/tenants/[tenantId]/activate-subscription-form.tsx` exists and is a `'use client'` component
    - `activate-subscription-form.tsx` imports `activateSubscription` from `@/lib/actions/admin`
    - `activate-subscription-form.tsx` contains `ShieldCheck` icon
    - `activate-subscription-form.tsx` contains two-click pattern (`confirmed` state)
    - `app/admin/tenants/[tenantId]/page.tsx` imports `ActivateSubscriptionForm`
    - `page.tsx` tenant select query includes `subscriptionExpiresAt: true`
    - `page.tsx` renders `<ActivateSubscriptionForm tenantId={tenantId} />`
    - `page.tsx` contains `Управление подпиской` heading
    - `lib/i18n/translations.ts` contains `subscription:` key in ru, en, and kz locales
    - `lib/i18n/translations.ts` contains `frozen: 'Заморожен'` in ru locale
  </acceptance_criteria>
  <done>Admin tenant detail page has Activate Subscription UI with two-click confirmation. Expiry date visible. i18n translations added for all 3 locales.</done>
</task>

</tasks>

<verification>
- `npx jest __tests__/subscription-lifecycle-surface.test.ts -t "billing" --no-coverage` — all billing tests pass
- `npx jest __tests__/subscription-lifecycle-surface.test.ts -t "admin" --no-coverage` — all admin tests pass
- `npx jest --no-coverage` — full suite green
- `npx tsc --noEmit` — no TypeScript errors (TenantInfo type matches, activateSubscription signatures correct)
</verification>

<success_criteria>
- Billing page shows "Подписка активна до" for PRO/ACTIVE tenants
- Billing page shows "Ваша подписка истекла" EXPIRED alert with neu-inset styling
- renewSubscription action sends Telegram and sets PENDING
- activateSubscription atomically sets PRO/ACTIVE, 30-day expiry, unfreezes all
- Admin tenant detail page has activation UI with subscription management block
- i18n translations added for subscription-related strings in all 3 locales
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-lifecycle-and-automated-resource-freezing/03-03-SUMMARY.md`
</output>
